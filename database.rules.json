{
  "rules": {
    ".read": true,
    ".write": true,
    "scanningData": {
      ".read": true,
      ".write": true
    },
  "customization": {
      ".read": true,
      ".write": true
    },
    "campaigns": {
      ".indexOn": ["status", "brandName"],
      ".read": true,
      ".write": true
    },
    "rewards": {
      ".indexOn": ["status", "guestPhone", "campaignId"],
        "$rewardId": {
        ".read": "auth != null && (root.child('guest-rewards').child(auth.token.phone_number).child($rewardId).exists() || auth.token.admin === true)",
        ".write": "auth != null && auth.token.admin === true",
        ".validate": "newData.hasChildren(['typeId', 'guestPhone', 'campaignId', 'status', 'createdAt'])",
        "status": {
          ".validate": "newData.val().matches(/^(pending|active|used|expired|cancelled)$/)"
        },
        "value": {
          ".validate": "newData.isNumber()"
        },
        "expiresAt": {
          ".validate": "newData.isNumber() && newData.val() > now"
        }
      }
    },
    "guest-rewards": {
      "$phoneNumber": {
        ".read": "auth != null && auth.token.phone_number === $phoneNumber",
        ".write": "auth != null && auth.token.admin === true"
      }
    },
    "campaign-rewards": {
      "$campaignId": {
        ".read": "auth != null && auth.token.admin === true",
        ".write": "auth != null && auth.token.admin === true"
      }
    },
    "rewardTypes": {
      ".indexOn": ["status", "category"],
      ".read": true,
      ".write": true
    },
"receipts": {
  ".indexOn": ["phoneNumber", "guestPhoneNumber"],
  ".read": true,
  ".write": true
  },
  "googleReviews": {
    ".read": "auth != null",
    ".write": "auth != null && auth.token.admin === true",
    "$reviewId": {
      ".validate": "newData.hasChildren(['reviewerName', 'rating', 'text', 'timestamp'])",
      "rating": {
        ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 5"
      },
      "timestamp": {
        ".validate": "newData.isNumber()"
      }
    }
  }

}
}