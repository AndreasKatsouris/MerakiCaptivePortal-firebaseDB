# Copy this file to .env and update with your actual values

# === Twilio Configuration ===
TWILIO_SID=your_twilio_account_sid
TWILIO_TOKEN=your_twilio_auth_token
TWILIO_PHONE=your_twilio_whatsapp_number  # e.g. +14155238886

# Webhook signature verification (CRIT-07). The WhatsApp webhook verifies Twilio's
# X-Twilio-Signature (HMAC-SHA1) using TWILIO_TOKEN. Rollout is staged:
#   monitor (default) — validate + log match/mismatch, but still process requests
#   enforce           — reject (403) on invalid/missing signature (target state)
#   off               — skip the check entirely (escape hatch)
# Deploy with 'monitor', confirm in logs that real Twilio traffic validates, then
# set 'enforce'. Unset defaults to 'monitor' (never hard-breaks the live channel).
TWILIO_SIGNATURE_MODE=monitor

# The EXACT webhook URL configured in the Twilio console. REQUIRED for validation to
# pass on gen2 Cloud Functions: gen2 strips the function-name path before the request
# reaches the code (req.originalUrl is only '/'), so the URL can't be reconstructed —
# the signature is HMAC'd over the full URL Twilio called. Paste it verbatim from
# Twilio (Messaging → your WhatsApp sender → "when a message comes in"). Accepts one
# URL or a comma-separated list (valid if any matches). If unset, the code falls back
# to reconstructing the URL from request headers (works on gen1, fails on gen2).
TWILIO_WEBHOOK_URL=https://us-central1-your-project.cloudfunctions.net/receiveWhatsAppMessage

# Delivery-status callback URL. Twilio POSTs each status transition (queued →
# sent → delivered / undelivered) here, which is the ONLY way to tell "Twilio
# accepted it" apart from "WhatsApp delivered it". Leave unset and outbound
# messages simply carry no statusCallback (no behaviour change). Point it at the
# deployed whatsappStatusCallback function; the handler accepts a signature
# HMAC'd over either this URL or TWILIO_WEBHOOK_URL.
TWILIO_STATUS_CALLBACK_URL=https://us-central1-your-project.cloudfunctions.net/whatsappStatusCallback

# What to do when a template send has to degrade to a FREEFORM message (template
# missing, disabled, or rejected by Twilio). A freeform message is undeliverable
# outside WhatsApp's 24-hour customer-service window — Twilio accepts it and Meta
# silently drops it — so this degradation is an invisible outage on any
# business-initiated send (bookings, reminders, the Ross daily nudge).
#   monitor (DEFAULT) — log the degradation at ERROR, still attempt the send
#   enforce           — refuse business-initiated freeform sends (throws)
# Staged like TWILIO_SIGNATURE_MODE: leave on monitor until the ERROR logs show
# which sends degrade, fix those templates, THEN flip to enforce. An unset value
# must never be able to dark-out a live guest channel.
WHATSAPP_TEMPLATE_FALLBACK_MODE=monitor

# === WhatsApp Template ContentSids ===
# ContentSids are managed in Firebase RTDB via the admin UI.
# Go to Admin > WhatsApp Management > Templates tab to configure them.
# No env vars or redeploy needed — changes take effect within 5 minutes.
# NOTE: a template with no ContentSid (or enabled=false) silently degrades to
# freeform — see WHATSAPP_TEMPLATE_FALLBACK_MODE above. As of 2026-07-26 only
# admin_new_booking_notification and ross_daily_digest were configured.

# === Google APIs ===
GOOGLE_PLACES_API_KEY=your_google_places_api_key
GOOGLE_PLACE_ID=your_google_place_id

# === SendGrid Configuration ===
SENDGRID_API_KEY=your_sendgrid_api_key_here
