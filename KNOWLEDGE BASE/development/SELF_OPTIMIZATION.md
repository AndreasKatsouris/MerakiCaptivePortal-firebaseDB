# Self-Optimization Patterns

Workflow patterns that improve agent efficiency. Reviewed at session start, updated at session end.
Patterns promoted from "observed" to "validated" after 3+ confirmations.
Max 15 patterns — unvalidated patterns older than 30 days are dropped.

## How to Use

1. **Session start:** Scan validated patterns for anything relevant to the current task
2. **During work:** Apply relevant patterns proactively
3. **Session end:** Add new patterns as "observed", increment count on existing ones, promote at 3+

## Validated Patterns

| Pattern | Why | Confirmed |
|---------|-----|-----------|
| Parallelize independent Firebase reads with `Promise.all()` | Sequential reads compound latency on slow connections — 10 locations = 10 round trips vs 1 | 3x (2026-04-08 → 2026-04-13) |
| Verify field names against the actual server seed / CF write path — not the KB doc — when wiring a v2 read or pick-then-act flow | PR #28: `t.id` was undefined (server uses `templateId`). PR #30: caught before code via grep of allowedFields. PR #32: verified `VALID_INPUT_TYPES` + `allowedTaskFields` against `functions/ross.js` lines 25/667/718 before any client code. PR #35: verified `subtasks` storage path + `rossUpdateTemplate.allowedFields` includes `subtasks` (means the new fields ride along automatically — no template CF change needed) | 4x (PR #28 → #30 → #32 → #35) |
| Inline-confirm pattern variants by destructive scope: row-inline (People delete), slide-down strip on card (Playbook workflow + template + task delete), modal (TBD HfConfirm — bulk actions only) | One pattern doesn't fit every destructive action. PR #32 reused the slide-down strip a third time inside the per-task editor row | 3x (PR #28 → #30 → #32) |
| Read all four feedback files (PROJECT_BACKLOG + SELF_OPT + LESSONS + SCORECARD) at session start, not just PROJECT_BACKLOG | Backlog gives the goal; the other three give the *how*. PR #30 needed user prompt to make this fire; PR #32 was self-initiated; PR #35 was reflexive. **Still drifting in fresh sessions:** PR #53 session — read PROJECT_BACKLOG + project_sprint.md (memory) at start, declared sprint goal, went to brainstorming. User caught it ("are you up to date with your workflow claude.md?") before any code was written, but after spec+plan were drafted. Working memory still bridges across same-session conversations but not across `/clear` boundaries. CLAUDE.md mandates the read at step 3 — the literal protocol is right; the gap is between memory-files (auto-loaded MEMORY.md) and KB feedback files (manual reads). | 4x (PR #30 → #32 → #35 → #53 with re-prompt) |
| Fold-in scope rule: same module / same concern / small → FOLD; different module → BACKLOG | PR #48 (footer fix folded; access-control deferred). PR #51 (5 folded, 3 deferred). PR #53 (3 review fixes folded). PR #55 (5+ review fixes folded). PR #58 (M1+M2 constants, I1+I5+M2 helper extract, style booleans, VALID_INPUT_TYPES dedup, buildTaskFromSubtask dedup, 1500ms timeout cap — 6 folded; registerUser callable bug correctly NOT folded with rationale). Heuristic stable across 5 PRs. | 5x (PR #48 → #51 → #53 → #55 → #58) |
| `gh pr edit --body` on scope expansion (NOT a PR comment) | PR #51 used a comment; PR #53 + #55 + #58 reflexively used `gh pr edit --body`. PR #58 refreshed the body twice mid-PR — once after the pivot, once after the final review fold-in. Comments are ephemeral; the body is what reviewers + future agents read first. | 4x (PR #51 → #53 → #55 → #58) |
| Subagent-driven-development + detailed plan eliminates ambiguity to near-zero | PR #51 (11 tasks), PR #53 (4 tasks), PR #55 (11 tasks), PR #58 (7 tasks + pivot + fold-ins, 18 subagent dispatches) — all hit ZERO BLOCKED/NEEDS_CONTEXT across implementer + spec + quality + final subagents. Pattern scales from 4-task to 11-task PRs, AND survives mid-PR architecture pivots (subagent dispatch absorbs the pivot work cleanly). Invest in plan detail when work is mechanically uniform (CF mods, validators, helpers, seed scripts, data-layer Vue components). | 4x (PR #51 → #53 → #55 → #58) |

## Observed Patterns

| Pattern | Why | First Seen | Count |
|---------|-----|------------|-------|
| Pre-flight verification grep before any deletion or shape change — audit consumers, anchor refs, build config, store/plugin requirements before code | PR #44: ran 5 pre-flight greps from §8 of the plan (landing-page.css consumers, #testimonials inbound links, firebase.json rewrites, Pinia in package.json, RossOnboardingHello import path) before touching any code. **Zero surprises mid-implementation** — first PR of the sprint where the verification step caught nothing, validating that the plan was correct. Cheap insurance even when it returns clean. Sister to PR #42's "audit shared utils for CDN deps" — both fire from the same discipline | 2026-05-05 | 2 (PR #42 + #44) |
| Grep for hardcoded prototype strings (proper nouns especially) on any v2 surface that was originally a Hi-Fi scripted prototype, before claiming the surface is "in good shape" | PR #48: footer Profile block shipped `Maya Alvarez / MA / Group Ops` to preview. My plan said "footer already serves the role" — structurally true, but data was scripted placeholder. Hi-Fi prototypes are saturated with named placeholder data (Maya Alvarez / Tannie's Kitchen / Cape Town stats). Heuristic: at read-first pass on any pre-existing v2 file, `rg '[A-Z][a-z]+ [A-Z][a-z]+'` to surface proper-noun strings that may be prototype debris. Sister to the 2026-05-01 "verify field names against actual server seed" pattern but applied client-side. | 2026-05-11 | 1 |
| ~~Fold-in scope rule: same module / same concern / small → FOLD; different module → BACKLOG~~ | _Promoted to Validated_ | 2026-05-11 | 3 |
| Worktree Edit-tracker: always Read files from the worktree path before editing, never the master worktree path — Edit silently rejects edits to files-not-Read-in-this-path | PR #48 implementation: first Edit attempts in `.worktrees/ross-sidebar-cleanup/...` errored silently because I'd Read the master copies. `npm run build` "succeeded" on unchanged files. Caught by noticing the Edit error block + asset hash didn't change. Heuristic: when working in a worktree, prefix every file path with the worktree root explicitly, and treat asset-hash delta as the post-build "did my edits land" check — not just build-green. | 2026-05-11 | 1 |
| Fold an adjacent backlog PR into the current one when a review-time fix already implements its intent — never ship a knowingly-broken intermediate state | PR #46: review surfaced fresh-signup → wizard routing bug; fix required rewriting `resolvePostLoginDestination` semantics, which IS the PR 6 work. Folded PR 6 into PR 4 (instead of shipping the bug + opening a follow-up PR), updated the PR title/body to reflect expanded scope before merge. Heuristic: when an in-scope fix coincidentally implements an adjacent sequenced item, fold rather than ship-then-follow-up — but always refresh the PR description so reviewers see the real surface area. Sister to PR #39's "expand scope by one line to fix data-loss in same PR" | 2026-05-11 | 2 (PR #46 + boundary-set PR #48) |
| When a SELF_OPT pattern reaches Validated (3x), promote its substance into CLAUDE.md's literal protocol text in the same reflect cycle — patterns in working memory don't survive conversation boundaries on their own | This session: validated 3x pattern "read all four feedback files at session start" never made it into CLAUDE.md's Session Opening Protocol step 3 (which still mandated only BACKLOG + SELF_OPT). On a fresh conversation the literal protocol fired correctly but the validated pattern didn't, so I started reactive without LESSONS or SCORECARD loaded. Operator caught it. Fix landed in this PR. Heuristic: every time a pattern crosses the validated threshold, ask "is this in CLAUDE.md or anywhere agent-loadable?" If not, promote it now, not next sprint. Sister to the 2026-04-30 "agent feedback loop must be updated per-PR not at session-end" pattern — both about closing the gap between insight and durability | 2026-05-11 | 1 |
| Refresh the PR title + body when scope expands during review or fix cycles — stale PR descriptions mislead reviewers and blur the post-merge backlog accounting | PR #46: original PR body listed only the 5-site rewire; mid-review fold-in of PR 6 changed the surface significantly but the body wasn't updated until operator caught it on re-check. PR #48 had the same gap quieter: the footer-fix commit didn't trigger a `gh pr edit` either, though operator's "merged. looks good" arrived before it mattered. Heuristic: on every commit that adds files outside the original PR description's scope, immediately also `gh pr edit --title --body`. Costs ~30s; saves a "what did this PR actually do?" review pass. PR #51: added a PR comment for the fix scope rather than `gh pr edit` — comment captures it but reviewers landing on the PR see stale body. Sub-pattern: when fix commits land mid-review, prefer `gh pr edit --body` over a long PR comment for durable scope visibility. | 2026-05-11 | 2 (PR #46 + PR #48) |
| ~~`gh pr edit --body` on scope expansion (not a comment)~~ | _Promoted to Validated_ | 2026-05-12 | 3 (PR #51 + PR #53 + PR #55) |
| Construct spec-reviewer prompts by copy-pasting canonical strings from source artifacts (seed file, server enum, KB doc), NOT from working-memory shorthand or abbreviations | PR #53 Task 3 review: my spec-reviewer prompt abbreviated template names ("CoA, Liquor Licence, Weekly Social Media...") for brevity. Reviewer flagged 7 of 8 All-in entries as non-compliant because the commit used the full canonical names (`Certificate of Acceptability`, etc.) from the seed. I had to override the false positive. Cost of shorthand: a wasted review iteration + the cognitive load of distinguishing "real spec drift" from "my prompt was sloppy". Heuristic: when the spec is a list of identifiers, grep them from source and paste verbatim into the reviewer prompt — same discipline as the 2026-05-01 field-verify pattern applied to reviewer prompts, not just code. | 2026-05-12 | 1 |
| When relocating a doc, grep for self-references inside the doc body — the file knows its own old path | PR #53: spec doc was `git mv`'d from `docs/superpowers/specs/` to `docs/plans/` but the "Files touched" section inside still listed the old path as "this file". Caught by automated review, not by me. Pattern: after `git mv X Y`, run `grep -n "X" Y` (or the old basename) to catch internal self-references. Sister to "grep for hardcoded prototype strings" pattern, but applied to docs. | 2026-05-12 | 1 |
| When a reviewer flags "X mismatch vs sibling Y" as blocking, verify which is canonical against the project source-of-truth before changing your code — the reviewer may be flagging the sibling as the source of truth when it isn't | PR #53 Task 2 quality review: reviewer flagged `databaseURL` mismatch between new script and sibling `ross-templates-backfill-tier.js` as IMPORTANT/blocking. Default behavior would be "match the sibling" — but a quick grep across `firebase-config.js`, `.firebaserc`, and two other seed scripts showed the SIBLING was the outlier (using a legacy short URL); our new script was correct. Resolved by folding a 1-line fix on the sibling, not by changing ours. Heuristic: before resolving a "matches/doesn't match sibling X" finding, confirm sibling X is canonical (grep the project-level config, not just the two files compared). | 2026-05-12 | 1 |
| Verify `--hf-*` design tokens against `public/css/hifi-tokens.css` BEFORE authoring scoped styles — never from memory | PR #55: I used `--hf-fg`, `--hf-surface`, `--hf-font-sans`, `--hf-radius-3`, `--hf-border`, `--hf-fg-muted`, `--hf-surface-muted` etc. across `UpgradeApp.vue` + `RossPlaybook.vue` lock-badge. NONE of those names exist. Actual tokens: `--hf-ink`, `--hf-paper`, `--hf-font-body/display`, `--hf-radius-lg/sm`, `--hf-line`, `--hf-muted`, `--hf-bg2`. Pages still RENDER (browser falls through to defaults) so the bug doesn't show in `npm run build` or unit tests — only on visual preview. Operator caught it. Heuristic: same field-verify discipline as the 2026-05-01 server-seed pattern, applied to CSS tokens — `grep -oE '\-\-hf-[a-z0-9-]+' public/css/hifi-tokens.css` before any new scoped-style block. Sister to the 2026-05-01 field-verify pattern. | 2026-05-12 | 1 |
| Preview-channel testing only validates client/hosting — for PRs that change a Cloud Function protocol, deploy the CF to prod from the worktree BEFORE merging (or before asking the operator to validate on preview) | PR #55: I shipped client code that sends `includeLocked: true` + server code that handles it, but only the client made it to the preview channel (hosting deploy). The CF was still the old version — operator validated on preview, saw the OLD behavior (4 free templates with my data-drift bug compounding the confusion), reported "not working". Required mid-PR `firebase deploy --only functions:rossGetTemplates` to unblock validation. Heuristic: any PR that changes both client and CF must deploy the CF before claiming "ready for preview validation". Already a LESSON for the merge sequencing direction (2026-05-01); this extends to PREVIEW validation, not just merge. | 2026-05-12 | 1 |
| Operator preview testing catches issues automated review can't — budget for at least one preview-driven round-trip on any UI-touching PR | PR #55: subagent-driven dev + 11 spec/quality reviews caught zero of the three real defects operator found on preview (token drift → broken visual styling; data drift on prod RTDB; CF not deployed). PR #58: subagent reviews caught zero of the "CF never runs in prod" defect — only preview e2e revealed the inline seed never fired. Pattern: shipped-to-preview is the highest-fidelity verification step we have. Plan PRs to expect at least one fix cycle after operator preview, rather than hoping for clean-preview-first-try. Sister to the 2026-05-11 "structural-vs-content" pattern — automated reviews check structure, operator preview checks rendered reality. | 2026-05-12 | 2 (PR #55 + PR #58) |
| Verify the CF you're hooking into ACTUALLY EXECUTES in production — read recent `firebase functions:log --only <fn>` output for a successful call, OR check the client caller's error-handling shape — before designing logic that depends on the CF running | PR #58: I designed the day-zero seed as an inline addition inside `registerUser` callable. The CF has been throwing `unauthenticated` on every call (pre-existing v1-vs-v2 callable signature bug from the firebase-functions v7 upgrade); `signup-service.js:109-111` silently swallows the error and falls back to direct RTDB writes. Static code review confirms "the seed code is correct" but cannot reveal "the CF never reaches the seed code". I code-reviewed it 4×, deployed to prod, and only discovered via the operator's preview e2e test that nothing happened. Heuristic: before designing a hook inside an existing CF, run `firebase functions:log --only <fn> -n 50` to confirm recent invocations succeed — OR read every client call site and check whether the failure path is silently swallowed. Sister to the 2026-05-01 field-verify pattern, applied to runtime execution rather than data shape. | 2026-05-12 | 1 |
| Client-side `try/catch` around a server call with `console.warn`-only fallback can hide a long-running prod bug for months | `signup-service.js:109-111` swallows `registerUserFunction(...)` failures into `console.warn` and runs the Path B fallback. The fallback works end-to-end, so signup completes. The callable failure goes to `console.warn` ONLY — no telemetry, no error reporter, no user-visible signal. Discovered PR #58. Heuristic: when wrapping a server call in client-side try/catch with a fallback, the catch handler must AT MINIMUM bump an error counter, emit telemetry, or log to a queryable surface — NOT only `console.warn` which dies with the user's browser session. Add this to the security/observability checklist. | 2026-05-12 | 1 |
| Mid-PR architecture pivot when post-deploy operator test reveals a spec defect — pivot in-PR rather than ship-known-broken-then-follow-up | PR #58: deployed to prod, operator e2e revealed inline seed in `registerUser` never fires (CF bug, see preceding pattern). Three options: (a) ship the merge-ready PR with a known dead code path, (b) revert + redesign in a follow-up PR, (c) pivot architecture in-PR. Picked (c). Hoisted seed to new dedicated CF `rossSeedFirstWorkflow` called from `signup-service.js`. Pivot was clean: ~30min implementation + re-deploy; existing tests stayed valid (Task 1's `buildLocationsFromTemplate` + `buildWorkflowRecord` helpers didn't change); operator re-tested green. Heuristic: a mid-PR architecture pivot is the right move when (1) the existing in-branch tests + helpers stay valid, (2) the hook-point change is mechanically contained (single CF + single client call site), and (3) operator e2e re-test is fast (<10min). Sister to PR #46's "fold in adjacent backlog item rather than ship broken intermediate state". | 2026-05-12 | 1 |
| Cap unbounded client-side `await` on a background-style server call with `Promise.race(work, timeout(N))` — preserves UX (workflow visible on first render) while protecting the worst-case path from cold-start latency | PR #58 final review flagged the `await seedFirstWorkflow(freshUser)` as unbounded — a cold-start CF doing 4 RTDB reads + 1 write could add 3-8s to post-signup redirect. Folded in 1500ms cap via `Promise.race`. If the seed completes inside the cap, workflow is visible on first ROSS render; if not, signup proceeds and the seed lands in the background (workflow appears on next ROSS read). Heuristic: any `await` on a server call whose result is nice-to-have-but-not-blocking should be bounded by `Promise.race` with a timeout matching the UX patience budget (typically 1-2s for post-action redirects). Sister to the "non-blocking by design" failure-mode contract for best-effort side effects. | 2026-05-12 | 1 |
| `functions/node_modules` is NOT inherited by worktrees — run `cd functions && npm install` (~50s) before `firebase deploy --only functions:X` from any new worktree | PR #55: first deploy attempt failed with `Cannot find module '@sendgrid/client'` because the worktree's `functions/` had no `node_modules`. Master's `functions/node_modules` doesn't share into worktrees. Fix is one command but 53s of install. Pattern: if a future PR's flow includes a CF deploy from a worktree, the deploy task in the plan must include the npm-install step. | 2026-05-12 | 1 |
| ~~Subagent-driven-development + detailed plan eliminates ambiguity to near-zero~~ | _Promoted to Validated_ | 2026-05-12 | 3 (PR #51 + PR #53 + PR #55) |
| Adapt the migration step when the standard tool isn't available — `firebase database:update` with a manually-built patch JSON substitutes for a service-account-credentialed Node script | PR #51 Task 12 backfill: the shipped backfill script needs `GOOGLE_APPLICATION_CREDENTIALS` (service account); no service account was available in the worktree. Substituted `firebase database:get` → build patch JSON in Node → `firebase database:update --force`. Same result, uses existing firebase-CLI auth. Pattern: the shipped artifact (backfill script) is correct for someone with credentials; the in-session execution path (CLI patch) is correct for the developer running the deploy without them. | 2026-05-12 | 1 |
| Verify `.validate` rule reachability before adding — under `.write:false` the rule is unreachable; Admin SDK writes always bypass `.validate` | PR #51 review caught both dead rules added in Task 8. Pattern for future RTDB rule design: ask "who can write here?" — if `.write:false` and only Admin SDK writes, `.validate` is decorative at best, misleading at worst. The actual schema enforcement lives in the Cloud Function (`validateTier`). Don't add rules for documentation; add them where they fire. | 2026-05-12 | 1 |
| Auto-mode classifier may not see AskUserQuestion-derived authorization — when blocked, surface to user not as "I tried and was blocked" but as "X needs approval to proceed; tell me to retry" | PR #51 Task 12: user picked "I drive deploys now" from an AskUserQuestion, then I tried `firebase database:get` and was blocked by an earlier "pause for me before that" instruction the classifier still saw as active. Resolution: short status message explaining the action + what was authorized, asking the user to approve on retry. User typed "allow" and the next call went through. Pattern: when the classifier blocks, don't keep retrying — surface to user concisely. | 2026-05-12 | 1 |
| Component dual-mode reuse via optional props with defaults that preserve current behavior — beats fork-and-modify | PR #44: `RossOnboardingHello.vue` refactored with three optional props (`findings`, `continueHref`, `tourHref`) so the marketing page mounts it with synthetic data + signup CTAs while the post-signup mount keeps every behavior byte-for-byte. Q4 spec lock ("same component, two data feeds") delivered without a wrapper component, no test mocks needed, no API surface drift. Heuristic for Hi-Fi component reuse: when a second consumer needs slightly different behavior, prefer optional props with safe defaults over wrapper or copy. Sister to PR #39's "deferred-side-effect dependency injection" pattern | 2026-05-05 | 1 |
| Typographic / structural mock beats imagery for editorial product surfaces — the design system *is* the moodboard | PR #44: hero stock photo (`ob-bg.jpg`) replaced with a paper-card "Daily Opening Checklist" mid-execution panel. Concrete-not-abstract: visitor sees what a workflow run actually looks like before they scroll. Aria-hidden because the hero copy carries the narrative for SR users; the panel is decorative. Aged-screenshot risk gone, on-brand, no asset to maintain. Heuristic for editorial Hi-Fi surfaces: when reaching for a stock photo, first try a typographic representation of the product itself | 2026-05-05 | 1 |
| When porting a v1 surface to a v2 mount shell, audit every imported util for transitive CDN deps that won't show up in any `.js` grep | PR #42: `showToast()` silently no-op'd on the entire signup v2 surface because the SweetAlert2 CDN isn't loaded by the Hi-Fi mount shell. The dependency was invisible from the import graph — only the runtime console showed it. Heuristic: for each `import` from `/js/utils/*`, also grep the v1 host page for any `<script src="https://cdn">` that the util might depend on. Apply before deleting CDN tags from the new shell | 2026-05-05 | 2 (PR #42 + #44) |
| For form controls where the design system must govern the *opened* state (select, datepicker, autocomplete, color picker), build a custom widget from day one — never wrap a native control | PR #42: HfSelect v1 used native `<select>` styled with `appearance:none`. Closed state looked editorial; opened panel showed Windows/macOS chrome that ignores `--hf-*` tokens entirely. There is no CSS hook for the option panel — fix is a true combobox with WAI-ARIA + keyboard. Audit heuristic for the kit going forward: any control with a popout/panel needs a custom impl from the start, not a "v1 + token polish" stopgap | 2026-05-05 | 1 |
| When a server-side validation pattern already exists somewhere in the codebase (e.g. `createUserAccount` line 858 validates `tier` against `subscriptionTiers/{tierId}`), grep for and apply it to *every* sibling write path before opening the PR | PR #42 review caught 2 Major findings (no tier validation in `registerUser`, no `.validate` rule on `subscriptions/$uid`) that `createUserAccount` already had the canonical pattern for. Pre-flight `rg "subscriptionTiers/"` would have found the precedent in seconds. Sister to the 2026-05-01 "verify field names against server" pattern but applies to *validation rules*, not just field names | 2026-05-05 | 1 |
| Operator's spec-collapse instinct often beats agent's literal interpretation of a locked spec — pause to confirm reframes that sound bigger than they are | PR #42: spec said "collapse to two hardcoded tiers"; operator clarified mid-implementation that admin already curates tiers via existing admin-dashboard Tier Management UI, so the right move was "keep dynamic RTDB load, admin curates to whatever they want." Smaller diff, reuses existing infra, doesn't ossify product decisions in code constants. The literal-spec read would have written `constants/tiers.js` and then needed deletion in Phase 6. Heuristic: when a spec mandates a code-level invariant that an admin-facing UI could enforce instead, pause and ask | 2026-05-05 | 1 |
| ~~Parallelize independent Firebase reads with `Promise.all()`~~ | _Promoted to Validated_ | 2026-04-08 | 3 |
| Use `limitToLast(N)` query instead of fetch-all-then-slice for RTDB | Avoids downloading entire node when only N records needed | 2026-04-08 | 1 |
| Verify HTML container IDs exist before writing mount/init code | Stale container refs cause silent failures and error flashes | 2026-04-08 | 1 |
| Remove stale init paths when a module has been refactored to a new entry point | Dual init causes race conditions — the old path will always fail | 2026-04-08 | 1 |
| Spawn BACK/FRONT/QA agents in parallel for independent workstreams | 3x faster than sequential — each agent works in isolation without blocking others | 2026-04-08 | 1 |
| Audit test directories for temp files during QA cleanup tasks | Temp files accumulate silently — thousands of lines of dead code | 2026-04-08 | 1 |
| Use KB routing table to scope agent prompts with specific doc paths | Agents load only relevant context — saves tokens and improves focus | 2026-04-08 | 1 |
| After fixing a pattern, grep the entire module for the same anti-pattern | Same bug often exists in multiple files — LocationService had the same sequential fetch we'd already fixed in database-operations.js | 2026-04-13 | 1 |
| Prefer a pure `withX(item)` helper alongside any in-place `attachX(items[])` so both immutable consumers (UI rows mid-edit) and pipeline consumers (post-save mutation) can share the same key derivation | The pipeline path mutates for memory efficiency; UI consumers must not. Co-locating the pure variant in the same `services/*-identity.js` file preserves DRY while honoring the project's immutability rule | 2026-04-30 | 1 |
| Read `KNOWLEDGE BASE/<feature>.md` *before* the first edit, not as background reading after the fix is in flight | The KB encodes the intended invariants (e.g. flag pipeline only runs post-save; `itemKey` shape is `code:` or `hash:`). Reading after the fact tends to validate whatever was already coded rather than challenge it | 2026-04-30 | 1 |
| Always bump cache-busting version strings after code changes, grep for old version to find all occurrences | Without version bump, browsers serve stale cached scripts even after Firebase deploy | 2026-04-13 | 1 |
| Guard Vue watchers with `_initializing` flag when batch-setting values | Without guard, each setter triggers its own watcher → cascading calls | 2026-04-13 | 1 |
| Verify legacy fallback reads actually return data before keeping them | Dead legacy paths (Permission denied, no data) add seconds of latency for no benefit | 2026-04-13 | 1 |
| Always double-check investigation findings against actual console logs | Initial analysis may blame the wrong code path — logs reveal the real bottleneck | 2026-04-13 | 1 |
| Launch 2 parallel Explore agents (structure + git history) before planning any CLAUDE.md or protocol change | Gives full picture without gaps — structure agent finds KB/hooks/agents, git agent reveals commit ratio and existing planning docs | 2026-04-30 | 1 |
| Surgical CLAUDE.md edits (4 targeted inserts, nothing removed) beat rewrites | Rewrites risk breaking hook references, agent role descriptions, and KB paths that agents depend on | 2026-04-30 | 1 |
| Spec / gap-analysis PR before any phased build-out — read existing v2 module, v1 CFs, and handoff design side-by-side and produce a single mapping doc | Phase 1 of ROSS v2 sprint revealed home feed was already wired behind a flag; original "Phase 2 wire home feed" was a no-op. Saved a full implementation session | 2026-04-30 | 1 |
| Pause to confirm product purpose with user ("do you understand the purpose of X?") before building a redesign that touches existing IA | The mental-model reframe ("v1 admin = AI agent's playbook, not staff CRUD") collapsed 6 sibling tabs to 3 and reshaped Phase 4 entirely. Building the original 6-tab port would have been wasted | 2026-04-30 | 1 |
| `?tab=<name>` URL-param routing inside a single SPA, with `popstate` listener, beats a router framework for 3-4 tabs | No bundle bloat, browser back/forward works, deep links work, "coming next" stubs let unimplemented tabs ship cleanly | 2026-04-30 | 1 |
| Parallel agent on disjoint scope (Phase 5 audit) while parent thread continues serial work (Phase 4 tabs) | When CLAUDE.md "disjoint file scopes" rule blocks N-way parallelism, find the one slice that's actually disjoint and run that in isolation. Phase 5 touched only onboarding files, parent worked on RossHome.vue + new tab files | 2026-04-30 | 1 |
| Update PROJECT_BACKLOG.md + project-status.json + SELF_OPT + LESSONS + SCORECARD inside the same PR that ships the work, not after merge | User explicitly asked "have you updated…" mid-sprint after 9 merges — the protocol is "after every PR merge or significant change", which means before-the-next-PR-not-after | 2026-04-30 | 1 |
| When user feedback rejects a v1 convention (e.g. SweetAlert2) on a v2 surface, confirm the convention is v1-only and pick a Hi-Fi-native pattern (inline confirms, inline error banners) instead of theming the v1 library | v1 conventions in CLAUDE.md may not survive the v2 redesign. Don't assume — check what fits the current visual language | 2026-04-30 | 1 |
| ~~Verify field names against the actual server seed / CF write path~~ | _Promoted to Validated_ | 2026-05-01 | 3 |
| ~~Inline-confirm pattern variants by destructive scope~~ | _Promoted to Validated_ | 2026-05-01 | 3 |
| Show a read-only preview of the entity being committed inside the editor for any "pick X then act" flow (activate template, attach guest, etc.) | User-asked for it on PR #28; reduces surprise-result bugs (empty templates, wrong selection) and gives the user a chance to back out before the round trip | 2026-05-01 | 1 |
| ~~Read all four feedback files at session start~~ | _Promoted to Validated_ | 2026-05-01 | 3 |
| Double-check the planner's plan output before approving — re-audit each open question + look for enum-enforcement holes the plan implicitly leaves open | PR #35: planner proposed `inputType` enum validation only on `rossCreateWorkflow`. Re-audit caught that `rossCreateTemplate` and `rossUpdateTemplate` had no enum check either — meaning a hand-crafted client could write `inputType: 'frobnicate'` to a template, and `rossActivateWorkflow` would defensively fall back to `'checkbox'`, silently corrupting policy. Adding the validator at all 4 write paths was 6 extra lines. The pattern: planners focus on the obvious paths; user/operator review covers the symmetric paths the plan doesn't visit | 2026-05-01 | 1 |
| Single shared helper between sibling CFs that build the same shape — define once, call N times — to prevent drift over future edits | PR #35: `rossCreateWorkflow` and `rossActivateWorkflow` both build per-location task objects from incoming subtasks. Inlined task construction in two places means a future change to one easily skips the other. Centralised as `buildTaskFromSubtask(subtask, nextDueDate)`; post-edit grep verifies 1 def + 2 call sites. Same applies to validators (`validateSubtasksInputTypes` reused at 3 write paths) | 2026-05-01 | 1 |
| Deploy CFs **before** merging client when a PR changes the protocol between them — avoids a window where new client sends new fields but old CFs strip them | PR #35: deployed `rossCreateWorkflow` + `rossActivateWorkflow` + `rossCreateTemplate` + `rossUpdateTemplate` to prod from the worktree, soaked, then merged the PR. Reverse order would have left users with broken create/activate flows for ~30s of CI deploy time, and the failure mode is invisible (subtasks silently lose their inputType) — exactly the gap 4e.2 was fixing | 2026-05-01 | 1 |
| Pre-flight defensive-read audit before extending a server contract that has many client read sites — `rg -n "task\.inputType"` confirms all reads use `\|\| 'checkbox'` fallback | PR #35: ran the audit before writing any code. Found ~15 read sites across v1 admin (`public/js/modules/ross/index.js`) and v2 (the editor itself) — all defensive. Took ~30 seconds and confirmed no migration / backfill needed. Saves catching a regression class in PR review when the audit could have been done up-front | 2026-05-01 | 1 |
| For role-gated UI on a v2 surface, read the role marker once per session for UI gating only — server CFs always remain authoritative | PR #30 superAdmin gate: `loadSuperAdminStatus()` reads `admins/{uid}.superAdmin` and caches in store. Server's `verifySuperAdmin` runs on every CF regardless. Pattern keeps the client gate cheap (one RTDB read) and honest (no role logic anywhere except the server) | 2026-05-01 | 1 |
| Hand off prod auth-user creation to the user; agent only writes the gate record | When testing a role-gated UI you need a non-elevated test account. Agent creating an auth user means: prod write, credentials in transcript, agent needing admin SDK creds. User creates the user themselves (Console / signup flow) and hands over the UID; agent writes only `admins/{uid} = { superAdmin: false }` (or equivalent gate). Reversible, no creds in chat, smaller blast radius | 2026-05-01 | 1 |
| Address minor review findings on the same branch before merge when small/in-scope; defer pre-existing or out-of-scope ones to backlog with explicit cross-reference | PR #30: 4 findings → 2 fixed in branch, 1 backlogged, 1 verified. PR #32: 5 findings → 4 fixed in branch (alert icon, dead code, finally symmetry, raw-input through validator), 1 noted as 4e.2-era. One commit on the branch is cheaper than a follow-up PR for demonstrably small items; backlog items keep pre-existing drift visible without scope creep | 2026-05-01 | 2 (PR #30, PR #32) |
| Pause and surface scope-drift mid-implementation rather than silently expanding the PR | PR #32: discovered the v2 surface assumed flattened `(workflowId, locationId)` rows but the service didn't flatten — `w.locationId` was `undefined` and rendering "by accident". Stopped, surfaced the finding to the user with two paths (in-scope fix vs scope-pure workaround), got explicit approval before continuing. Cheaper than implementing then arguing in PR review | 2026-05-01 | 1 |
| Always invoke matching skills explicitly even when the work feels redundant — "create a plan" is a 100% match for /plan, no leading slash needed | PR #32 session: I drafted a plan inline thinking it was good enough, user called out "why didn't you use the plan skill?". Re-running through `/plan` + `planner` agent produced a tighter spec with a server-contract table + 6 explicit open questions I had to answer before code. The discipline isn't redundant — it forces structure I'd skip when "rolling my own". Matching the user's intent (literal plan request → plan skill) avoids the rationalization "this doesn't need the formal version" | 2026-05-01 | 1 |
| Add to the design system kit (HfIcon, HfButton variants) only when the new element has no semantic stand-in already in the kit; prefer reusing existing tokens | PR #32 mid-run warning banner used `name="x"` (close/error icon) for an informational warning. The kit had no `alert` icon; adding a triangle+bang to `HfIcon.vue` was a 1-line change to the canonical paths map. Don't reach for `bell` or `bolt` as approximations — the icon ledger is small enough that a missing semantic warrants an entry, not a workaround | 2026-05-01 | 1 |
| Standalone docs-only planning PR when a feature's scope is being meaningfully reframed from the backlog row that prompted it | PR #37: original Phase 5 backlog row was "wire onboarding"; the actual work after operator dialogue was "make ROSS the central post-login surface + Hi-Fi-rewrite homepage/signup/wizard + clean up sidebar". Five-PR sequence with locked Q1–Q5 product decisions warranted a planning PR ahead of any code. Precedent: PR #18 (Phase 1 = gap analysis & spec). The rule isn't "every feature gets a planning PR" — small in-scope features still ride inline; the signal is the scope reframe | 2026-05-02 | 1 |
| Step 0 of Standard Task Workflow (read the matching `public/kb/features/<X>.md` doc) fires for **design artifacts**, not just code edits — and fires **before** drafting, not after | PR #37 spec drafting: read all four feedback files at session start (validated 3x pattern fired) but skipped the ROSS feature doc. Cost: 2 rounds of vocabulary pushback before I retroactively read the KB and discovered the canonical IA table + vocabulary lock + already-shipped Runs server surface that should have grounded the design from the start. Upgrade to existing 2026-04-30 lesson: KB read also covers design specs, not only code | 2026-05-02 | 1 |
| Persist `/plan` output to `docs/plans/YYYY-MM-DD-<slug>.md` BEFORE execution begins — never inline-only | PR #39: planner returned ~600-line plan, I went straight to code. Operator caught it: "make a note to add to lessons - plans should always be saved to ... nowhere else". Closed the loop by retrofitting the plan in the same PR. Spec→plan→implementation is a three-doc chain; all three deserve filesystem-permanence. Naming: `YYYY-MM-DD-pr<N>-<slug>.md` so plans are sortable + cross-referenceable to PRs. **Path corrected 2026-05-05: canonical location is `docs/plans/` (19+ historical plans), NOT `docs/superpowers/plans/`.** | 2026-05-02 (corrected 2026-05-05) | 1 |
| Deferred-side-effect dependency injection — any function whose only side effect is a single global-state mutation should accept that effect as a callback param with sensible default | PR #39 router: `routePostLogin(user, navigate?)` defaulted to `(url) => { window.location.href = url }`. One DI parameter served two purposes — testability (planned, vi.fn() mocks) AND timing-correctness fix (unplanned: caller defers the navigate to avoid Promise.all timing bug). Heuristic: when designing an API for a single-side-effect function, make the effect injectable from day one. It costs ~5 chars at the call site for a default-using caller, makes tests trivial, and provides an escape hatch for any consumer that needs to defer or intercept | 2026-05-02 | 1 |
| Audit ALL writers (not just readers) before adding a field to a shared RTDB node — `set()` overwrites silently while `update()` merges | PR #39 pre-flight: greps confirmed no CF reads of `onboarding-progress`, but `onboarding-wizard.js:381` wrote via `set()` which would have wiped the new `helloSeen` field after every wizard completion. Caught by reading EACH consumer's specific operation, not just confirming "no consumer drift". The audit is `rg -n "<node-path>"` THEN `Read` each match's actual `set()` vs `update()` semantic. Without this, the matrix would mis-route every post-wizard user (helloSeen missing → backwards-compat treats as `true` → router skips hello → "the new flow doesn't fire" mystery) | 2026-05-02 | 1 |
| Pair operator UX feedback with automated review findings — when both fire on the same PR, the overlap is almost always the actual bug | PR #39: operator said "was a bit confusing to test"; automated reviewer flagged Promise.all timing bug. Both pointed to the same root cause (toast disappeared in <200ms). Vague UX feedback is usually a downstream symptom of a mechanical bug, not subjective preference — when correlated with a mechanical signal, the correlation localises the root cause better than either signal alone | 2026-05-02 | 1 |
