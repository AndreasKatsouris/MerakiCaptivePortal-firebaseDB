# Lessons Learned

Rolling log of non-obvious discoveries. **Soft cap ~20 entries — evict by REDUNDANCY, not age.**
Updated by agents at session end when a session hits a gotcha, failed approach, or surprise.

## Eviction policy (when at/over the cap)

Pure FIFO-by-date is wrong: a lesson's age is a poor proxy for whether it's still worth keeping. Per the #143 guard-first flip, once a defect-bearing lesson becomes a mechanical guard (test/lint/CI), the *prose* is redundant — the guard enforces it regardless of who reads this file. So evict in this order:

1. **Already-mechanized lessons first** — if a lesson's defect is now covered by a guard (and/or its class is obsolete), drop the prose; the code + git history are the durable record.
2. **Then obsolete/superseded** entries.
3. **NEVER FIFO out an INHERENTLY-PROCESS lesson** (discovery/workflow discipline that *can't* be mechanized — e.g. session-start freshness, writer-census judgment, "read `toolResults` not the model's prose") to make room for a newer one. Those are the only entries that truly vanish when dropped. Being short on slots is a signal to **mechanize more**, not to evict process wisdom.

## Entry Guidelines

- Only log **non-obvious** lessons — skip things derivable from reading the code
- Include the **module** and **source session** for traceability
- If a lesson leads to a validated optimization pattern, note that in SELF_OPTIMIZATION.md
- Prefer shipping a **guard** (per CLAUDE.md Step 11 guard-first default) and demoting the lesson to the guard's code comment — that's what lets it age out of here safely

## Lessons

| Date | Module | Lesson | Source |
|------|--------|--------|--------|
| 2026-07-21 | rtdb/rules-null-equality | **In RTDB rules `null === null` is TRUE, so a comparison you intend as a DENY silently GRANTS when both sides are absent — and "both absent" is exactly what a CREATE looks like.** PR #176: removing the `!data.exists() \|\|` escape hatch from `rewards/$rewardId .validate` blocked NOTHING — on create `data.child('guestPhone').val()` is `null` AND `auth.token.phone_number` is `null` (this app has NO phone auth: `signInWithPhoneNumber`/`RecaptchaVerifier`/`PhoneAuthProvider` → zero hits; every `setCustomUserClaims` writes only `{admin}` and REPLACES the whole claims object at `functions/index.js:782,987,992,1321`) → the clause granted. **Heuristics: (1)** never rely on a null mismatch to deny — test existence explicitly (`data.exists() && …`), it is one load-bearing term. **(2)** before writing a rule against `auth.token.<claim>`, grep every `setCustomUserClaims` to confirm the claim is ever SET (and note it REPLACES, not merges) — a rule keyed on a claim nobody carries is inverted, not weak. **(3)** CLASS not one bug: `guest-rewards/$phoneNumber` (`database.rules.json:88`) is written the same way. Sister to the 2026-06-02 `.validate`-shape lesson. Guard blocked on the RTDB emulator needing Java → stays prose; the empirical REST probe is the net. | Session: 2026-07-21 rules-batch PR-1b (#176) |
| 2026-07-21 | gcs/public-acl-vs-rules | **A GCS object made public with `file.makePublic()` is served by `storage.googleapis.com` WITHOUT evaluating Firebase Storage rules — so tightening `storage.rules` on that path neither breaks nor fixes anything.** CRIT-09: `receiptProcessor.js:440` calls `makePublic()` (writes an `allUsers:READ` object ACL) on every receipt image and stores an enumerable `…/receipts/{ms-epoch}_receipt.jpg` URL → exposure is unauthenticated to the whole internet, and the audited "lock the rules" fix would have recorded a false closure. **Heuristics: (1)** for any Storage-exposure finding, find how the object is PUBLISHED (`makePublic`, uniform bucket-level access, signed URLs, download tokens) before reasoning about rules — publication decides whether rules are even in the path. **(2)** Firebase download-token URLs are the same trap — bearer capabilities that bypass rules. **(3)** a code fix that stops NEW exposure does not remediate EXISTING objects — public ACLs persist until revoked, so a data-remediation sweep is a first-class deliverable, sequenced FIRST since it is what closes the live hole. Design/discovery discipline → stays prose. | Session: 2026-07-21 PR-2 receipts scoping |
| 2026-07-21 | security/rules-audit-blind-spot | **A `.validate` that is TOO STRICT fails closed — indistinguishable from correct security at audit time — so an over-tight rule scores as a pass and ships invisibly, breaking legit non-admin writers.** `locations .validate` required `ownerId === auth.uid` while the live dashboard wrote only `userId`/`createdBy`; non-admin location creation was broken for ~7 weeks (since PR #100), masked by an admin-bypass clause + a swallowed `console.error`. Heuristics (preserved in the guard's comment): run the writer-census payload→rule (not rule→threat) for EVERY `.validate`, grep that each named field is actually written, and treat an admin-bypass clause as ALSO a test-coverage bypass → smoke-test as a non-admin. **Guard:** `public/js/modules/user-dashboard/tests/location-service.test.js` (payload contract, written RED-first). Orig 2026-07-21. | Session: 2026-07-21 PR-1 re-census (#169/#170) |
| 2026-07-21 | security/cve-closure-verification | **Closing a dependency CVE on "`npm audit` no longer lists it" or "our version looks newer" is not verification — put the advisory's FIXED-IN version and the INSTALLED version side by side, because the vulnerable range's upper bound is exclusive and the last vulnerable patch looks reassuringly high.** HIGH-08 (`@grpc/grpc-js`): a scan read `1.12.6` as "above the fix" and marked it CLOSED, but the range is `>=1.12.0 <1.12.7` — `1.12.6` is the LAST vulnerable release. Worse, a later scan "re-confirmed" the closure without re-deriving it and cited two agreeing scans as strength — two passes of the same unverified inference is one data point in disguise. **Heuristics: (1)** a CVE closure must quote fixed-in vs installed explicitly (`>=1.12.7` vs `1.12.6`), never a bare tool-output absence; **(2)** "re-confirming" means re-deriving from the primary source, else record it as inherited-unverified, never corroboration; **(3)** `fixAvailable`/`effects` in `npm audit --json` show remediation cost up front (`effects:[]` → lockfile-only `npm audit fix`). Mechanizable via `npm audit --audit-level=high` in CI — blocked on backlog 3c (baseline not green) → stays prose. | Session: 2026-07-21 #168 + HIGH-08 retraction |
| 2026-07-05 | process/scheduled-scan-visibility | **A scheduled session that commits findings to a branch but never opens a PR produces work INVISIBLE to the operator — `git log origin/master` is the only ground truth, not "a branch exists."** Three daily OWASP scans committed real Critical/High findings to their own branches and stopped; `master` sat unchanged 19 days. **Heuristic: any session whose job is to REPORT findings (audit/scan/monitor) is not done at "commit to a branch" — it is done when the operator can see it without spelunking branch history, i.e. a PR is open.** Inherently-process → stays prose. | Session: consolidated 2026-07-05 OWASP re-scan |
| 2026-06-22 | functions/esm-in-cjs | **An ESM `export`/`import` in a CommonJS `functions/` module PASSES under vitest (its loader transforms modules) but throws `SyntaxError` at DEPLOYED `require()` runtime.** A cheap-model implementer wrote `export { summariseFoodCost }`; all vitest tests + the vitest-rerunning reviewer reported clean, yet the deployed CF would `SyntaxError` on first tool call. **Heuristics: (1)** `functions/` is CommonJS (no `"type":"module"`) — every file uses `require`/`module.exports`; an ESM statement is a runtime landmine vitest cannot see. **(2)** a review whose job is "matches the verbatim plan" must `git show`/diff the ACTUAL file bytes — re-running vitest transforms away the exact defect that breaks prod. Sister to the #140 tests-green-prod-400 dialect trap. Guard candidate (not shipped): eslint ban `import`/`export` under `functions/**`, or a plain `node -e "require('./mod')"` smoke (vitest structurally can't guard this). | Session: food-cost reader D1 (#167) |
| 2026-06-16 | security/webhook-signature-rollout | **Adding signature verification to a LIVE third-party webhook is a two-phase (`monitor`→`enforce`) rollout, because the check can REJECT legitimate traffic, not just forged traffic — the failure mode is a silent outage of a real revenue/guest channel.** CRIT-07 (Twilio): the HMAC is over the EXACT URL the provider called, so any reconstruction mismatch (rewrite domain, trailing slash, query param) 403s every real message. `monitor` validates + logs a PII-free `{valid,hasToken,url}` and still processes; flip to `enforce` only after real traffic logs `valid:true` and the URL byte-matches. Default the env to `monitor` so an unset value can't hard-break the channel. **Corollaries: (a)** trusting `x-forwarded-host` isn't a bypass — forging still needs the secret, so host-spoofing fails closed; **(b)** a missing token under `enforce` rejects ALL — verify `hasToken:true` first; **(c) [gen2, confirmed #164]** you CANNOT reconstruct the URL — Google strips the function-name path so `req.originalUrl` is only `/`; validate against an explicit operator-set `TWILIO_WEBHOOK_URL` (comma-list, valid-if-any), reconstructing only when unset. Use the SDK's `validateRequest`, but generate a REAL HMAC in tests. Mechanical half guarded by 22 real-HMAC tests (incl. a gen2 path-strip regression); the live-rollout discipline stays prose. | Session: CRIT-07 Twilio signature (#162/#164) |
| 2026-06-11 | whatsapp/template-constraints | **WhatsApp business-initiated send mechanics (verified live, W2 #158). Mechanized half — guarded by `functions/agent/__tests__/sweep-formatter.test.js:45-53`: template VARIABLES reject newlines and >4 consecutive spaces, so sanitize every user-supplied string into vars.** Prose that stays (not mechanizable): (2) a template BODY may not END with a variable — park a sign-off after the last `{{n}}`; (3) keep the URL domain STATIC in the body and pass only the query string as the variable (whole-URL vars risk Meta rejection); (4) the freeform fallback is deliverable ONLY inside a 24-hour customer-initiated session — outside it Twilio accepts the API call but Meta never delivers, a SILENT non-delivery that logs as success. **Heuristic: any unattended/business-initiated WhatsApp feature needs an APPROVED content template as its primary path (submit early — external long-pole); the fallback is session-window-only, never production.** | Session: W2 proactive nudge (#158) |
| 2026-06-10 | functions/cf-handler-testing | **Firebase Functions v2 `onRequest` handlers (incl. `defineSecret` consumers) ARE directly unit-testable — set `process.env.<SECRET_NAME>` BEFORE requiring the module, then invoke the export as a plain `(req,res)` function with fakes** (`defineSecret(...).value()` just reads `process.env` at runtime). **Heuristic: every NEW unauthenticated or money-moving CF gets handler-level smoke tests (sig→200+credit, bad sig→401, GET→405, non-success→200+no write) — "shells are deploy-smoke-only" is acceptable only for Bearer-gated CRUD wrappers whose gates are themselves shared, tested code.** Guard: `functions/payments/__tests__/cloud-functions.webhook.test.js`. Orig 2026-06-10. | Session: Payment Rail backend (#156) |
| 2026-06-08 | process/external-service-availability | **Verify a third-party service's REGIONAL availability + capability (currency/settlement/charging) against a LIVE source before baking it into a design — fast-moving fintech outpaces training data, and a wrong assumption cements into both spec AND code.** Both plausible-from-memory picks were wrong, each caught for one web search: Stripe is NOT direct-to-SA (→ Paystack), and Paystack-SA cannot charge USD (→ keep USD as unit of account, charge ZAR per bundle). **Heuristic: every external dependency in a design (payment gateway, region-gated SaaS, SMS/WhatsApp provider, API) gets a live-source availability + capability check for the user's ACTUAL region/currency BEFORE the spec locks — never assert from memory.** Design-time discipline → stays prose. | Session: Payment Rail design (#155) |
| 2026-06-07 | evals/diagnosis | **An eval/agent failure blamed on the MODEL can be a HARNESS bug — diagnose from the tool's ACTUAL output (`transcript.toolResults`), never the model's prose (an agent honestly reporting a tool error reads exactly like confabulation).** #152: Ross DID call `getStaff`/`snoozeCard`; each returned `{error:"Can't determine Firebase Database URL"}` because the driver seamed rossChat/tools/ledger but not `execute.js`. **Heuristics: (1)** read the `tool_result` before blaming the model; **(2)** seam EVERY db consumer in a test driver (rossChat+tools+execute+ledger); **(3)** give a non-deterministic judge the exact LITERAL value it must check (#153 put the literal SA due-date in owner-context to stop a date-derivation flap). Guard: `functions/agent/evals/__tests__/evals-driver.test.js:115` asserts the captured output is not an `{error}` envelope. Orig 2026-06-07. | Session: askRoss eval 21/21 (#152/#153) |
| 2026-06-05 | agent/tenant-isolation | **An LLM agent runs via the Admin SDK, which BYPASSES RTDB rules — so per-adapter access scoping in CODE is the ONLY tenant boundary; any tool resolving an owner/path from a model-supplied arg is a cross-tenant vector unless it re-checks the caller's access.** #144: `getRunHistory` resolved a workflow owner from a GLOBAL index with no caller check → HIGH IDOR. **Heuristic: treat every LLM-supplied tool arg as attacker-controlled; enforce tenancy from the server-derived uid, never inferred from an arg. Capability-absence > prompt-guarding — a jailbroken model can't exfiltrate what no tool returns.** Guard: cross-tenant test in `functions/agent/__tests__/tools.test.js` (attacker→[] / delegated-manager→served). Orig 2026-06-05. | Session: askRoss security audit (#144) |
| 2026-06-05 | evals/loop-signalling | **When a harness RE-DRIVES a production function, what it EMITS vs RETURNS is a real seam — fold both, or silently drop half the outcome.** `runAgentLoop` *emits* `text`/`action` but *returns* `{paused,pendingTool}`/`{error}`; a driver folding only emitted events left `transcript.confirms`/`error` permanently empty. **Heuristics: (1)** before reusing a function in a new harness, read its RETURN statements, not just its `emit`/callbacks — signalling splits across both. **(2)** per-task reviews can't see cross-task seams (a test-helper reset nulling the live runner's client) — that's the controller's/final-review's job. | Session: askRoss slice 6 eval harness (#142) |
| 2026-06-04 | client/sse-transport | **To consume an authenticated SSE Cloud Function from the browser you CANNOT use `EventSource` — it is GET-only and cannot set an `Authorization` header.** Stream via `fetch(url,{method:'POST',headers:{Authorization},body})` → `res.body.getReader()` + `TextDecoder` + a hand-rolled `\n\n` frame parser. Two footguns: (a) `rossChat` reads `req.body` FLAT (`{message,threadId,clientToday}`), NOT the `{data:{…}}` envelope its callable-style siblings use; (b) make the transport NEVER throw — surface every failure (no-auth, network, non-ok, mid-stream abort) as a synthetic `{type:'error'}` event through one path. **Heuristic: authenticated browser SSE = `fetch`+`ReadableStream`, never `EventSource`; always diff the target CF's body-parse shape (`req.body` vs `req.body.data`) against the sibling you copy — they diverge.** | Session: askRoss slice 5 (#139) |
| 2026-06-04 | vue/escaping | **The "escapeHtml the agent/server string" requirement is satisfied STRUCTURALLY in Vue by text interpolation — `{{ }}` auto-escapes; the real rule is "never `v-html` on agent/server content."** **Heuristic: on a Vue surface, translate any "escapeHtml X" requirement into "render X via `{{ }}`, audit for `v-html`" — don't import the v1 innerHTML string helper (it's noise).** | Session: askRoss slice 5 (#139) |
| 2026-06-03 | js/lazy-require-scope | **A `require(...)` placed INSIDE one function is invisible to sibling functions in the same module — a sibling using the same symbol gets a `ReferenceError` at call time, not load time.** askRoss slice 4: `executeTool` was lazy-required inside `runAgentLoop`; when sibling `resumeChatRequest` called it, the ReferenceError was swallowed by the approve path's try/catch into an `is_error` tool_result → the tool silently never executed. **Heuristic: a lazy-`require` is function-scoped — if a second function needs the same dependency, require it there too (or hoist to module top); when you copy a pattern to a new sibling, re-check every symbol is in the new scope. A swallowed error in a catch hides a missing binding.** | Session: askRoss slice 4 (#136) |
| 2026-06-03 | process/session-start | **An inherited worktree can be many commits behind master with a DELETED upstream — read the feedback files there and your whole session-start sprint picture is stale.** A harness worktree was 11 commits behind on a `[gone]` branch; the feedback files described ④a's rule-lock as the NEXT task when #125/#127 had already shipped it. **Heuristic: at session start, BEFORE trusting any in-repo doc (backlog/LESSONS/memory), run `git status -sb` + `git log origin/master --oneline -5`; if behind or upstream `[gone]`, sync to master (or re-read from `origin/master`) FIRST.** | Session: askRoss slice 3 (#134) |
| 2026-06-03 | billing/idempotency | **Swapping an idempotency key from caller-supplied to per-invocation-random (to close an abuse vector) silently removes the dedup benefit AND creates unbounded guard-node growth.** rossChat slice 3: a client-reusable header `requestId` let a client short-circuit the debit (free turns), so it became a server `crypto.randomUUID()` — correct, but now every turn writes a unique `billing/debitGuard/{uid}/{requestId}` that never dedupes and grows ~1/turn forever. **Heuristic: when you make an idempotency key per-invocation-random, note (a) it no longer dedupes — OK only while there's no retry path, and (b) the guard table grows unbounded → pair it with a TTL/retention sweep. A genuine retry/resume needs a SERVER-ISSUED token threaded across requests, not a fresh UUID.** | Session: askRoss slice 3 (#134) |
| 2026-06-03 | billing/sdk-shape | **A field-shape mismatch across two SDK boundaries silently zero-charges** — the Anthropic API returns `usage` snake_case (`input_tokens`), the ledger reads `units` camelCase (`inputTokens`), so the spec shorthand `units: usage` maps every field to `undefined→0` (a 0 charge every turn; the ledger itself is correct — the bug is at the boundary). **Heuristic: whenever you pass one SDK's response object straight into another's input, diff the field names — a silent zero is worse than a crash.** Guard: `toLedgerUnits` tests in `functions/agent/__tests__/llm-client.test.js:23`. Orig 2026-06-03. | Session: askRoss spec revision (#129) |
| 2026-06-03 | functions/import-safety | **To keep a lightweight, unit-testable CORE module that wraps logic living in a HEAVY module, lazy-`require` the heavy module INSIDE the function, not at the top.** A top-level `require('../ross')` would drag ~22 `onRequest` registrations + `defineSecret` into the core (slow imports; can throw under vitest with no admin init). **Heuristic: a core module's top-level requires should be cheap + side-effect-free; defer anything heavy/side-effectful (CF registration, secrets) to call time — behind a test seam if it's also an engine/SDK dependency.** | Session: askRoss agent core (#130) |
| 2026-06-02 | tools/rtdb-write-windows | **To write a single RTDB value from a Windows dev box, a direct REST `PATCH` with a `gcloud auth print-access-token` token is the reliable method — the other paths all fail** (firebase MCP tools error internally; `firebase database:update` rejects stdin on Windows; PowerShell `Out-File -utf8` adds a BOM that breaks JSON parse; git-bash MSYS mangles the `/path`). `curl -X PATCH -d '{"k":v}' "https://<proj>-default-rtdb.firebaseio.com/<path>.json?access_token=$(gcloud auth print-access-token)"` — the OAuth token acts as ADMIN (bypasses rules) and PATCH MERGES so sibling keys are preserved. Read back the same way to verify. | Session: PR5 maxWorkflows deploy (#127) |
| 2026-06-02 | rtdb/rules-validate | **A parent-level `.validate` using `newData.hasChild('X')` to reject field X is BUGGY for multi-path `update()` — at the parent `newData` reflects the POST-MERGE state, so it "sees" X whenever X already exists in the DB, even when the write doesn't touch X** (would false-reject every admin write on an already-materialized record). **Heuristic: to forbid a client writing a specific subtree, use child `"X": { ".validate": false }` on that subtree, NOT a parent `!newData.hasChild()` guard — child `.validate` evaluates only when the write tree actually includes data there, and IS effective (unlike child `.write:false`, the dead-rule trap; `.validate` does not cascade-grant).** | Session: PR4 rule lock (#125) |
| 2026-06-02 | process/writer-census | **A "grep ALL client-SDK writers before a rule lock" census must include inline `<script>` in HTML pages — not just `.js`/`.vue` modules.** PR4 missed a legacy `public/admin_tools/enhanced-user-subscription-manager.html` inline multi-path features/limits write; because RTDB `update()` is atomic, the lock's child `.validate:false` would have rejected the WHOLE tier change → hard-broken that admin workflow. **Heuristic: the grep-all-writers census must use `rg --include=*.js --include=*.html --include=*.vue` — inline HTML scripts are live code, and an atomic multi-path `update()` carrying one now-rejected field fails the entire write.** | Session: PR4 rule lock (#125, security-review catch) |
| 2026-06-01 | deploy/firebase-cli | **`firebase deploy` analyses the WHOLE codebase, so an undeployed `defineSecret` blocks ALL function deploys** (the entire OWASP hardening sat live-undeployed for a day because `MERAKI_SHARED_SECRET`/`INITIAL_ADMIN_SETUP_SECRET` were never provisioned). **Heuristics: (1)** after any change adding `defineSecret`, set it (`firebase functions:secrets:set NAME`) before the next deploy of ANY function; check with `:secrets:get` (404 = missing). **(2)** large deploys hit a 429 mutation-quota → auto-retries, don't panic. **(3)** firebase-tools HANGS after a successful deploy on Windows — verify end state independently (`functions:list` count, live rules via REST) and kill the zombie. **(4)** a function-count gap can be a phantom export — `exports.X` clobbered by a later `module.exports = {}`. CLI RTDB writes: MSYS mangles `/path` (use `MSYS_NO_PATHCONV=1`); PowerShell `Out-File -utf8` adds a BOM (use `[IO.File]::WriteAllText`). | Session: Phase 7 ① ledger deploy (2026-06-01) |
| 2026-05-31 | tools/git-commit | **Backticks (and `$`/parens) inside a double-quoted `git commit -m "..."` are shell command-substitution — they execute, mangling the message** (`` `before` `` ran as a command). **Heuristic: for any commit message containing backticks/`$`/parens, use a single-quoted heredoc — `git commit -m "$(cat <<'EOF' … EOF)"` (the quoted `'EOF'` delimiter disables interpolation).** | Session: Phase 7 ① ledger build (#113) |
| 2026-05-31 | agent/no-fabrication | **An LLM agent with write tools must be forbidden — server-side, not just by prompt — from auto-submitting any value it cannot actually obtain or verify; otherwise it will fabricate records.** `submitResponse` tiered `auto` would let the agent auto-submit a `temperature`/`yes_no` regulatory measurement it has no sensor for → a fabricated food-safety compliance record. **Heuristic: for every agent write-tool ask "can the agent legitimately KNOW this value?" — if it asserts a real-world measurement/verification the agent didn't perform, the tool must refuse or escalate IN CODE (auto only for non-measurement types), enforced in the adapter + as a hard eval assertion, never left to model behaviour.** | Session: askRoss Agent spec (PR #110 review) |
| 2026-05-31 | process/design-audit | **The field-verify / write-path discipline pays off at SPEC-DESIGN time, not just at build time.** Auditing the live subscription module while DESIGNING the entitlements resolver both validated the "resolver becomes sole writer" assumption AND surfaced a client-writable `subscriptions/$uid/features`+`limits` self-grant vuln (`database.rules.json:33`) before any code was written. **Heuristic: when a design assumes "X becomes the sole writer/reader of Y", audit every current writer/reader of Y against the LIVE code BEFORE writing the spec — the audit validates the assumption and routinely surfaces the security/drift the docs hide.** | Session: Phase 7 entitlements design (PR #107) |
| 2026-05-31 | process/pr-amend | **Amending an open PR's doc to address review feedback gets orphaned if the operator merges (squash) while you edit — your follow-up commit lands on the dead branch, not master.** **Heuristic: when addressing review by amending an open PR, either (a) batch all edits and push before the operator reviews, or (b) `gh pr view N --json state` before pushing an amendment; if MERGED, branch off `origin/master` and open a follow-up PR rather than pushing to the dead branch.** | Session: Phase 7 ledger spec (#106 → #108) |
| 2026-05-31 | rtdb/rules-cascade | **RTDB read permission CASCADES from a parent, and a query is authorised at the QUERIED node — rules cannot filter query results.** (1) a root `".read":"auth != null"` makes every per-child rule below it DEAD — tightening means removing/admin-scoping the ROOT; (2) you cannot express "let a user query `guests` where `locationId` is theirs" — the query auths at the `guests` root and rules don't post-filter. **Heuristic before touching any root `.read`: map every consumer and classify — direct keyed-path reads (`users/$uid`, `salesDataIndex/byLocation/$locId`) are lockable NOW with a scoped child rule; root-level location-QUERIES (guests/queue) need the client read path changed first. Use the `userLocations/$uid/$locId` cross-ref for scoping, and INCLUDE the `locations/$locId/ownerId === auth.uid` fallback (users with unpopulated userLocations would otherwise lose their own data — an automated-review must-fix).** | Session: PR #96 (H-2 cross-tenant design + review must-fix) |
