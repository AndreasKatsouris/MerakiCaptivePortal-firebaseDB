# Lessons Learned

Rolling log of non-obvious discoveries. **Soft cap ~20 entries — evict by REDUNDANCY, not age.**
Updated by agents at session end when a session hits a gotcha, failed approach, or surprise.

## Eviction policy (when at/over the cap)

Pure FIFO-by-date is wrong: a lesson's age is a poor proxy for whether it's still worth keeping. Per the #143 guard-first flip, once a defect-bearing lesson becomes a mechanical guard (test/lint/CI), the *prose* is redundant — the guard enforces it regardless of who reads this file. So evict in this order:

1. **Already-mechanized lessons first** — if a lesson's defect is now covered by a guard (and/or its class is obsolete), drop the prose; the code + git history are the durable record.
2. **Then obsolete/superseded** entries.
3. **NEVER FIFO out an INHERENTLY-PROCESS lesson** (discovery/workflow discipline that *can't* be mechanized — e.g. session-start freshness, writer-census judgment, "read `toolResults` not the model's prose") to make room for a newer one. Those are the only entries that truly vanish when dropped. Being short on slots is a signal to **mechanize more**, not to evict process wisdom.

## Entry Guidelines

- Only log **non-obvious** lessons — skip things derivable from reading the code
- Include the **module** and **source session** for traceability
- If a lesson leads to a validated optimization pattern, note that in SELF_OPTIMIZATION.md
- Prefer shipping a **guard** (per CLAUDE.md Step 11 guard-first default) and demoting the lesson to the guard's code comment — that's what lets it age out of here safely

## Lessons

| Date | Module | Lesson | Source |
|------|--------|--------|--------|
| 2026-06-22 | functions/esm-in-cjs | **An ESM `export {…}` (or `import`) in a CommonJS `functions/` module PASSES under vitest but throws `SyntaxError` at DEPLOYED runtime — because vitest transforms modules, so its loader interops ESM/CJS that plain Node `require()` rejects.** Food-cost reader D1: a cheap-model implementer wrote `export { summariseFoodCost }` instead of the plan's verbatim `module.exports = {…}`; all 6 vitest tests passed, and the combined reviewer (re-running vitest) reported "uses module.exports / no ESM" — both masked by vitest's transform. The deployed CF does `require('./food-cost-summary')` at call time → it would have `SyntaxError`'d in prod on the first tool call. Caught only because the NEXT task's implementer hit it and fixed it to CJS. **Two heuristics: (1)** `functions/` is a CommonJS package (`package.json` has no `"type":"module"`) — every file must use `require`/`module.exports`; an ESM statement is a runtime landmine vitest cannot see. **(2)** when a review's job is "does this match the verbatim plan code," the reviewer must `git show`/diff the ACTUAL file bytes against the spec — re-running vitest is NOT sufficient (it transforms away the exact defect class that breaks prod). Sister to the #140 mock-vs-real-API dialect trap (tests-green-prod-400) — same family: a test harness that's more permissive than the production runtime. Guard candidate (cheapest real one): an eslint rule banning `import`/`export` under `functions/**`, OR a plain-`node -e "require('./<mod>')"` smoke (NOT vitest) — vitest structurally cannot guard this. | Session: food-cost reader D1 (PR #167) |
| 2026-06-16 | security/webhook-signature-rollout | **Adding signature verification to a LIVE third-party webhook is a two-phase rollout, because the check can REJECT legitimate traffic, not just forged traffic — the failure mode is a silent outage of a real revenue/guest channel.** CRIT-07 (Twilio WhatsApp): the signature is HMAC'd over the EXACT URL the provider called, so if your reconstructed URL (`https://${x-forwarded-host||host}${originalUrl}`) differs from the URL configured in the provider's console — by a Hosting-rewrite domain, a trailing slash, a query param, or a missing token — `enforce` mode 403s every real message. Mitigation = an env-gated `monitor` → `enforce` staging: `monitor` validates + logs a PII-free `{valid, hasToken, url}` summary but still processes; you confirm in logs that real traffic shows `valid:true` and the reconstructed `url` byte-matches the console before flipping to `enforce`. Default the env to `monitor` so an unset value can never hard-break the channel; log loudly when not enforcing so the gap isn't forgotten. **Three corollaries: (a)** trusting `x-forwarded-host` for reconstruction is NOT a bypass — an attacker still can't produce a valid HMAC without the secret token, so host-spoofing only fails-closed; **(b)** a missing `TWILIO_TOKEN` under `enforce` rejects ALL traffic, so verify token presence (the `hasToken:true` log) before promoting; **(c) [confirmed live #164] on gen2 Cloud Functions you CANNOT reconstruct the URL at all** — Google's routing strips the function-name path before the request reaches the handler, so `req.originalUrl` is only `/` and reconstruction yields `https://host/` while the provider signed `https://host/receiveWhatsAppMessage` → every genuine request is `valid:false`. The fix is to validate against an explicit operator-configured `TWILIO_WEBHOOK_URL` (the exact console URL; accept a comma-list for multiple deployed endpoints, valid-if-any — safe since forging needs the token), falling back to reconstruction only when unset. The monitor soak is what surfaced this (the very first real message logged `valid:false url:'.../'`) — proving the staged rollout's worth: caught a real prod mismatch with **zero downtime**, fixed it, re-soaked `valid:true`, then enforced. Use the provider SDK's own `validateRequest` (Twilio's sorted-param scheme) rather than hand-rolling — but generate a REAL signature with crypto in the test (not a mocked boolean) so you exercise the actual algorithm. Inherently-process (live-rollout discipline) → stays prose; the mechanical part is guarded by the 22 real-HMAC unit tests (incl. a gen2-path-strip regression test). | Session: CRIT-07 Twilio signature (#162 + #164) |
| 2026-06-11 | whatsapp/template-constraints | **WhatsApp content-template mechanics that silently break a business-initiated send — verified against live docs (W2 #158): (1) template VARIABLES reject newlines and >4 consecutive spaces (a user-supplied workflow name with a line break kills the send → sanitize every string into vars); (2) a template BODY may not END with a variable (park a sign-off after the last `{{n}}`); (3) keep the URL domain STATIC in the body and pass only the query string as the variable (whole-URL variables risk Meta rejection); (4) the freeform fallback path (`buildFallbackMessage`) is only deliverable INSIDE a 24-hour customer-initiated session — outside it, Twilio accepts the API call but Meta never delivers: a SILENT non-delivery that looks like success in logs.** Heuristic: any unattended/business-initiated WhatsApp feature needs an APPROVED content template as its primary path (submit early — it's the external long-pole), and the fallback is dev/session-window-only, never the production path. Partially mechanized: the formatter's `sanitizeInline` + its newline/multi-space tests guard (1); (2)–(4) are design/ops discipline → prose. Sister to the 2026-06-08 live-source availability lesson — same verify-against-live-docs discipline, applied to template mechanics. | Session: W2 proactive nudge (#158) |
| 2026-06-10 | functions/cf-handler-testing | **Firebase Functions v2 `onRequest` handlers (incl. `defineSecret` consumers) ARE directly unit-testable — set `process.env.<SECRET_NAME>` BEFORE requiring the module, then invoke the export as a plain `(req, res)` function with hand-built fakes.** Payment Rail: the repo convention ("cores unit-tested, CF shells deploy-smoke-only", inherited from billing) left the **unauthenticated** `paystackWebhook` — the internet-facing surface with the signature gate, method guard, and grant wiring — at 0% coverage, which the code review flagged as the highest-risk untested code in the change. The fix was cheap: `defineSecret(...).value()` just reads `process.env` at runtime, a v2 `onRequest` export is an Express-style callable, and the existing `__setDbForTests` seams (payments + billing ledger pointed at the SAME fake RTDB) made the full grant path assertable (sig→200+balance credited, bad sig→401, GET→405, non-charge.success→200+no write). 4 smoke tests, no emulator. **Heuristic: for any NEW unauthenticated or money-moving CF, the shell gets handler-level smoke tests via env-secret + direct invocation — "shells are deploy-smoke-only" is only acceptable for Bearer-gated CRUD wrappers whose gates are themselves shared, tested code.** (Sister discovery, already mechanized so no prose entry: the billing fake-rtdb `transaction()` lacked RTDB abort semantics — a duplicate webhook would have deleted the claim record and falsely committed; the payments copy fixes it and `fake-rtdb-abort.test.js` pins both branches. See the helper's header comment.) | Session: Payment Rail backend (#156) |
| 2026-06-08 | process/external-service-availability | **Verify a third-party service's REGIONAL availability + capability (currency / settlement / charging) against a LIVE source before baking it into a design — fast-moving fintech/SaaS outpaces training data, and a wrong assumption gets cemented into the spec AND the code.** Payment Rail design: the operator wanted **Stripe** → a web check showed **Stripe is NOT direct-to-SA** (a SA-registered business can't open a standard account or get ZAR payouts — needs a UK/US entity), so we pivoted to **Paystack** (Stripe-owned, SA-native). Then the operator wanted **USD bundles** → another check showed **Paystack-SA cannot charge USD** (ZAR-only base; USD settlement is Nigeria/Kenya-only), so the design keeps USD as the *unit of account* (1:1 ledger grant) but **charges ZAR per-bundle**. BOTH were plausible-from-memory and BOTH wrong; each caught for the price of one web search before the spec locked. **Heuristic: every external dependency in a design (payment gateway, region-gated SaaS, SMS/WhatsApp provider, API) gets a live-source availability + capability check for the user's ACTUAL region/currency BEFORE the spec is written — never assert it from memory.** Inherently-process (design-time discipline) → stays prose. | Session: Payment Rail design (#155) |
| 2026-06-07 | evals/diagnosis | **An eval/agent failure blamed on the MODEL can be a HARNESS bug — diagnose from the tool's ACTUAL output (`transcript.toolResults`), never the model's prose. An agent honestly reporting a tool error reads exactly like confabulation.** #152: the eval-tuning plan called `q-staff`/`q-staff-runs`/`snooze` "Group B: Ross won't call his tools / confabulates 'a technical error'." Reading `toolResults` showed the opposite — Ross DID call `getStaff`/`snoozeCard`; each returned `{error:"Can't determine Firebase Database URL"}`. Cause: the eval driver seamed `rossChat`/`tools`/`ledger` to the fake RTDB but **not `execute.js`** — `executeTool`'s audit-write (+ no-undo capture) use execute.js's OWN `getDb()`, so every tool's `run()` succeeded then threw at the audit step. Ross reported the error faithfully → looked like an excuse. Fix: `execute.__setDbForTests(db)`. **Guard:** the driver test now asserts the captured output is not an `{error}` envelope (it previously only checked the entry *existed*, so an errored result satisfied it). **Heuristics: (1) when an agent "won't use a tool" or "makes excuses," read the `tool_result` before blaming the model — field-verify applied to the harness itself. (2) Seam EVERY db consumer in a test driver (rossChat + tools + execute + ledger), not the obvious ones. (3) Judge non-determinism: `q-overdue`/`q-today` flapped because Ross derived the due date (today − 11d) while the digest only said "11 days overdue" — #153 put the LITERAL date in the owner-context so the judge verifies what it sees. Give the judge the exact value it checks.** | Session: askRoss eval 21/21 (#152 / #153) |
| 2026-06-05 | agent/tenant-isolation | **An LLM agent runs via the Admin SDK, which BYPASSES RTDB rules — so per-adapter access scoping in CODE is the ONLY tenant boundary, and any tool that resolves an owner/path from a model-supplied argument is a cross-tenant vector unless it re-checks the caller's access.** #144: `getRunHistory` resolved a workflow owner from the GLOBAL `workflowsByLocation` index with no caller check, then read `ross/runs/${ownerUid}/...` → an authed user supplying another tenant's (workflowId, locationId) leaked their run history (confirmed HIGH IDOR). The human CFs were safe (they call `verifyLocationAccess` before resolving); the agent adapter dropped that guard. Fix: caller must BE the owner or have the location in `userLocations/${caller}`. **Guard (guard-first per #143): a cross-tenant unit test in `tools.test.js` (attacker→[] / delegated-manager→served).** Heuristic: treat every LLM-supplied tool arg as attacker-controlled; tenancy is enforced from the server-derived uid, never inferred from an arg. **Capability-absence > prompt-guarding — a jailbroken model can't exfiltrate what no tool returns.** | Session: askRoss security audit (#144) |
| 2026-06-05 | evals/loop-signalling | **When a harness RE-DRIVES a production function, what that function EMITS vs RETURNS is a real seam — fold both, or silently drop half the outcome.** Slice 6 eval driver: `runAgentLoop` *emits* `text`/`action` events but *returns* `{paused, pendingTool}` (a proposed confirm) and `{error}` — it never emits those. A driver that folded only emitted events left `transcript.confirms`/`error` permanently empty + two dead `emit` branches; the final review caught it. Also caught (controller, not a per-task review): a cross-task bug where the test-isolation client reset (`__setClientForTests(opts.client||null)`) would null the *live* runner's `ensureClient` client — fixed to reset only when a test addresses it (`'client' in opts`). **Heuristics: (1) before reusing a function in a new harness, read its RETURN statements, not just its `emit`/callback calls — signalling splits across both. (2) per-task reviews can't see cross-task seams (test-helper reset vs live entrypoint) — that's the controller's/final-review's job.** Sister to the slice-5 "final review catches cross-turn seams" lesson. | Session: askRoss slice 6 eval harness (#142) || 2026-06-04 | client/sse-transport | **To consume an authenticated SSE Cloud Function from the browser you CANNOT use `EventSource` — it is GET-only and cannot set an `Authorization` header.** askRoss slice 5: `rossChat` is a POST `onRequest` SSE CF gated by a Bearer ID token. The native `EventSource` API only does GET and exposes no header hook, so the client streams via `fetch(url, {method:'POST', headers:{Authorization}, body})` then reads `res.body.getReader()` + `TextDecoder` + a hand-rolled `\n\n` frame parser. Two further footguns surfaced: (a) **`rossChat` reads `req.body` FLAT** (`{message,threadId,clientToday}`), NOT the `{data:{...}}` envelope the sibling callable-style services (`rossV2Snooze`, `rossGetHomeWorkflowDigest`) use — a test asserts `sent.data === undefined` to lock it; (b) make the transport **never throw** — surface every failure (no-auth, network, non-ok HTTP, mid-stream abort) as a synthetic `{type:'error'}` event so the reducer renders banners through one path. **Heuristic: authenticated browser SSE = `fetch`+`ReadableStream`, never `EventSource`; and always diff the target CF's body-parse shape (`req.body` vs `req.body.data`) against the sibling you're copying — they diverge.** | Session: askRoss slice 5 (#139) |
| 2026-06-04 | vue/escaping | **The "escapeHtml the agent/server string" requirement is satisfied STRUCTURALLY in Vue by text interpolation — `{{ }}` auto-escapes; the real rule is "never `v-html` on agent/server content."** Slice 5's spec + backlog said "must escapeHtml the confirm-card" (written assuming `innerHTML`). But the confirm-card/message bubbles are Vue SFCs — rendering `pending.summary`/`turn.text`/`a.tool` via `{{ }}` is auto-escaped, so no `escapeHtml()` import is needed (and importing the v1 helper would be noise). The discipline becomes a grep: confirm ZERO `v-html` anywhere agent/server text reaches the DOM. **Heuristic: on a Vue surface, translate any "escapeHtml X" requirement into "render X via `{{ }}`, audit for `v-html`" — don't reach for the string helper the v1 innerHTML code used.** | Session: askRoss slice 5 (#139) |
| 2026-06-03 | js/lazy-require-scope | **A `require(...)` placed INSIDE one function is invisible to sibling functions in the same module — a sibling that uses the same symbol gets `undefined` and fails at call time, not load time.** askRoss slice 4: `executeTool` was lazy-required *inside* `runAgentLoop` (slice 3, to keep the core import-cheap). When `resumeChatRequest` (a new sibling function) called `executeTool(...)` directly, it hit a `ReferenceError` — which the approve path's `try/catch` swallowed into an `is_error` tool_result, so the tool **silently never executed** (workflow stayed unpaused). No syntax error, no load error; only the resume-approve unit test caught it (status assertion failed). **Heuristic: a lazy-`require` is function-scoped — if a second function needs the same dependency, require it there too (or hoist to module top). When you copy a pattern from one function to a new sibling, re-check that every symbol it uses is actually in the new scope.** Sister to the "verify the code path actually executes" family — a swallowed error in a catch hides a missing binding. | Session: askRoss slice 4 (#136) |
| 2026-06-03 | process/session-start | **An inherited worktree can be many commits behind master with a DELETED upstream — read the feedback files there and your whole session-start sprint picture is stale.** This session opened in a harness worktree on `chore/bank-session-status` whose remote was `[gone]` and which was 11 commits behind master; the four feedback files read at session start described ④a's rule-lock as the *next* task when #125/#127 had already shipped + deployed it. Caught only because the operator said "I'm pretty sure that's done" → `git log origin/master` confirmed. The validated "git fetch before backlog" pattern exists, but I read the working-tree copies without first confirming the branch was current. **Heuristic: at session start, BEFORE trusting any in-repo doc (backlog / LESSONS / memory), run `git status -sb` + `git log origin/master --oneline -5`; if the branch is behind or its upstream is `[gone]`, sync to master (or re-read from `origin/master`) FIRST.** Sister to the validated git-fetch-before-backlog pattern, sharpened for inherited/stale worktrees. | Session: askRoss slice 3 (#134) |
| 2026-06-03 | billing/idempotency | **Swapping an idempotency key from caller-supplied to per-invocation-random (to close an abuse vector) silently removes the dedup benefit AND creates unbounded guard-node growth.** rossChat slice 3: the security review flagged that deriving `requestId` from an inbound header let a client reuse it to short-circuit the debit (free turns), so I switched to a server `crypto.randomUUID()`. Correct fix — but now every turn writes a *unique* `billing/debitGuard/{uid}/{requestId}` that never dedupes a real retry (there is no retry path in slice 3) and accumulates ~1/turn forever (the automated review's T-2). **Heuristic: when you make an idempotency key per-invocation-random, note that (a) the guard no longer dedupes — acceptable only while there is no retry path, and (b) the guard table now grows unbounded → pair it with a retention/TTL sweep.** A genuine retry/resume (slice 4) needs a *server-issued* token threaded across requests, not a fresh UUID. | Session: askRoss slice 3 (#134) |
| 2026-06-03 | billing/sdk-shape | **A field-shape mismatch across two SDK boundaries silently zero-charges.** The Anthropic Messages API returns `usage` in snake_case (`input_tokens`, `cache_read_input_tokens`), but the ① ledger's `recordUsageAndDebit` reads `units` in camelCase (`inputTokens`, `cacheReadTokens`). The askRoss spec's shorthand `units: usage` would map every field to `undefined → 0` → a **0 charge every turn** (revenue silently lost). The ledger itself is CORRECT — it already prices all 4 token classes incl. cache-write/read (`ledger.js:50-69`); the bug lives at the boundary. **Fix: an explicit `toLedgerUnits(apiUsage)` mapper in slice-1 `llm-client.js`, unit-tested against a verbatim real `usage` block.** Caught at SPEC-revision time by ground-truthing the ledger rather than trusting the shorthand. Heuristic: whenever you pass one SDK's response object straight into another's input, diff the field names — a silent zero is worse than a crash. | Session: askRoss spec revision (#129) |
| 2026-06-03 | functions/import-safety | **To keep a lightweight, unit-testable CORE module that wraps logic living in a HEAVY module, lazy-`require` the heavy module INSIDE the function, not at the top.** askRoss agent core: `getWorkflowDigest`'s adapter needs `buildHomeWorkflowDigest`, exported from `ross.js` — but `ross.js` at load registers ~22 `onRequest` CFs + pulls `defineSecret`/firebase-functions, so a top-level `require('../ross')` would drag all that into the core, slow imports, and can throw under vitest (no admin init / secrets). Putting the `require` *inside* `run()` keeps `tools.js` import-cheap + testable (the 3 self-contained adapters test against the fake RTDB; the ross-dependent one defers to slice-3 integration). Same trick decouples the Agent SDK (`toSdkMcpServer` lazy-requires it behind a test seam). Heuristic: a core module's top-level requires should be cheap + side-effect-free; defer anything heavy/side-effectful to call time. | Session: askRoss agent core (#130) |
| 2026-06-02 | tools/rtdb-write-windows | **To write a single RTDB value from a Windows dev box, a direct REST `PATCH` with a `gcloud auth print-access-token` token is the reliable method — the other paths all fail.** PR5 deploy (setting `subscriptionTiers/free/limits/maxWorkflows = 5`): (a) the firebase MCP `realtimedatabase_set_data`/`get_data` tools errored internally (`Cannot read properties of undefined (reading 'join')`); (b) `firebase database:update <path>` rejects stdin on Windows (`STDIN input is not available`); (c) per the 2026-06-01 lesson, PowerShell `Out-File -utf8` adds a BOM that breaks firebase's JSON parse, and git-bash MSYS mangles the `/path`. The winner: `curl -X PATCH -d '{"maxWorkflows":5}' "https://<proj>-default-rtdb.firebaseio.com/<path>.json?access_token=$(gcloud auth print-access-token)"` — the OAuth token acts as **admin** (bypasses rules, same as the `.settings/rules.json` readback + Admin SDK), and **PATCH merges** so sibling keys are preserved. Read back the same way to verify. Sister to the 2026-06-01 CLI-write lesson — REST PATCH sidesteps MSYS/BOM/stdin entirely. | Session: PR5 maxWorkflows deploy (#127) |
| 2026-06-02 | rtdb/rules-validate | **A parent-level `.validate` using `newData.hasChild('X')` to reject field X is BUGGY for multi-path `update()` — `newData` at the parent reflects the POST-MERGE state, so it "sees" X whenever X already exists in the DB, even when the write doesn't touch X.** PR4 entitlement lock: the merged plan's §2 proposed `subscriptions/$uid .validate: "... && !newData.hasChild('features') && !newData.hasChild('limits')"` to make the resolver the sole writer. That would have false-rejected EVERY admin status/extend write on any already-materialized subscription. Correct tool: **child-level `"features": { ".validate": false }`** — it only evaluates when the write tree actually includes data at/under that node, so admin writes to other fields pass while any client write to features/limits is rejected. And child `.validate:false` IS effective (unlike child `.write:false`, the dead-rule trap) — `.validate` does not cascade-grant. Caught at ground-truth/implementation time, not in the merged plan. **Heuristic: to forbid a client from writing a specific subtree, use child `.validate:false` on that subtree, NOT a parent `!newData.hasChild()` guard.** | Session: PR4 rule lock (#125) |
| 2026-06-02 | process/writer-census | **A "grep ALL client-SDK writers before a rule lock" census must include inline `<script>` in HTML pages — not just `.js`/`.vue` modules.** PR4: I grepped `public/js/**.js`, routed 5 admin tier sites, and MISSED a 6th — a standalone legacy tool `public/admin_tools/enhanced-user-subscription-manager.html` (linked live from `admin-dashboard.html`) with the same multi-path features/limits write inline in a `<script type="module">`. Because RTDB `update()` is atomic, the lock's child `.validate:false` would have rejected the WHOLE tier change → hard-broken that admin workflow post-deploy. The security-reviewer agent caught it. **Heuristic: the validated grep-all-writers pattern (2026-05-19) must use `rg --include=*.js --include=*.html --include=*.vue`; inline HTML scripts are live code.** Sister to the validated grep-all-writers-before-tightening pattern. | Session: PR4 rule lock (#125, security-review catch) |
| 2026-06-01 | deploy/firebase-cli | **A `firebase deploy --only functions:X` analyses the WHOLE codebase, so an undeployed `defineSecret` blocks ALL function deploys — and the CLI hangs on exit after a large deploy.** Deploying the billing CFs surfaced that `MERAKI_SHARED_SECRET` + `INITIAL_ADMIN_SETUP_SECRET` (introduced by OWASP #89's `defineSecret` migration) were **never provisioned** → non-interactive deploy errors `no value for the secret`. So no function had deployed since #89 — the entire OWASP hardening was sitting live-undeployed for a day. **Heuristics:** (1) after any change that adds `defineSecret`, the secret MUST be set (`firebase functions:secrets:set NAME`) before the next deploy of ANY function; check with `firebase functions:secrets:get NAME` (404 = missing). (2) Large deploys hit **429 'Per project mutation requests' quota** → firebase auto-retries; don't panic. (3) **firebase-tools hangs after a successful deploy on Windows** (process stays alive, 0 output if piped through `tail`) — verify the END STATE independently (`firebase functions:list` count, fetch live rules via `…/.settings/rules.json?access_token=$(gcloud auth print-access-token)`) instead of waiting on the command; kill the zombie once confirmed. (4) A `106 vs 107` count gap traced to a **phantom export** — `queueAnalytics.js` `exports.X` clobbered by a later `module.exports = {}` → `No function matches the filter`. **CLI RTDB writes:** git-bash MSYS mangles `/billing/priceTable` (use `MSYS_NO_PATHCONV=1` or PowerShell); PowerShell 5.1 `Out-File -utf8` adds a BOM that breaks firebase's JSON parse (use `[IO.File]::WriteAllText`). | Session: Phase 7 ① ledger deploy (2026-06-01) |
| 2026-05-31 | tools/git-commit | **Backticks inside a double-quoted `git commit -m "..."` are shell command-substitution — they execute, mangling the message.** Phase 7 ledger build: a commit body with `` keyset `before` pagination `` ran `before` as a command (`bash: before: command not found`); the commit still landed but with the backtick content stripped/evaluated. **Fix: for any commit message containing backticks/`$`/parens, use a single-quoted heredoc — `git commit -m "$(cat <<'EOF' ... EOF)"`** (the quoted `'EOF'` delimiter disables interpolation). Used that form for the rest of the build with no recurrence. Sister to the general "shell-eval of unquoted metacharacters" class. | Session: Phase 7 ① ledger build (PR #113) |
| 2026-05-31 | agent/no-fabrication | **An LLM agent with write tools must be forbidden — server-side, not just by prompt — from auto-submitting any value it cannot actually obtain or verify; otherwise it will fabricate records.** askRoss Agent spec review (#110): `submitResponse` was tiered `auto` as "drudgery Ross absorbs". But ROSS compliance templates (Health & Safety Audit, Compliance Sweep) carry `inputType: temperature`/`yes_no` regulatory measurements — the agent has no sensors, so auto-submitting a value would **fabricate a food-safety compliance record** (regulatory/liability risk, and a direct violation of "amplify, not replace"). The 422 `requiredNote` net stops an *incomplete* submission but NOT a *fabricated primary value*. Fix (spec §3.1): `submitResponse` auto only for non-measurement types (`text`/`checkbox`/`signature`, `requiredNote` false); measurement/attestation types escalate to the owner; **enforced in the adapter server-side + as a hard eval assertion**, not left to model behaviour. **Heuristic: for every agent write-tool, ask "can the agent legitimately KNOW this value?" — if it asserts a real-world measurement/verification the agent didn't perform, the tool must refuse or escalate, in code.** Sister to auth-vs-authz: defence that doesn't depend on the model (or the user) behaving. | Session: askRoss Agent spec (PR #110 review) |
| 2026-05-31 | process/design-audit | **The field-verify / write-path discipline pays off at SPEC-DESIGN time, not just at build time — auditing the live code while *designing* caught a privilege-escalation vuln before any code was written.** Phase 7 entitlements design: dispatched an Explore agent to map the real subscription module (every writer/reader/rule) to validate a "resolver becomes the sole writer of `features`/`limits`" assumption. The audit confirmed the materialization pattern fit — AND surfaced that `subscriptions/$uid` `.write` lets any authed owner write their own `features`/`limits` (database.rules.json:33), actively exercised by client `subscription-service.js` (writes from a hardcoded RTDB-unsynced constant). Fix folded into the design (server-side resolver = sole writer, then lock the rule). **Heuristic: when a design assumes "X becomes the sole writer/reader of Y", audit every current writer/reader of Y against the live code BEFORE writing the spec — the audit both validates the assumption and routinely surfaces the security/drift problems the docs hide.** Sister to the field-verify SELF_OPT + auth-vs-authz, applied at design time. | Session: Phase 7 entitlements design (PR #107) |
| 2026-05-31 | process/pr-amend | **Amending an open PR's doc to address review feedback gets orphaned if the operator merges while you edit — a squash-merge closes the branch and your follow-up commit lands nowhere useful.** PR #106 (ledger spec): operator merged it while I was resolving the §10 open questions per "you decide"; my follow-up commit landed on the already-merged branch, NOT master. Recovery: cherry-pick onto a fresh branch off `origin/master` → follow-up PR #108. **Heuristic: when addressing review by amending an open PR, either (a) batch all edits and push before the operator reviews, or (b) `gh pr view N --json state` before pushing an amendment; if MERGED, branch off master and open a follow-up PR rather than pushing to the dead branch.** | Session: Phase 7 ledger spec (PR #106 → #108) |
| 2026-05-31 | rtdb/rules-cascade | **RTDB read permission CASCADES from a parent, and a query is authorised at the QUERIED node — rules cannot filter query results.** Two consequences that drove the whole H-2 design (PR #96): (1) a root `".read": "auth != null"` makes every properly-scoped per-child rule below it DEAD — the node is fully readable regardless. So "tightening" means removing/admin-scoping the ROOT, not just fixing children. (2) You cannot express "let a user query `guests` where `locationId` is theirs" — `query(ref('guests'), orderByChild('locationId'), equalTo(x))` is auth'd at the `guests` root and rules don't post-filter. So a node read via a root-level location-*query* by non-admins CANNOT have its root locked without changing the client read path (→ that's why guests/queue became "Bucket C", deferred for a restructure). Nodes read by **direct keyed path** (`users/$uid`, `salesDataIndex/byLocation/$locId`) CAN be locked + given a scoped child rule with no client change. **Heuristic before touching any root `.read`: map every consumer and classify each as direct-path (lockable now) vs root-query (needs refactor first). Use the `userLocations/$uid/$locId` cross-ref pattern (already in `stockFlagAudit`/`stockItemFlags`) for location scoping — and include the `locations/$locId/ownerId === auth.uid` fallback (automated review caught its omission as a must-fix: users with unpopulated userLocations would lose their own data).** Sister to auth-vs-authz + the field-verify SELF_OPT — verify ground truth before changing behaviour, here applied to rule cascade + query semantics. | Session: PR #96 (H-2 cross-tenant design + review must-fix) |