# Lessons Learned

Rolling log of non-obvious discoveries. Max 20 entries — oldest dropped when full.
Updated by agents at session end when a session hits a gotcha, failed approach, or surprise.

## Entry Guidelines

- Only log **non-obvious** lessons — skip things derivable from reading the code
- Include the **module** and **source session** for traceability
- If a lesson leads to a validated optimization pattern, note that in SELF_OPTIMIZATION.md

## Lessons

| Date | Module | Lesson | Source |
|------|--------|--------|--------|
| 2026-04-08 | project | CLAUDE.md set to read-only blocked all development work — CLAUDE.md must match the actual agent role, review it when project workflows change | Session: claude-md-rewrite |
| 2026-04-13 | food-cost | LocationService had the same sequential `for...of` + `await get()` pattern we'd already fixed in database-operations.js — when fixing a pattern, grep the entire module for the same anti-pattern | Session: food-cost-deploy |
| 2026-04-13 | food-cost | Version bump is essential after perf fixes — without cache-busting query strings, browsers serve old cached scripts even after deploy | Session: food-cost-deploy |
| 2026-04-13 | food-cost | Version strings were scattered across 6 files (index.js, admin-dashboard.js, refactored-app-component.js, historical-usage-service.js, food-cost-analytics.html, cost-driver.html) — grep for the old version string to catch all occurrences | Session: food-cost-deploy |
| 2026-04-13 | food-cost | Vue watchers on modal open caused cascade: setting 4 params each triggered `regeneratePurchaseOrder()` — use `_initializing` guard when batch-setting values | Session: po-perf |
| 2026-04-13 | food-cost | `clearCache()` before every PO regeneration defeated the service's own 30-min TTL cache — never clear cache unless the underlying data source changes | Session: po-perf |
| 2026-04-13 | food-cost | Dynamic `import('../../config/firebase-config.js')` returned HTML instead of JS module — use already-imported functions from the top-level import, never re-import dynamically | Session: po-perf |
| 2026-04-13 | food-cost | Root `stockUsage/` and `stockData/` legacy reads added ~8s per call with no data found — always verify legacy fallback paths actually have data before keeping them | Session: po-perf |
| 2026-04-13 | food-cost | Initial investigation blamed multi-strategy search loop, but logs proved only 1 strategy ran (name resolution was broken). Always verify assumptions with actual console logs before proposing fixes | Session: po-perf |
| 2026-04-30 | food-cost | `attachItemKeys` only runs in `buildFlagPipelineContext` (post-save). Rows in `editableItems` between CSV upload and save have no `itemKey`, so any pre-save consumer (e.g. `FlagTagModal`) silently fails its precondition check. When adding a new consumer of `itemKey`, prefer the pure `withItemKey(item)` helper so the row in the editable table is not mutated mid-edit | Session: flagtagmodal-itemkey-fix |
| 2026-04-30 | process | Auto-mode "prefer action over planning" is *lower* priority than CLAUDE.md's Standard Task Workflow. Self-classifying a feature-module bugfix as the "trivial exception" to skip KB-read, plan, failing test, immutability, smoke, and preview is corner-cutting — every step is mandatory unless the user explicitly authorizes the skip for that request | Session: flagtagmodal-itemkey-fix |
| 2026-04-30 | functions/deploy | A worktree's `functions/` directory has no `node_modules` until you `npm install` there. `firebase deploy --only functions` from a worktree fails with `MODULE_NOT_FOUND` for sendgridClient (which transitively requires `@sendgrid/mail`). Fix: `cd functions && npm install` once per worktree | Session: ross-v2 sprint (CORS deploy) |
| 2026-04-30 | functions/cors | The Cloud Functions CORS allowlist hardcoded only `merakicaptiveportal-firebasedb.web.app` + `firebaseapp.com`. Preview channels live at `<site>--<channel>-<hash>.web.app` and were rejected by the preflight, so admin login silently failed (`verifyAdminStatus` returned non-OK and the gate showed "not admin"). Use a function-form `origin` callback that accepts a regex for the channel pattern | Session: ross-v2 sprint (PR #17) |
| 2026-04-30 | ross/v1-data | `rossGetWorkflows` and `rossGetReports` return `locationId` as `locationName` when the workflow was created without explicit `locationNames` array (`functions/ross.js:348` fallback). Old workflows are stuck with this. Client must enrich from `locations/{id}/name` per row for display. Helper duplicated in 3 stores so far — factor out planned | Session: ross-v2 sprint (PRs #23 / #24) |
| 2026-04-30 | hifi-design-system | `HfNavItem` renders as `<button>` (non-navigating) unless you bind `:href` to it. Easy to miss in a `v-for` over data that carries `href` if you only bind `:label` and `:icon` and rely on it "just working" | Session: ross-v2 sprint (PR #23 review) |
| 2026-04-30 | conventions | v1 conventions in CLAUDE.md (e.g. "SweetAlert2 for all user notifications") don't automatically apply to v2 surfaces. Hi-Fi v2 has its own visual language; SweetAlert2's rounded-modal Bootstrap-ish look is a clash. Convention update needed: SweetAlert2 is v1-only; v2 uses inline confirms / banners or HfModal (TBD) | Session: ross-v2 sprint (PR #25) |
| 2026-04-30 | git/concurrency | `git fetch origin master` can fail with `cannot lock ref 'refs/remotes/origin/master'` during concurrent worktree operations (likely Firebase deploy or another git op holding the ref). Symptom: stale local view of master. Fix: retry the fetch after a moment; the lock clears | Session: ross-v2 sprint |
| 2026-04-30 | process | The session-end feedback loop (PROJECT_BACKLOG, SELF_OPT, LESSONS, SCORECARD) must be updated *per PR or significant change*, not "at the end of the session". Skipping the per-PR cadence and trying to backfill 9 merges' worth of reflections from memory loses signal — the patterns and gotchas blur together | Session: ross-v2 sprint (this) |
| 2026-05-01 | process | The Session Opening Protocol's "read PROJECT_BACKLOG.md" step is necessary but not sufficient. The backlog can be stale relative to master if a prior session ended without running the Step 11 reflection. Always cross-check the "In Progress" row and "Recently Completed" table against `git log --oneline -10` before stating sprint state — if PR numbers on master are missing from Recently Completed, flag the drift and offer to sync before doing anything else | Session: backlog-sync-pr26 |
| 2026-05-01 | ross/data-shape | Templates store their id as `templateId` (server seed + `rossCreateTemplate`) and their tasks as `subtasks` (server seed + `rossActivateWorkflow`). The KB doc had drifted to `id: string` + `tasks: Task[]` and the PR #28 read view trusted the doc — result: `t.id` was `undefined`, `openActivateTemplate(undefined)` set the store to a falsy value, the editor's `mode` computed silently fell into `create` instead of `activate`, the user got an empty workflow. **Always verify field names against the actual server seed/CF code, never against KB docs that may have drifted.** When introducing a `pick X then act on it` flow, log a warning if the picked id is falsy at the action handler — don't let it silently fall through to a default mode | Session: ross-v2 phase 4d.1 (PR #28 review) |
| 2026-05-01 | rtdb/security-rules | `database.rules.json` line 139 grants self-read at `admins/$uid` (`auth.uid === $uid`), so a v2 client can probe its own admin record for UI gating without rule changes — non-obvious because the parent `admins` node `.read` requires `auth.token.admin === true`. Also: line 141 `.validate` requires the `superAdmin` child on every write, so post-rule-era `admins/{uid}` is always an object — `typeof v === 'object' && v.superAdmin` is the correct idiom (a literal `true` value would be a pre-rule legacy entry) | Session: ross-v2 phase 4d.2 (PR #30) |
| 2026-05-01 | vue/v-for | v-for keyed on the array index for components with editable local state (focus, half-typed input) breaks on reorder — after splice, the wrong row keeps the stale state. Fix: stable `_uid` per row generated at row-creation time (`crypto.randomUUID()` is fine in modern Vite-built targets, no polyfill needed). Strip `_uid` in the explicit field map at payload-build time so server never sees it. Caught in PR #30 review; the same anti-pattern existed in `RossPlaybookWorkflowEditor.vue` from PR #28 — **when fixing a v-for-on-index bug, grep the module for the same anti-pattern** | Session: ross-v2 phase 4d.2 (PR #30 review) |
| 2026-05-01 | ross/v2-validation | An empty `daysBeforeAlert` chip array silently passes server-side validation and saves a no-alert workflow/template. Server filters with `.filter(d => Number.isInteger(d) && d > 0)` but doesn't reject empty. Client-side `errors` computed must reject empty explicitly (`if (!form.daysBeforeAlert.some(d => Number.isInteger(d) && d > 0))`) — the chip UI makes "deselect all" easy enough that this needs guarding. Same gap in both editors; fix in both | Session: ross-v2 phase 4d.2 (PR #30 review) |
| 2026-05-01 | process | When the user asks "have you reviewed your system ruleset?" early in a session, take it literally — re-read all four feedback files (PROJECT_BACKLOG + SELF_OPT + LESSONS + SCORECARD), state the relevant patterns/lessons explicitly back to the user before declaring "ready to proceed". Skipping LESSONS in particular meant I almost trusted the KB doc again on PR #30 instead of grepping ross.js up-front. The Session Opening Protocol formally requires only the backlog; the other three are the *how* and have to be loaded into working memory at session start to actually fire | Session: ross-v2 phase 4d.2 (PR #30) |
| 2026-05-01 | ross/v1-data | `rossGetWorkflows` (functions/ross.js:632-639) only flattens its response when a `locationId` arg is supplied — server hoists `tasks`, `locationStatus`, `locationNextDueDate` to the top level. **Without `locationId`, the response is the unflattened `{ ..., locations: { [id]: {...} } }` shape.** The v2 client `getPlaybookWorkflows()` was calling without `locationId`, but `RossPlaybook.vue` keys cards on `${w.workflowId}::${w.locationId}` — so `w.locationId` was `undefined` and the per-location card layout was rendering "by accident" since 4d.1 (location chip hidden by `v-if="w.locationName"` falsy guard). PR #32 fixed by flattening client-side in the service. **When a v2 surface assumes a shape that contradicts what the service returns, the layout will appear to work as long as the missing fields are render-guarded — verify per-row identity early.** | Session: ross-v2 phase 4e.1 (PR #32) |
| 2026-05-01 | hifi-design-system | `HfIcon.vue` paths map at `public/js/design-system/hifi/components/HfIcon.vue:13-47`. No `alert`/`warning`/`info` icon shipped in the original kit. Don't approximate with `bell`/`bolt`/`fire` — add a new entry to the paths object. The kit is small enough (~33 icons) that a clear semantic miss warrants the addition. PR #32 added `alert: '<path d="M12 3 2 20h20L12 3Z"/><path d="M12 10v5"/><path d="M12 18v.5"/>'` (triangle + bang). Pattern: 1.5px stroke, 24×24 viewBox, line-cap round | Session: ross-v2 phase 4e.1 (PR #32 review) |
| 2026-05-01 | vue/forms | A computed setter that silently coerces invalid input back to a default hides the rejection from the user — the field validator never fires because the stored value is already legal. PR #32 first attempt: `scaleVal.set(v) { patch('scale', Number.isInteger(Number(v)) ? Number(v) : 5) }` ate `3.5` and silently stored `5`. Fix: pass the raw input through (`patch('scale', v === '' ? null : v)`) so `validateInputConfig` can surface "Scale must be an integer between 2 and 10". Add `step="1" min="2" max="10"` on the `<input>` as first-line UX guidance. **Setters should be transparent about what the user typed; the validator is the gate, not the setter.** | Session: ross-v2 phase 4e.1 (PR #32 review) |
| 2026-05-01 | process | The `using-superpowers` rule "even a 1% chance a skill might apply, you ABSOLUTELY MUST invoke the skill" applies to **plain English requests that match a skill's purpose**, not just `/<name>` invocations. PR #32 session: user asked "create a plan?" and I drafted one inline because the structured thinking was already done. User explicitly called this out — re-running via `/plan` + `planner` agent produced a tighter spec with explicit open questions. The red-flag thought "This doesn't need a formal skill" is the rationalization the rule names. **When the user's words map cleanly onto a skill's description, invoke the skill — the discipline forces structure I'd skip rolling my own.** | Session: ross-v2 phase 4e.1 (user feedback) |
