# Project Backlog — Sparks Hospitality

> Claude reads this file at the start of every session and updates it at the end.
> The Sprint Goal is the contract for the session — don't deviate without explicit user confirmation.

Last updated: 2026-06-07 (**Parallel-orchestration session — 4 scope-disjoint PRs merged: #147 (drop `v-html` on the Ross home story card → close the latent stored-XSS + a `no-v-html` forward guard), #148 (askRoss slice-5 polish — mobile pill wiring, fetch abort/reset-on-close, ⌘K cold-start hoist, conversation reset keys; 24 tests), #149 (eval-harness Group-A fairness — realistic 2026 fixture dates, `toolResults` capture seam, judge grounding via synthetic `__systemContext__`; expected ~15→18/21 honestly, Group B left failing as real product signal), #150 (CF auth-posture guard tests — 8 admin-only CFs verified dual-factor gated; 20 tests).** Ran as **4 parallel worktree subagents** on disjoint file scopes (orchestrator carried the session protocol + serialized merges + folded each PR's review nits via SendMessage). Both fold rounds caught **errors in the automated reviewer's own suggested fixes** (#149: a `toolUseId` field that doesn't exist on `toolsCalled`; #150: a `verifyAuthToken` count inflated by the import line) — the verify-the-reviewer's-remedy discipline fired twice. Backlog 3aa-GroupA + 3a-slice-5 done; 3c CF-auth-posture seed item done; the v-html triage-queue bug CLOSED. Still parked for a focused session: 3aa **Group B** (Ross tool-calling proactiveness — prompt eng + live key), 3c **CI gate** (baseline not green) + **vue/no-v-html** (now unblocked). ——— Prior: 2026-06-05: **Phase 7 ② askRoss AGENT BUILD COMPLETE — slice 6 eval harness (#142) + cross-tenant security fix (#144) MERGED.** The askRoss agent is now built end-to-end: slices 1–7 + the #140 schema fix + #144 security, all merged and live; Ross answers in prod, is eval-covered (`npm run eval`, 21 cases vs the real API + Haiku judge + a cross-tenant probe) and security-audited (all 8 adapters; the one HIGH IDOR in `getRunHistory` closed). **#142** — the live eval harness (machinery unit-tested in CI, run is live/manual; reuses prod gates/loop/prompt; no debit/persistence; 196/196 green). **#144** — the agent runs via the Admin SDK so adapter code is the only tenant boundary; added the missing caller-access check, independent security-review confirmed it was the sole hole, deployed + verified. **Remaining:** one operator live `npm run eval` (needs the key); slice-6 follow-ups (harvester from real `agentChats`, nightly cron, judge tool-results) + the #144 shared location-access helper → backlog 3a; next major = ③ Payment Rail + ④b tier migration (deferred). ——— Prior: 2026-06-04: **Phase 7 ② askRoss slices 5 + tool-schema fix MERGED + LIVE (#139 + #140)** — Ask Ross is now functional end-to-end in prod, verified by a real turn. **#140 was a launch-critical fix:** the agent loop 400'd on EVERY turn because `toAnthropicTools` emitted the OpenAPI-3.0/draft-4 dialect (boolean `exclusiveMinimum`) but the Anthropic API requires JSON Schema draft 2020-12 — found via prod logs while smoke-testing the #139 preview (139 server tests all mocked the LLM client, so the dialect was never validated against the real API); fixed by dropping `{target:'openApi3'}`, +2 regression guards, rossChat redeployed. **#139 (slice 5)** — the live ⌘K Ask Ross client; see below. ——— Prior: 2026-06-04: **Phase 7 ② askRoss slice 5 — Ask Ross ⌘K client (#139)** — the live consumer of `rossChat`. A ⌘K command-palette modal (`RossAskModal.vue`) streams a real multi-turn Ross conversation over the SSE CF, renders **inline confirm-cards** (Approve→resume / Cancel→decline) for confirm-tier actions, live tool-action lines, and friendly terminal/error banners. Built as 3 pure/thin modules + 3 components: `ross-agent-sse.js` (stateful frame parser), `ross-agent-conversation.js` (immutable event→state reducer, reuses `threadId`), `ross-agent-client.js` (`fetch()`+`ReadableStream` transport — native `EventSource` is GET-only & can't send an `Authorization` header; sends a **FLAT** body since `rossChat` reads `req.body` directly, NOT `req.body.data` like sibling services; all failures → synthetic `{type:'error'}` events). Opens from ⌘K / rail panel / header button / card `#ask=<seed>` deep-link; scripted `askRoss()` stub retired. **20 new unit tests** (parser/reducer/transport — the risky pure logic) + build green + **173/173 functions tests** (no regression). Subagent-driven build: per-task spec+quality review on all 8 tasks + a final cross-cutting review; 2 findings folded (fresh bubble on resume, finalize turn on terminal/error) + 1 correctly DECLINED (`:busy="convo.busy"` would have disabled the confirm buttons since busy stays true during a pending confirm). Security: no `v-html` (all server text auto-escaped). **Desktop only** — mobile pill wiring + fetch-cancellation + ⌘K-cold-start + hash-clear → backlog item 3a. **NEXT = slice 6 (20-prompt eval harness)** — the last askRoss build slice. ——— Prior: 2026-06-03: **Phase 7 ② askRoss slice 4 confirm-flow MERGED (#136)** — the agent now authors the playbook with the owner in the loop. A `confirm`-tier tool PAUSES → persists `ross/agentPending/{uid}/{turnId}` (10-min expiry, server-keyed, uid-scoped) + debits Request A (§2.1 — abandoned confirms still bill) + emits a `confirm` card; the owner's resume (`{resumeTurnId, decision}`) re-runs the pre-flight gates (kill-switch holds), fail-closed expiry, **atomic one-time-consume**, executes (audited `confirmed:<uid>`) or declines, then continues (Request B, distinct requestId). HALF B: extracted the 3 live workflow CFs' write-logic into owner-callable `*AsOwner` cores in `ross.js` shared by the CF handler AND the agent adapters (byte-for-byte parity); promoted the 4 confirm tools pending→READY (those 3 handlers had zero prior tests → `ross-workflow-ops.test.js` is the net). 8 commits / 7 TDD phases; **two review rounds — local security (H-1 gate-on-resume, M-1 fail-closed expiry, M-2 payload guard) + automated (M-1 pending-flag symmetry, M-2 email-in-audit, M-3 summary fallbacks), all folded.** 166/166 tests + build green. **⚠ rossChat is LIVE — redeploy needed** (`ANTHROPIC_API_KEY` provisioned; `cd functions && npm install`). **NEXT = slice 7 `ross/agent*` RTDB rules + prune** (launch-blocker before slice 5). ——— Prior: **Phase 7 ② askRoss slice 3 `rossChat` reactive SSE CF MERGED (#134)** — the project's **first AI-inference Cloud Function**. `functions/agent/rossChat.js`: 4 pre-flight gates (kill switch → per-owner enable → `features.rossAgent` → ledger balance; super-admins skip entitlement+balance, NOT the kill switch) → cached two-breakpoint prompt + bounded history → `runAgentLoop` (streams text; auto tools via `executeTool`; non-auto refused server-side — confirm-flow is slice 4) → single idempotent `recordUsageAndDebit(toLedgerUnits(usage), requestId)` → persist turn + SSE. New `verifyRossAgentAccess` (recognises `rossAgent`; `verifyUserOrAdmin` untouched) + a `ross.js` test seam. Built in 6 TDD phases; **two review rounds (local security+code, then automated — no blockers)**, all findings folded: server-generated `requestId` (closed a free-turn vector), threadId/clientToday validation, best-effort loop that still debits partial spend on an LLM error, structured SSE events, warm-client reuse. **139/139 functions tests + build green;** catalog updated (both copies). **⚠ deploy SECRET-FIRST** (`functions:secrets:set ANTHROPIC_API_KEY` — unprovisioned `defineSecret` blocks ALL deploys); server-only, no client yet. Remaining: slice 4 confirm-flow, 5 Ask Ross client, 6 evals, 7 `ross/agent*` RTDB rules (before slice 5). T-2 (`billing/debitGuard` unbounded growth from the per-request UUID) → bug queue. ——— Prior: **Phase 7 ② askRoss slice 1 (`llm-client.js`) MERGED (#132)** — the swappable wrapper over the raw Anthropic Messages API for the v1 reactive engine: `streamTurn` (pipes text deltas to `onText`, returns `{content,usage,stopReason}`), `createTurn` (non-streaming eval judge), **`toLedgerUnits`** (snake→camel mapper closing the #129 §2.1 zero-charge trap), `MODELS`, lazy/injectable client seam. 9 tests; API surface verified vs the live SDK docs (context7); deliberately NOT using `beta.toolRunner` (reactive v1 owns its loop). Review **1 🔴 folded** — Haiku model ID needed the date suffix `claude-haiku-4-5-20251001` (short form would 404 at runtime) — + 2 nits (`??` vs `||`, `maxTokens` 4096). 48 agent tests + build green. **Slices 1+2 done; NEXT = slice 3 `rossChat` SSE CF** (pre-flight gates → agent loop wiring `streamTurn`+`executeTool` → `recordUsageAndDebit(toLedgerUnits(usage))` → SSE + `defineSecret` + CORS + `rossAgent`-aware auth). ——— **Phase 7 ② askRoss Agent BUILD STARTED.** After a design session on the **Claude Agent SDK vs the raw Anthropic Messages API**, the agent spec was revised (#129: **§1.1 engine-agnostic core** — v1 reactive *owns* the loop on the raw API for the confirm-pause + per-turn billing; v2 proactive *rents* the loop on the Agent SDK for the unattended sweep — plus **§2.1 per-engine billing**). A ground-truth check of `functions/billing/ledger.js` confirmed it already prices the 4 cache-token classes (no change) but caught a snake_case API `usage` → camelCase ledger `units` **silent zero-charge trap** → slice-1 `toLedgerUnits` mapper. Then the **engine-agnostic core slice 2 shipped (#130)**: `functions/agent/` (constants/policy/prompt/tools/execute), ONE Zod tool registry → `toAnthropicTools` + `toSdkMcpServer` (lazy/seamed → **zero engine dep**), `effectivePolicy` (tighten-only) + `autoAllowlist`, two-breakpoint cached mode-aware prompt, `executeTool` audit-wrap + no-undo prev capture, `isAgentSubmittable` (§3.1 measurement gate). **4 ready adapters** (getWorkflowDigest/getStaff/getRunHistory/snoozeCard) + **12 pending** (catalog-complete, `run` throws). 39 agent + 87 functions tests; 0 must-fix; 5 review findings folded — incl. **rejecting a reviewer-suggested `agentConfig` client-write rule** as contradicting spec §9 (server-only writes). Deferred follow-ups: 3 `ross.js` read-logic extraction refactors, 3 reader CFs (food-cost/guests/sales), `advanceDueDate` recurrence (overlaps the recurrence-engine bug), slice-7 `ross/agent*` rules timing. **Next: slice 3 `rossChat` SSE.** ——— Prior: 2026-06-02 (Phase 7 **④a COMPLETE — `maxWorkflows` cap MERGED + DEPLOYED (#127)**, the first entitlement-limit enforcement. `rossActivateWorkflow`/`rossCreateWorkflow` gate the active workflow count (`status !== 'paused'`) vs materialized `subscriptions/{uid}/limits/maxWorkflows` → 403 `WORKFLOW_LIMIT_REACHED` + upsell link; superAdmin bypass; absent/`-1` ⇒ unlimited. **Shipped live:** CFs + hosting deployed, `subscriptionTiers/free/limits/maxWorkflows = 5` set (via RTDB REST PATCH — the firebase MCP tool errored + firebase-CLI stdin is unavailable on Windows; a gcloud-token PATCH acts as admin and merges cleanly), recompute 43/43, verified free users materialized `maxWorkflows:5` (all-in-mapped tiers — professional/enterprise — unlimited by absence). Review: 0 must-fix, 3 🟡 folded (upgradeUrl origin guard, TOCTOU comment, ROSS.md drift). With #125 (rule lock) + #127 (cap) both deployed, **④a Entitlements is fully done.** Next major = **② askRoss Agent build** (`docs/plans/2026-05-31-askross-agent-design.md`). ——— Prior: Phase 7 ④a **PR4 rule lock MERGED + DEPLOYED (#125)** — the live **High** self-grant vuln is CLOSED: `subscriptions/$uid` `.write` → admin-only + child `.validate:false` on `features`/`limits`; the resolver (Admin SDK) is the sole writer. 6 admin tier writers (incl. a legacy `admin_tools/*.html` the security-reviewer caught) routed through `entitlementSetTier` (relaxed superAdmin→admin, now dual claim+RTDB verified); signup Path-B fallback dropped; 2 owner-writers (`user-subscription.js`, `subscription-tier-fix.js`) made read-only; `locationIds` writers confirmed DEAD. **Corrected the merged plan's buggy §2 rule shape** (parent `!newData.hasChild` is wrong for multi-path `update()` → used child `.validate:false`). security-reviewer APPROVED the vuln closure; all review findings folded. 49 tests + build green. **⚠ DEPLOY STILL GATED for operator — strict order CFs→hosting→emulator-verify→rules(LAST)→backfill sweep→signup smoke.** Remaining ④a tail: ROSS `maxWorkflows` cap (PR5, spec §8) + the post-lock backfill. Next major = ② askRoss Agent build. Also this session: recovered an orphaned never-merged OWASP audit report via #124 (`docs/security/owasp-audit-2026-05-30.html`) + pruned 100 merged remote branches → 2 + removed stale worktrees. ——— Prior: 2026-06-01 (Session bank — ④a writer-consolidation merged (#120) AND **deployed to prod** (registerUser/createUserAccount/checkSubscriptionStatuses + hosting); reflects #121, and the rule-lock plan merged (#122) with Q1–Q3 settled + review fold-ins. Next = the rule-lock build itself (closes the still-live High self-grant vuln) — NOT started, recommended as a fresh focused session. Earlier today: #118 (catalog the slipped #117 🔴) + #119 reflect. ——— Prior: PR #120 — Phase 7 ④a writer-consolidation (PR3, additive half) merged: server writers route features/limits through the resolver, the 3 dead client writers stripped, the client fake-charge free-upgrade replaced with a /upgrade.html redirect. #120 review handed over the full rule-lock client-writer audit list (admin writers at subscription-status-manager.js:352,468 + enhanced-user-subscription-manager.js:866,1408, the signup Path-B fallback, the locationIds user writes). ——— Prior: PR #118 — resolved the open #117 review findings, caught by re-reading the #113–#117 automated-review comments at session start: catalogued the 5 entitlement CFs in both `CLOUD_FUNCTIONS_CATALOG.md` copies [🔴 was merged unactioned], switched `entitlementGetEffective` to `authError()` for the 403 path [🟡], documented the intentional any-admin-read policy. Also folded the two #114 doc NITs [SELF_OPT stray separator row + Medium-priority duplicate numbering]. ——— Prior (2026-05-31): Phase 7 askRoss entered design — decomposed into a 4-component program: ① Credit Ledger → ④a Entitlement resolver + add-on layer → ② askRoss Agent, with ③ Payment Rail + ④b tier 4→2 migration sequenced after. Two spec docs written: `docs/plans/2026-05-31-metering-credit-ledger-design.md` and `docs/plans/2026-05-31-entitlements-addon-layer-design.md`. The entitlements design audit surfaced a NEW **High** vuln — entitlement self-grant via client-writable `subscriptions/{uid}/features`+`limits` (`database.rules.json:28`), logged in the Bug Triage Queue, fix scoped into ④a. ——— Prior: OWASP remediation COMPLETE through all Highs + most Mediums/Lows — 14 PRs #89–#102, all merged. After #89–#91: **#93** H-4/H-7/H-10 (CSPRNG passwords, JWT-header + WhatsApp-PII logging); **#94** H-5/H-6 (ross.js CORS via new shared `functions/cors-allowlist.js` + setAdminClaim CORS wrap); **#95** H-3 (stale inline firebaseConfig removed) + H-4b (voucher CSPRNG); **#96** H-2 Bucket A/B (locked `users`/`subscriptions`/`salesData`/`salesDataIndex`/`forecasts`/`forecastIndex` root reads to admin + per-child owner/userLocations rules) + design doc; **#97** H-9 (`npm audit fix` lockfile-only — all crit+high cleared, 8 moderate `uuid` chain deferred to firebase-admin@13); **#98** M-6/M-10/L-1/L-2; **#99** M-4 stored XSS (escapeHtml+safeUrl); **#100** M-1 (location ownerId .validate); **#101** M-2 (X-Frame-Options/nosniff/HSTS/Referrer-Policy); **#102** M-8 POPIA (deleteUserData → wifiLogins/whatsapp-history/bookings/activeUsers/queue + phoneNumber indexes). Every PR got an automated review; two real must-fixes surfaced and were fixed pre-merge — #96 missing `ownerId` fallback on index rules, #102 missing `.indexOn:[phoneNumber]`. **KEY LESSON:** RTDB read-rule *cascade* + queries auth''d at the queried node ⇒ you cannot rule-filter a root location-query, so root-read tightening needs consumer-mapping first (why guests/queue = H-2 Bucket C, deferred). Also: an automated review can compute its diff against a stale `origin/master` (#98 phantom "17 files") — verify the real diff before acting. **Deferred (each own task):** H-2 Bucket C (guests/queue/receipts query refactor — architecture call needed); M-3 SRI; M-5/M-7/L-3 (Bucket-C-class root reads); M-9 MFA; L-4 password policy; CSP; firebase-admin@13. **Deploy prereqs pending:** secrets:set + rotate leaked Meraki/admin secrets; GCP referrer-restrict Places key; emulator smoke on #96/#100/#102. ——— Prior (2026-05-30): OWASP Top-10 audit remediation — operator-supplied audit HTML, mid-session pivot from sprint. Three PRs merged: **#89** (C-1 operator-precedence admin-bypass across 12 CFs → `admin !== true`; C-2 hardcoded secrets `Giulietta!16` + `MerakiAdmin2024!` → `defineSecret` Secrets Manager + `INITIAL_SETUP_ENABLED` gate; C-4 `receiptPatternLogs`/`debug` RTDB `.write: true` → admin-only); **#90** (C-5 SSRF allowlist in `detectReceiptText` covering both callers + bypassable `.includes('twilio.com')` → `new URL().hostname` parse; H-8 Storage `/receipts/` `allow write: if true`→`if false`); **#91** (C-3 `getGoogleConfig` → admin-gated via new `requireAdmin` helper, caller `googleReviews.js` sends token via `onAuthStateChanged`; H-1 four queue mutators admin-gated). **5/5 Criticals + 3 Highs (H-1,H-7-partial,H-8) closed.** All three reviewed (security-review on #89, automated review on #90/#91); #91 had a real 🔴 must-fix — token-only auth on a billable-key endpoint still leaks to non-admin operators (auth≠authz). Remaining audit findings → Bug Triage Queue. Two operator decisions open: GCP referrer-restriction on Places key; `getQueueStatus` auth posture. **Deploy steps required:** `firebase functions:secrets:set MERAKI_SHARED_SECRET INITIAL_ADMIN_SETUP_SECRET` + rotate both leaked values + update Meraki dashboard secret. ——— Prior (2026-05-20): post PR #86 + PR #87 cleanup-mode reflect — both merged same evening. PR #86: derive Missed/Overdue counters CLIENT-SIDE from nextDueDate in playbook-store + activity-store, fixing the operator-reported PR #72 inconsistency where home card warned of Missed workflows while tabs said "Overdue: 0". Root cause: server's `locData.status` field is set to 'active' on activation and NEVER updated to 'overdue' — `w.status === 'overdue'` filter always returned 0. New `workflow-status.js` shared helper (MISSED_DAYS_THRESHOLD + computeDaysLate + isOverdue + isMissed) consumed by both stores AND detectors.js — single source of truth across three surfaces. RossPlaybook/RossActivity stat tiles now use dynamic Missed/Overdue label. 25 new vitest cases. PR #87: catalog all 22 ROSS CFs + allowed-fields per mutator in ROSS.md, plus 8-category summary in CLOUD_FUNCTIONS_CATALOG.md with field-verify rule footnote. Automated review caught a forward-reference: PR #87 cited PR #86's workflow-status.js while #86 was still open — fixed via "(ships with PR #86)" qualifier, then switched to past tense after #86 merged + #87 rebased. Lesson: forward-references between in-flight PRs need a merge-order-tolerant qualifier OR the PRs need stacking. Earlier same day: PRs #81/#82/#83/#84/#85 all shipped.)

---

## 🎯 Sprint Goal

**Effective implementation of Hi-Fi v2, with ROSS as the central post-login surface for every subscribed user.** Workflows are the product; v1 modules become workflow step types over time.

**Mental model (locked 2026-04-30, refined 2026-05-02):** ROSS v1 isn't operator-facing compliance CRUD; it's the **policy + playbook the future AI agent runs against**. The 6 sibling v1 admin tabs collapse to 3 deeper governance destinations behind the concierge home (**Playbook / Activity / People**). The **playbook is the product** — workflows direct every operation; modules become step types inside workflows, not destinations. **Two tiers only**: Free (full ROSS UI + 5 starter templates) and All-in (full template library). No POS integrations yet — manual entry / file uploads only. LLM integration (`askRoss`) is now central, not deferred — own sprint.

Sprint: 2026-04-30 → until complete

---

## 🔄 In Progress

_Nothing in flight as of 2026-06-07 — this session's 4 PRs (#147–#150) all merged; see Recently Completed. Historical context retained in the table below._

| Item | Branch | Notes |
|------|--------|-------|
| Phase 7 askRoss — **② Agent build COMPLETE** | merged (#130/#132/#134/#136/#138/#139/#142 + #140 fix + #144 security) | **② Agent build DONE — all 7 slices + the #140 schema fix + #144 cross-tenant security merged & live; #142 eval harness merged. askRoss answers in prod, is eval-covered + security-audited. Remaining: operator live `npm run eval`; ③ Payment Rail + ④b tier migration deferred.** _Historical:_ **② Agent build — slices 1+2+3+4+7 SHIPPED; slice 5 client PR open (#139); NEXT = slice 6 evals.** **Slice 4 confirm-flow MERGED (#136)** — the agent authors the playbook with the owner in the loop: a `confirm`-tier tool PAUSES → persists `ross/agentPending/{uid}/{turnId}` (10-min expiry, uid-scoped) + debits Request A + emits a confirm card → resume (`{resumeTurnId, decision}`) re-runs gates (kill-switch holds), fail-closed expiry, atomic one-time-consume, executes (audited `confirmed:<uid>`) or declines, continues (Request B). The 4 confirm tools (activate/create/edit/pause workflow) promoted to READY via owner-callable cores in `ross.js` shared with the live CFs. 166/166 tests; security + automated review, all folded. **⚠ rossChat is LIVE — redeploy needed.** **NEXT recommended = slice 7 `ross/agent*` RTDB rules + prune** (`agentChats`/`agentAudit`/`agentConfig`/`agentPending`: server-only writes, CF-mediated reads) — the security review flagged it a **launch-blocker BEFORE slice 5 (the Ask Ross client)**. Then slice 5 client (must `escapeHtml` the confirm-card), slice 6 evals. **Slice 3 (#134):** 4 pre-flight gates → cached prompt + bounded history → `runAgentLoop` → idempotent ledger debit → SSE; `verifyRossAgentAccess`. **② Agent build STARTED**: spec revised (#129) + **core slice 2 (#130)** + **llm-client slice 1 (#132)** SHIPPED. Slice 2: `functions/agent/` tools/policy/prompt/execute (one Zod registry → two engine projections, `effectivePolicy`+`autoAllowlist`, mode-aware cached prompt, audit-wrapped `executeTool`, §3.1 gate; 4 ready + 12 pending tools). Slice 1: `llm-client.js` (`streamTurn`/`createTurn`/`toLedgerUnits`/`MODELS` + lazy client seam; `@anthropic-ai/sdk`). 48 tests. **Remaining slices:** 3 `rossChat` SSE, 4 confirm-flow, 5 Ask Ross client, 6 evals, 7 `ross/agent*` rules. **① Credit Ledger LIVE** (#113). **④a Entitlements COMPLETE + DEPLOYED** — resolver+CFs (#116/#118), writer consolidation (#120), rule lock (#125, self-grant vuln closed), and the `maxWorkflows` cap (#127, Free=5 live; first limit enforced). The resolver is the sole writer of materialized features/limits; `subscriptions/$uid` `.write` admin-only. **Next = ② askRoss Agent — slice 3** (`rossChat` SSE CF: pre-flight gates → agent loop → ledger debit → SSE + `defineSecret` + CORS + `rossAgent`-aware auth; spec `docs/plans/2026-05-31-askross-agent-design.md`; §3.1 measurement-gates `submitResponse`). Memory `project_phase7_askross_agent` + `project_phase7_program`. ③ Payment Rail + ④b tier 4→2 migration still deferred. ——— _historical ④a detail:_ **① Credit Ledger LIVE in prod** (#113). **④a Entitlements: resolver core + 5 CFs (#116)** + catalog/error fold-ins (#118) + **writer consolidation (#120, deployed)** + **RULE LOCK MERGED (#125 — closes the High self-grant vuln, code-complete).** #125: `subscriptions/$uid` `.write` → admin-only + child `.validate:false` on `features`/`limits` (resolver = sole writer); 6 admin tier writers routed via `entitlementSetTier` (relaxed superAdmin→admin, dual claim+RTDB verified); signup Path-B dropped; `user-subscription.js`/`subscription-tier-fix.js` made read-only; `locationIds` writers confirmed DEAD. Corrected the plan's buggy parent-`hasChild` rule shape → child `.validate:false`. security-reviewer APPROVED; all findings folded. **⚠ DEPLOY GATED for operator — strict CFs→hosting→emulator-verify(deep-path `.validate`)→rules(LAST)→backfill sweep→signup smoke.** **④a remaining tail:** ROSS `maxWorkflows` cap (**PR5**, spec §8) + the post-lock `recomputeEntitlements` backfill. **Next major = ② askRoss Agent build** (`llm-client.js` + tool registry + policy engine + `rossChat` SSE + Ask Ross client + 20-prompt eval harness; spec `docs/plans/2026-05-31-askross-agent-design.md`). Memory `project_phase7_program`. ③ Payment Rail + ④b tier migration still deferred. |

---

## ✅ Sprint Tasks

### Phase 0–3 — Done

- [x] **Phase 0** — Project Status dashboard + sidebar cleanup (PR #16) + CORS fix for preview channels (PR #17)
- [x] **Phase 1** — Gap analysis & spec doc (`docs/plans/2026-04-30-ross-v2-implementation.md`, PR #18)
- [x] **Phase 2** — Right-rail + first-run findings wired (PR #19) — 1 real finding + 2 illustrative
- [x] **Phase 3** — Action handlers + snooze CF deployed (PR #20) — `rossV2Snooze` + `ross/v2Snoozes/{uid}` RTDB node + read-side filter

### Phase 4 — Admin redesign (concierge-first IA) — In progress

- [x] **IA reframe** — v1 = agent's playbook; concierge home stays front door; tabs collapse 6 → 3
- [x] **Phase 4a** — Playbook tab read-view (PR #21) + locationName enrichment fix (PR #24)
- [x] **Phase 4b** — Activity tab (run history + reports) (PR #23)
- [x] **Phase 4c** — People tab — staff CRUD (PR #25). First edit-capable v2 surface; established inline-editor + two-step inline delete + inline error banner patterns + client-side phone normalization.
- [x] **Phase 4d.1** — Playbook tab — workflow create / edit / pause / delete + activate-from-template (PR #28). First edit-capable v2 surface for the workflow data path. Slide-down inline delete confirm. Server's allowedFields limit on `rossUpdateWorkflow` surfaced via locked-field UX.
- [x] **Phase 4d.2** — Template CRUD (superAdmin) (PR #30). Inline `RossPlaybookTemplateEditor` mirrors workflow editor; slide-down delete confirm on cards; `admins/{uid}.superAdmin` probe gates UI (server still enforces `verifySuperAdmin`). Same PR fixed KB doc drift (`templateId`/`subtasks`) and follow-up commit added stable subtask `_uid` keys + empty-`daysBeforeAlert` validator in both editors.
- [x] **Phase 4e.1** — Per-task `inputType` / `inputConfig` editor on existing workflow tasks (PR #32). New `RossPlaybookWorkflowTasksEditor` + `RossPlaybookTaskRow` + `RossPlaybookTaskConfigFields` reachable from each card's "Edit tasks" button. All 10 server input types supported via `constants/input-types.js`. Same PR fixed a latent v2 bug: `playbook-service.getPlaybookWorkflows()` now flattens client-side to one row per (workflowId, locationId), matching what the v2 surface always assumed. Follow-up commit addressed 4 of 5 review findings (alert icon, dead code, `taskSavingTaskId` symmetry, raw-rating-input through validator).
- [x] **Phase 4e.2** — Server propagation + template-level inputType editor (PR #35). Single `buildTaskFromSubtask(subtask, nextDueDate)` helper shared by `rossCreateWorkflow` + `rossActivateWorkflow` keeps create-from-scratch and activate-from-template byte-identical. `validateSubtasksInputTypes` rejects invalid `inputType` enum values upstream at all 4 write paths (`rossCreateWorkflow`, `rossCreateTemplate`, `rossUpdateTemplate`, `rossManageTask`) — closing the validation hole I caught during plan-review (planner had only proposed it for `rossCreateWorkflow`). Subtask row UI gains always-visible type select + collapsed config sub-form, reusing `RossPlaybookTaskConfigFields` from 4e.1. Functions deployed pre-merge to avoid client/server protocol gap.

### Phase 5 — ROSS-as-central-funnel cleanup (LOCKED SPEC 2026-05-02)

Spec: `docs/superpowers/specs/2026-05-02-ross-central-funnel-cleanup-design.md`. Five PRs, sequenced as below. Replaces the original "Phase 5 = onboarding wiring" framing — that was too narrow. The actual work is making ROSS the post-login destination for the first time (`ROSS_IS_HOME` flag has been unread since shipped) and Hi-Fi-rewriting the three v1 surfaces (homepage, signup, wizard) that sit between a visitor and ROSS.

- [x] Audit confirms `getFirstRunFindings()` → store → component is end-to-end functional (PR #22)
- [x] Spec locked + merged (PR #37, 2026-05-02)
- [x] **PR 1 (PR #39)** — `feat/post-login-router`: pure router + 19 tests at 100% coverage; hello auth gate; `helloSeen` field shipped end-to-end (signup init + hello CTA write + wizard `set→update` fix to preserve it + KB doc + matrix backwards-compat). Toast-timing fix landed via deferred-navigator pattern after review caught the `Promise.all` side-effect race.
- [x] **PR 2 (PR #42)** — `feat/signup-v2-hifi`: Hi-Fi Vue 3 rewrite of `/signup.html` (no Pinia, ephemeral form state). Dynamic-tier reframe (operator pivot — admin curates Free/All-in via existing admin-dashboard Tier Management UI rather than hardcoding in code constants). Service layer extracted to `signup-service.js`. `tier` written to `users/{uid}/tier`, `subscriptions/{uid}/tier` (canonical) AND `subscriptions/{uid}/tierId` (legacy compat). `registerUser` CF gap closed: server-side `subscriptionTiers/{tierId}` validation, length-bounds on free-text fields, atomic multi-path `update()` for users + subscriptions + onboarding-progress, idempotent onboarding-progress init. New `.validate` rule on `subscriptions/$uid` rejects unknown tier IDs (defense in depth). Two new Hi-Fi components shipped: `HfSelect` (rebuilt as true custom combobox after operator flagged native dropdown panel ignored design tokens — full WAI-ARIA + keyboard) and `HfCheckbox`. v2 surface uses inline error/success banners (not SweetAlert2 — that util silently no-ops on the Hi-Fi mount shell). Doc housekeeping: 3 plan stragglers relocated `docs/superpowers/plans/` → `docs/plans/` after operator clarified the canonical location; LESSON corrected.
- [x] **PR 3 (PR #44)** — `feat/homepage-v2-hifi`: `/index.html` itself promoted to Hi-Fi mount shell (legacy v1 + index-v2.html chrome stepping-stone + landing-page.css all deleted). Workflow-centric narrative replaces fabricated stats/testimonials/success stories — three concrete workflow cards (Daily Opening Checklist / Weekly Compliance Sweep / Monthly Marketing Push), single primary CTA "Start free", honest tone throughout. Synthetic public hello preview embeds the actual `RossOnboardingHello.vue` (Q4 lock — same component, two data feeds) with a Tannie's-Kitchen Cape Town narrative; CTAs route to `/signup.html`. RossOnboardingHello refactored with three optional props (`findings` / `continueHref` / `tourHref`) — defaults preserve post-signup behavior byte-for-byte. Pricing section reads `subscriptionTiers` via new shared `services/subscription-tiers.js` (extracted from PR 2's signup-service); static Free/All-in fallback if RTDB empty. Founder Lakis Katsouris quote preserved in single-card layout. Hero photo (`/img/ob-bg.jpg`) replaced with typographic workflow-card panel demonstrating the product in miniature. Module rename `landing-v2/` → `marketing/landing/`. SEO preserved (`<title>`, meta description, `#features`/`#about`/`#testimonials` alias). Three review fixes landed in same PR: logo `smoothScroll` always preventDefault, All-in fallback gets "Pricing announced soon" subtext, magic `top:-80px` promoted to `--lp-nav-height` custom property.
- [x] **PR 4 + PR 6 (folded) (PR #46)** — `feat/post-login-router-rollout`: wired the 5 remaining redirect call sites (`user-login.js` ×2, `onboarding-wizard.js` ×2, `user-dashboard.js`, `dashboard.store.js`) to the router; `user-dashboard.html` got a sticky deprecation banner. Review surfaced a real bug — fresh signups (`completed: false && helloSeen: false`) hit the wizard because the router's `!completed` short-circuit fired before the helloSeen branch. Fix folded the PR 6 work in: `resolvePostLoginDestination` rewritten with three explicit helloSeen states — `false` → hello, `true` → home (wizard skipped, PR 6 lock), missing → legacy `!completed`-gated path. Hello component default `continueHref` updated `/onboarding-wizard.html` → `/ross.html`. AUTHENTICATION_FLOW.md gained a Post-Login Routing section. 21/21 router tests pass (was 19 — added fresh-signup + PR 6 cases). The wizard remains reachable by direct URL for legacy use; only fresh signups skip it. **ROSS is now home.**
- [x] **PR 5 (PR #48)** — `feat/ross-sidebar-cleanup`: collapsed sidebar to Today + Ross's brain only (4 sections / 13 items → 2 sections / 4 items). All v1 module deep-links removed from ROSS primary nav (Overview / Queue / Guests / Operations all gone). Mobile bottom nav folded in (Overview / Guests / Queue / You → Playbook / Activity / People). v1 modules remain reachable via `/admin-dashboard.html` per Q3 lock. Operator caught on preview that the footer Profile/Settings block shipped hardcoded prototype data (`Maya Alvarez` / `Group Ops` / `MA`) — my original plan said "footer already serves the role" but conflated structure with content. Fix landed same PR: both desktop footer + mobile topbar now read `auth.currentUser` reactively via `onAuthStateChanged`, with proper unsubscribe in `onUnmounted`. Display shows `displayName` (or email local-part fallback) + email + live initials. Gear icon stays non-interactive — `/profile-settings.html` doesn't exist yet (only `/receipt-settings.html` which is per-restaurant config, different domain). **Phase 5 sprint closed.**

Lower-priority Phase 5 items (deferred to dedicated polish PR after the 5-PR sequence): surface `store.error` in `RossOnboardingHello.vue`; lower `FINDING_MIN_LIFT` threshold; parallelise nested RTDB reads in `detectBestWeekday`; un-hardcode the 3-of-5 step counter.

### Phase 6 — Playbook UX enrichment + template library curation (next sprint after Phase 5 cleanup)

> **Vocabulary anchor (per `public/kb/features/ROSS.md`):** Templates → activated → Workflows (per location) → composed of Tasks → executed via Runs. The Runs server surface (`rossCreateRun`, `rossSubmitResponse`, auto-flag, 422-on-required-note) is already shipped — Phase 6 is the operator-facing UX layer on top, plus content (template curation + tier gating).

- [x] **Tier gating mechanism** (PR #51, 2026-05-12) — `tier:'free'|'all-in'` schema, server filter on `rossGetTemplates`, activate gate on `rossActivateWorkflow` with audit log, symmetric validators on Create/Update Template, editor field, defensive client filter, KB docs. Mechanism complete; gate is inert until PR 1B flips templates.
- [x] **Curate starter library** (PR #53, 2026-05-12) — 5 Free (Daily Opening / Daily Closing / Weekly Deep Clean / Monthly Food Cost Review / Health & Safety Audit) + 8 All-in (CoA / Liquor Licence / Weekly Social / Monthly Google Reviews / Weekly Supplier Pay / Monthly Staff Meeting / Quarterly Performance Review / Monthly Equipment Service). Seed file updated for fresh deploys; one-off update script (`functions/seeds/ross-templates-curate-tiers.js`) ready for prod RTDB. Folded a sibling fix on PR #51's backfill script (legacy databaseURL → canonical `-default-rtdb` form).
- [x] **Tier-gated template list (upgrade affordance)** (PR #55, 2026-05-12) — server's `filterTemplatesByTier` now accepts opt-in `includeLocked:true` (v1 callers untouched); v2 Playbook renders locked All-in templates dimmed + "All-in" badge + ghost "Upgrade to All-in" button → `/upgrade.html`. New Hi-Fi comparison page reads `subscriptionTiers/` via `loadTiers()` with email CTA pre-filled by `?from=template&id=<id>` context. 8 new vitest cases (34/34 passing). 5+ review fixes folded mid-PR including a security finding (CRLF in mailto body via `?id=` query param)
- [x] **Day-zero auto-activation** (PR #58, 2026-05-12) — new `rossSeedFirstWorkflow` CF called from `signup-service.js` after both signup paths (callable + fallback) complete. Server resolves locationId from `userLocations/{uid}` and templateId from `ross/config/firstWorkflowTemplateId` pointer (set per-environment via one-off script). Idempotency marker on `onboarding-progress/{uid}/firstWorkflowSeededAt`; client wraps the call in `Promise.race` with a 1500ms cap so a cold-start CF can't block the post-signup redirect. Also: pure helpers `buildLocationsFromTemplate` + `buildWorkflowRecord` extracted from `rossActivateWorkflow` (17 unit tests). Originally inlined the seed inside `registerUser` — preview testing revealed `registerUser` callable has been throwing `unauthenticated` on every call due to a pre-existing v1-vs-v2 signature bug, and `signup-service.js` silently falls back to direct RTDB writes. Architecture pivot mid-PR to the dedicated CF.
- [x] **Operator-facing Run execution UI** (PR #65, 2026-05-15) — `?tab=run` page on the concierge shell. Polymorphic `RossRunTaskInput` covers all 10 inputTypes; auto-save on commit; client pre-flight `requiredNote` flow; server-422 safety-net wired via `store.pendingNoteTaskId` + card watcher; inline completion banner. Spec: `docs/plans/2026-05-13-ross-run-execution-ui-design.html`. Plan: `docs/plans/2026-05-13-pr3-ross-run-execution-ui-plan.md`. Subagent-driven dev across 16 tasks; 40 unit tests across 4 new suites (was 36; +4 from pre-merge review fold-in). Five preview-smoke fix-ups + pre-merge review fold-in landed (6 of 7 review items: must-fix 422 wiring, dead-mock cleanup, 4 NITs). Known limit: optimistic `onTime: true` on completion until first `getRun` refresh — documented in code.
- [x] **Concierge home active-run surfacing** (PR #72, 2026-05-19) — new `rossGetHomeWorkflowDigest` CF (read-only, `verifyUserOrAdmin`, reads `ross/workflows/{uid}` + `ross/runs/{uid}` in parallel, returns structured digest with `overdue`/`today`/`recentCompletions`/`upcoming`/`hasActiveWorkflows`/`activeWorkflowCount`). Thin client detector `detectActiveWorkflows` picks priority (overdue → in-progress → due-today → recently-completed → all-clear) and builds the slot-1 card. Hybrid card mix preserved — food-cost / VIP / revenue detectors keep slots 2-3. `LEARNING_MODE_WORKFLOW_CARD` fallback for users with zero active workflows. Two preview-driven fixes folded in: (a) `_normalizeNextDueDate` accepts number/ISO/full-timestamp shapes (existing workflows store nextDueDate as epoch ms NUMBER per ross.js:703/1150/1282/1502; my helper compared as string → all branches silently failed); (b) canonical run schema — runs have NO `status` field, lifecycle is `completedAt === null` vs non-null; runId field is `id` not `runId`; onTime/flaggedCount derived from responses+nextDueDate (server writes them only to history records). Mid-PR operator design call: "Missed" chip framing at `daysLate >= 4` — flips chip label, headline verb, aggSuffix wording, footnote, sidecar target copy. Home feed auto-reload on every mount (Pinia store survives in-app tab switches → stale data without explicit reload). 38 unit tests, build green, deployed.
- [x] (Stretch) **Compliance Sweep** as the first end-to-end curated template (PR #83, 2026-05-20) — 5 subtasks × 4 input types (2× temperature with auto-flag + requiredNote, yes_no, checkbox, text), `tier: 'free'`, weekly recurrence. Seed file + idempotent prod-additive script (`ross-templates-add-compliance-sweep.js`). No CF/client changes — the existing `rossActivateWorkflow` + `rossCreateRun` paths already honour `inputType`/`inputConfig` on subtasks per Phase 4e.2 / PR #65.

### Phase 7 — askRoss LLM (active)

Decomposed during the 2026-05-31 brainstorm from a single CF into a **4-component
program** (billing turned out to be shared platform infra, not an askRoss feature).
North star: *Ross runs the paperwork, not the restaurant.* **Design phase COMPLETE —
3 specs merged.** Build order: ① → ④a → ②.

**Design (done):**
- [x] **① Credit Ledger** spec — prepaid-ZAR metering modelled on Anthropic's prepaid wallet (`docs/plans/2026-05-31-metering-credit-ledger-design.md`, PR #106/#108)
- [x] **④a Entitlement resolver + add-on layer** spec — closes the live self-grant vuln (`docs/plans/2026-05-31-entitlements-addon-layer-design.md`, PR #107)
- [x] **② askRoss Agent** spec — tool-use agent, bands A+B, graduated-risk policy engine (`docs/plans/2026-05-31-askross-agent-design.md`, PR #110)

**Build (next, in dependency order):**
- [x] **① Credit Ledger build** (PR #113) — `functions/billing/` ledger.js + 3 CFs + server-only rules + seed; 27 TDD tests, code-reviewed. **Deploy pending** (npm install in worktree, deploy functions:billing* + database together, run price-table seed).
- [x] **④a Entitlements build** — resolver core + 5 CFs (#116), writer consolidation (#120, deployed), **rule lock (#125 — closes the High self-grant vuln, code-complete)**. Resolver is the sole writer of `features`/`limits`; `subscriptions/$uid` `.write` admin-only + child `.validate:false`. **⚠ deploy GATED (strict CFs→hosting→emulator→rules→backfill→signup-smoke).** Remaining tail: ROSS `maxWorkflows` cap (PR5) + post-lock backfill sweep.
- [x] **② askRoss Agent build — COMPLETE (slices 1–7 + #140 fix + #144 security all merged + live)** — _spec REVISED (#129: §1.1 engine-agnostic core + §2.1 per-engine billing). **Slice 2 SHIPPED (#130)** — `functions/agent/` engine-agnostic core: Zod tool registry → `toAnthropicTools`/`toSdkMcpServer` projections, `effectivePolicy`+`autoAllowlist`, mode-aware cached `systemBlocks`, audit-wrapped `executeTool`, `isAgentSubmittable` §3.1 gate; 4 ready + 12 pending tools. **Slice 1 SHIPPED (#132)** — `llm-client.js`: `streamTurn`/`createTurn`/`toLedgerUnits` (snake→camel mapper, #129 §2.1)/`MODELS` + lazy/injectable client seam over the raw Anthropic Messages API; `@anthropic-ai/sdk`. 48 tests. **Slice 3 SHIPPED (#134)** — `functions/agent/rossChat.js` reactive SSE CF: 4 pre-flight gates → cached prompt + bounded history → `runAgentLoop` (auto tools via `executeTool`, non-auto refused server-side) → single idempotent `recordUsageAndDebit(toLedgerUnits(usage), requestId)` → persist + SSE; `defineSecret('ANTHROPIC_API_KEY')` + CORS + new `verifyRossAgentAccess`. 139 tests; two review rounds (no blockers). ⚠ deploy secret-first; server-only._ **Slice 4 SHIPPED (#136)** — confirm-flow pause/resume + the 4 confirm-tier tools promoted to READY (extract-and-share cores in `ross.js`). Pause persists `ross/agentPending` (uid-scoped, 10-min expiry) + debits Request A + emits a confirm card; resume re-runs gates + fail-closed expiry + atomic one-time-consume + execute(`confirmed:<uid>`)/decline + continue (Request B). 166 tests; security + automated review folded. **Slice 7 SHIPPED (#138)** — `ross/agent*` RTDB rules (server-only writes, CF-mediated reads) + `rossAgentPrune` scheduled CF (daily 03:30; prunes expired `agentPending` + stale `debitGuard`). **Slice 5 MERGED + LIVE (#139)** — Ask Ross ⌘K client: streams `rossChat` SSE into a multi-turn modal, inline confirm-cards, terminal/error banners; desktop-only (mobile→3a); confirmed answering live in prod (after the #140 schema fix). **Slice 6 MERGED (#142)** — the live eval harness (`npm run eval`): 21 cases grading Ross's judgment against the real API with a seeded two-tenant RTDB + Haiku judge + a cross-tenant probe; machinery unit-tested in CI, run is live/manual; 196/196 green. **#144 (HIGH security)** — closed a cross-tenant read in `getRunHistory`. _**The askRoss agent is built end-to-end.** Remaining: operator live-eval smoke; slice-6 follow-ups (harvester/nightly/judge-tool-results) + #144 shared-helper → 3a. Next major = ③ Payment Rail + ④b tier migration (still deferred)._
  - **Slice-3 dependencies (deferred from #130, log here):** (a) extract read-logic helpers from `ross.js` for `getWorkflows`/`getReports` (currently inlined → those 2 agent tools are `pending`); (b) build food-cost / guests / sales summary reader CFs (no reader CF/schema exists yet → 3 agent tools `pending`); (c) `advanceDueDate` recurrence-advance logic (overlaps the recurrence-engine bug above — fix the engine, then the tool wraps it).

**Deferred (each own spec later):** ③ Payment Rail (gateway/top-ups — external long-poles), ④b tier 4→2 collapse migration, web-sourced day-zero findings (weather/events/holidays — once the agent is live).

---

## 📋 Backlog (Prioritized)

### High Priority

1. **Food cost open PR** — review and merge the in-flight food-cost feature branch
2. **Chart.js retirement** — soak ends ~2026-05-07; execute removal per `KNOWLEDGE BASE/development/CHARTJS_REMOVAL_AUDIT.md` (16 of 23 sites have v2 replacements ready)
3. **ROSS obligation templates** — implement per `docs/plans/2026-03-20-obligation-templates-plan.md`

### Medium Priority

3aa. **Eval harness — tuning follow-up from the first live run (HIGH, fresh session)** — **[✅ Group A DONE — #149, 2026-06-07: realistic 2026 fixture dates (no more epoch-1970 "20,609 days overdue"), `toolResults` capture seam, judge grounding via synthetic `__systemContext__`. Operator must re-run `npm run eval` (needs the key) to confirm the expected ~15→18/21. ⏳ Group B REMAINS — `q-staff`/`q-staff-runs`/`snooze` left failing on purpose: a real prompt-engineering signal (Ross isn't reaching for read/action tools) needing operator product input.]** — first `npm run eval` (2026-06-05) = **15/21**; the 6 fails triage into harness/fixture bugs vs one real product signal. Full plan: **`docs/plans/2026-06-05-askross-eval-tuning-followup.md`**. TL;DR — **Group A (3 false-fails, fix the harness):** fixture seeds `nextDueDate: 1` (epoch 1970 → "20,609 days overdue"); the judge is blind to the system-prompt digest (`buildSystemForOwner` preloads it → Ross correctly skips `getWorkflowDigest`, judge false-flags "fabrication"); `toolResults` never populated. Fix: realistic fixture dates + capture tool outputs into the transcript (or feed the judge the system context) + drop the preloaded-tool expectations. **Group B (real signal, do NOT loosen):** Ross isn't calling `getStaff`/`snoozeCard` and confabulates instead — under temp 0 + current prompt it answers/excuses rather than reaching for read+action tools → a prompt-engineering opportunity for the real product. Re-run after; residual should be genuine model/prompt signals needing operator input. Guardrail: never make the eval green by loosening assertions.

3a. **Ask Ross client — slice 5 follow-ups (mobile + polish)** — **[✅ client-polish DONE — #148, 2026-06-07: (a) mobile pill wired to the real modal (was decorative debris), (b) fetch cancellation — `abort()` on close/new-chat + reset-on-close so late events can't fold in, (c) ⌘K cold-start hoist (modal moved outside the `.ross-home` v-if), (d) reset keys (`${resetCount}-${i}`) + a "New chat" affordance; 24 tests. ⏳ REMAINS under 3a: the #144 shared agent location-access helper + slice-6 follow-ups (harvester from real `agentChats`, nightly cron, judge tool-results).]** — slice 5 shipped the **desktop** ⌘K Ask Ross modal (`RossAskModal.vue` consuming `rossChat` SSE). Deferred, each cheap: (a) **mobile pill wiring** — wire `RossHomeMobile.vue`'s "Ask Ross anything…" pill to open the same modal/surface; verify the inline confirm-card layout at the mobile breakpoint (watch the prototype-debris-on-mobile lesson — the mobile variant historically didn't inherit desktop data-wiring); (b) **fetch cancellation** — `ross-agent-client.js` creates an `AbortController` but never surfaces it; ESC/scrim close mid-stream doesn't abort the in-flight `fetch`, and late `onEvent`s still fold into state on reopen (modal isn't reset on close). Wire `abort()` + reset-on-close; (c) **⌘K during cold-start** — the modal mounts inside the `.ross-home` v-if, so the ⌘K listener is dead until home content loads; hoist the modal outside the loading-gated subtree to make the palette available immediately; (d) **conversation reset / index keys** — when a "clear conversation" / "new chat" affordance is added, switch `RossAskModal.vue`'s turn `v-for` off index `:key` (append-only today, safe; would cause stale-VDOM on reset) — review N-1. Source: slice-5 per-task + final review + #139 automated review (2026-06-04). _(Note: deep-link hash-clear [M-3], silent-EOF busy-hang guard [M-2], and SA cost formatting [N-2] were folded into #139 from the automated review.)_

3c. **Guard-backfill program — promote unguarded shipped-defect lessons into mechanical guards** _(Step 11 guard-first flip ACCEPTED + LIVE, PR #143 merged 2026-06-05)_ — **[✅ Seed item (4) CF auth-posture guards DONE — #150, 2026-06-07: source-scan tests assert `requireAdmin`/`verifyAdmin`/`verifySuperAdmin` dual-factor structure + per-CF call-site presence across 8 admin-only CFs (20 tests); no security holes found. Seed item (3) prototype-debris denylist DROPPED as entangled with intentional illustrative content (honest scope call). Seed item (2) `vue/no-v-html` now UNBLOCKED — #147 closed the live `RossHomeDesktop.vue:203` instance. REMAINING: (1) scoped CI gate, (2) vue/no-v-html rule, (5) rules-unit-testing harness, (6) shared CF-response fixtures, (7) docs path-existence check.]** — the lesson→guard coverage audit (`docs/analysis/2026-06-05-lesson-guard-coverage.md`) found 6 of 17 defect-bearing lessons are mechanizable-but-unguarded. Seed list, ordered by leverage: (1) **Scoped CI test gate** — add a `npm test`-style step to `.github/workflows/firebase-hosting-pull-request.yml` (today it only builds + deploys; **no guard runs in CI**, so even existing tests aren't enforced). ⚠️ **NOT turnkey — baseline verified 2026-06-05:** a naive `npm ci && vitest run && npm run lint` gate would block every PR. (a) **Lint is OFF the table** — `eslint .` reports **152,024 errors** (the flat config has never been enforced over `public/` legacy JS); a lint gate needs a drastically scoped config (e.g. rules only on `public/js/modules/ross/v2/**` + `functions/agent/**`), not the global run. (b) **The full `vitest run` is not green** — legacy suites fail (`tests/integration/sales-forecasting/*`, `tests/unit/sales-forecasting/forecast-engine`, `qms-*`), Playwright `*.spec.js` get mis-collected by vitest, and `functions/agent/*` tests need `functions/node_modules` (root `npm ci` doesn't install them). So the gate MUST be **scoped to the maintained-green suites** (`functions/{agent,billing,entitlements}/**`, `tests/unit/ross-*`) with `cd functions && npm ci` added and the legacy/e2e suites excluded via a committed `vitest.config.js` `include`/`exclude` (also exclude `.claude/**` so nested worktrees aren't double-scanned). Medium effort, high durable value. (2) **`vue/no-v-html` eslint rule** — scope it to the ross v2 components (the global lint is unusable); flags the live `RossHomeDesktop.vue:203` bug logged in the triage queue (fix that bug in the same/adjacent PR or the rule fails on enable); (3) **prototype-debris denylist test** (vitest grep over v2 components; recurred 2× — #48/#79); (4) **CF auth-posture unit tests** (`firebase-functions-test` already a devDep; assert admin-only CFs reject non-admin tokens — #89/#91 class); (5) **RTDB rules-unit-testing harness** (`@firebase/rules-unit-testing`; covers #125/#96 rule-cascade + writer-census class; highest setup cost, highest security value); (6) **shared CF-response fixtures** (replace hand-composed vitest mocks with fixtures imported from the server's `res.json` source — the field-verify pattern, 5×+2 the most-validated defect class); (7) **docs path-existence check** (#87). Each is its own small PR. Source: PR #143 audit + #143 review 🔵 + the 2026-06-05 CI-baseline pre-flight.

3b. **Export a shared `CLOUD_FUNCTIONS_BASE_URL` from `firebase-config.js`** — the cloudfunctions.net base URL is now hardcoded in `set-user-tier.js`, `enhanced-user-subscription-manager.js`, and others. PR #125 review NIT (left as-is in that PR to avoid churning shared config in a security PR). Region+project-id coupling in N places. Cheap consolidation.

4. **Extract shared auth helpers into `functions/auth.js`** — `verifyAuthToken` / `verifySuperAdmin` (+ now `verifyAdmin`) exist in BOTH `ross.js:48-90` and `functions/billing/auth.js` and `functions/entitlements/auth.js` (kept billing-local deliberately so the ledger doesn't depend on ross). Two copies will diverge once either is patched. Consolidate into a shared `functions/auth.js` both require. Flagged by the PR #113 review. Low risk, cheap.
5. **Hi-Fi v2 → v1 promotions** — partially superseded by Phase 5 PR 5 (sidebar cleanup removes v2 module deep-links from ROSS nav entirely). v1 modules stay reachable via `admin-dashboard.html` for the operator. Flag-flip to make v2 the canonical surface for guests/queue/analytics/campaigns/receipts can happen any time post-soak — it just doesn't change ROSS nav anymore.
6. **WhatsApp template UI polish** — surface improvements from `docs/plans/2026-02-19-whatsapp-template-management.md`
7. **QA cleanup** — execute `docs/plans/qa-cleanup-plan.md` (~65 test artifact files to remove)
8. **Sidebar dedup** — folded into Phase 5 PR 5
9. **Project status auto-derive** — read `PROJECT_BACKLOG.md` directly in the dashboard instead of maintaining `project-status.json` separately
10. ~~**Factor out `fetchLocationNames` helper**~~ — **already done** (found in `public/js/modules/ross/v2/utils/location-names.js`, imported by both stores; people-store has different `fetchUserLocations`). Stale entry removed 2026-05-20.
11. **`HfModal` / `HfConfirm` design-system components** — v2 needs a Hi-Fi-native modal pattern for cases that genuinely warrant an overlay (bulk-action confirms, multi-step wizards, etc.). PR #25 dropped SweetAlert2 in favour of inline confirms / inline error banners — that works for the People tab's single-row destructive actions but won't scale. Land in `public/js/design-system/hifi/components/` with --hf-* tokens, focus trap, ESC + scrim dismiss, and a worked example in `public/hifi/components.html`. Update CLAUDE.md convention: SweetAlert2 is v1-only.
12. **Fix KB doc field-name drift for ROSS templates** — `public/kb/features/ROSS.md` "Templates" block lists `id: string` but the actual server uses `templateId` (per seed + `rossCreateTemplate`). Same doc says template tasks are at `tasks: Task[]` but server stores `subtasks` (per `rossActivateWorkflow`, seed). Drift directly caused the PR #28 activate-from-template bug. One-line audit + edit. **[Done in PR #30]**
13. ~~**Catalog ROSS Cloud Functions**~~ — **Done in PR #87, 2026-05-20**. All 22 functions catalogued in `public/kb/features/ROSS.md#cloud-functions` (canonical) with allowed-fields per mutator table; `CLOUD_FUNCTIONS_CATALOG.md` gets 8-category summary + Source File Map entries for `ross.js` / `ross-tier.js` / `ross-workflow-builder.js`. Field-verify rule footnote cites the three negative-confirmation PRs (#28 / #72 / #86).
14. **Build `/profile-settings.html` for ROSS user account** — sidebar/topbar footer (PR #48) reads `auth.currentUser` reactively but the gear icon is non-interactive because no settings page exists. Account-scoped (display name, password change, email, notification prefs) — `receipt-settings.html` is per-restaurant config, different domain. Once shipped, wire the footer click (`RossHomeDesktop.vue:118` block + `RossHomeMobile.vue` topbar) to navigate there. Belongs in Phase 6 polish or as standalone follow-up.
15. **Fold `readUserTier` into `verifyUserOrAdmin` — eliminate the extra RTDB read on the hot path for tier-gated handlers.** PR #51 review finding #3. Currently `verifyUserOrAdmin` already reads `admins/{uid}` and possibly `subscriptions/{uid}`; adding `users/{uid}/tier` to the same call would drop one round trip per request. Touches the return shape consumed by 10+ handlers (`rossCreateWorkflow`, `rossUpdateWorkflow`, `rossDeleteWorkflow`, `rossActivateWorkflow`, `rossGetTemplates`, `rossManageTask`, etc.) — broader perf-refactor PR than this branch warranted. Long-term: consider promoting `tier` to a Firebase Auth custom claim so it rides the decoded token at zero RTDB cost.
16. **Backfill script polish (PR 1A follow-up)** — (a) add a pre-flight check warning if `database.rules.json` has been deployed with a `.validate` on `tier` BEFORE the backfill runs (rules+unbackfilled-records gives unpredictable behaviour); (b) pull `databaseURL` from `process.env.FIREBASE_DATABASE_URL` with a fallback, guarded against accidental copy-paste into other Firebase projects. PR #51 review findings #7 and #8. Cheap; bundle into a single script-polish PR.

### Low Priority / Nice-to-Have

13. Subscription admin charts migration (deferred from Chart.js audit, items #7–10)
14. Group overview v2 → v1 promotion
15. Onboarding flow improvements
16. **Edit-mode multi-location pill render** — `RossPlaybookWorkflowEditor` hydrates only the first `locationId` on edit (because `workflowById` flattens by first match in the rossGetWorkflows response). Locked caption already explains it but a future audit could surface all attached locations as read-only pills.

---

## 🐛 Bug Triage Queue

> Bugs discovered during sprint work — log here instead of fixing immediately unless sprint-blocking.

| Bug | Severity | Discovered | Blocking Sprint? |
|-----|----------|------------|-----------------|
| **`cleanupOldQueuesScheduled` cron has never deployed — export clobbered by `module.exports` reassignment** — `functions/queueAnalytics.js:343` does `exports.cleanupOldQueuesScheduled = onSchedule('every 24 hours', …)`, but line 357's `module.exports = { … }` REPLACES the whole exports object (CommonJS), dropping that assignment. So `require('./queueAnalytics').cleanupOldQueuesScheduled` is `undefined`, `functions/index.js:2243` exports undefined, and firebase reports `No function matches the filter: cleanupOldQueuesScheduled` — it's never been a deployable function. **Consequence: automated daily queue pruning is NOT running** (only the manual `cleanupOldQueues` HTTP fn works), compounding the unbounded-node retention bug below. Found 2026-06-01 during the Phase 7 ① ledger deploy (106 deployed vs 107 exports → traced to this phantom). Fix: include `cleanupOldQueuesScheduled` in the `module.exports = {…}` object (or move the assignment after it). One-line merge + redeploy. | Medium | 2026-06-01 (ledger deploy) | No |
| **`salesData` `.write` root still `auth != null`** — `database.rules.json`: the H-2 work (PR #96) tightened `salesData` *reads* to admin + per-child owner, but the root `.write` remains `auth != null`. The per-child `.validate` guards *overwriting* an existing record (owner check), but **any authenticated user can still create NEW `salesData` records** under arbitrary keys (no `userId`-match enforced on create). Low-impact (data pollution, not exfiltration) but inconsistent with the read-side lockdown. Fix: add a create-path `.validate`/`.write` requiring `newData.child('userId').val() === auth.uid || admin`. Surfaced by the #111 auto-review (NIT, on already-merged code). | Low | 2026-05-31 (PR #111 review, pre-existing) | No |
| **`rewardsProcessor.js:646` modulo bias in `generateFallbackCode`** — `crypto.randomBytes(6)` then `byte % 36` over-represents chars `0–3` by ~14% (`256 % 36 = 4`). Vastly better than the old `Math.random()`, but not uniform for a security-relevant voucher fallback code. Fix: rejection sampling (`while (code.length < 6) { const b = randomBytes(1)[0]; if (b < 252) code += chars[b % 36]; }`). ~0.09 extra bytes/code. Surfaced by the #111 auto-review (🟡, on already-merged OWASP-era code). | Low | 2026-05-31 (PR #111 review, pre-existing) | No |
| ✅✅ **CLOSED + DEPLOYED 2026-06-02** (PR #125). ~~**Entitlement self-grant — clients can write their own `subscriptions/{uid}/features`+`limits`**~~ — FIXED and LIVE IN PROD: `subscriptions/$uid` `.write` is admin-only and `features`/`limits` carry child `.validate:false` (verified live via `.settings/rules.json` readback). Gated deploy ran in order: 5 entitlement CFs (first-time) → hosting → rules lock → backfill (`recomputeEntitlements` swept 42/42 subscriptions). Emulator gate skipped (no Java in deploy env) — relied on the security-review's validated rule-shape reasoning + live readback; operator approved. Remaining: one real golden-path signup smoke (operator). Original report: `database.rules.json:28`: `$uid/.write` allows `auth.uid === $uid`, and the only `.validate` checks that `tier` exists in `subscriptionTiers`. Nothing stops an authed owner writing `features/<anyPremiumFlag>: true` or `limits/maxWorkflows: 99999` from the browser console — broken access control / privilege escalation. **Actively exercised** by client `subscription-service.js` (createSubscription/updateSubscription/startFreeTrial write features/limits from a hardcoded, RTDB-unsynced constant). OWASP follow-on the 2026-05-30 audit missed. **Fix is structural and scoped into Phase 7 ④a** (`docs/plans/2026-05-31-entitlements-addon-layer-design.md`): a server-side entitlement resolver becomes the *sole writer* of materialized `features`/`limits`, the 7 divergent writers (incl. client writes) route through CFs, then `subscriptions/$uid` `.write` locks to admin/server-only with a `.validate` rejecting client `features`/`limits`. Found during the entitlements design audit. | **High** | 2026-05-31 (entitlements design audit) | No (fixed by Phase 7 ④a build) |
| **OWASP audit — remaining findings (updated 2026-05-31)** — **All Criticals + all Highs done** (#89–#97), plus H-4b, and Mediums M-1/M-2/M-4/M-6/M-8/M-10 + Lows L-1/L-2 (#98–#102); L-6 already satisfied (CI uses `npm ci`); L-5 is a non-issue (emulator config is standard). **Still open:** **H-2 Bucket C** — `guests`, `queue`/`queues`, `receipts` root reads still `auth != null`; non-admin readers use root *location-queries* so can't be locked without a refactor — decide: location-indexed restructure (mirror `salesDataIndex`) vs CF-mediated reads vs accept residual (see design doc `docs/plans/2026-05-30-h2-cross-tenant-rtdb-design.md`); receipts may be lockable immediately (verify readers are admin-only). **M-3** SRI on all CDN `<script>`/`<link>` across every HTML page (mechanical but hash-generation + breakage-prone — own session). **M-5** `whatsapp-numbers`/`location-whatsapp-mapping` root reads + **M-7** `admin-claims` root read + **L-3** `rewards` root read — all Bucket-C-class (root-read tightening needs consumer mapping; fold into Bucket C). **M-9** MFA enforcement for admins (ops/config + auth-flow work). **L-4** min password 6→8 (needs a validation point in the password-input path). **CSP** (deferred from M-2 — per-page CDN/inline-source audit needed). **firebase-admin@13** major upgrade (clears the last 8 moderate `uuid` npm advisories from #97; needs deploy testing). Source: `owaspaudit20260530.html`. | Med (H-2 Bucket C, M-3), Low (rest) | OWASP audit 2026-05-30 | No |
| **OWASP operator decisions (2026-05-30)** — (a) GCP console: add HTTP-referrer restriction to Google Places API key (C-3 defence-in-depth, can't be done in code); (b) `getQueueStatus` auth posture — left unauthenticated in #91 (possible guest position-check); decide: lock to admin, add per-location token, or leave open. | n/a (decision) | PR #91 | No |
| **Recurrence-engine gap: nextDueDate never advances when daily workflow goes uncompleted** — Operator noted on PR #72 preview: "I see one workflow that hasn't been completed daily since it was created." `rossSubmitResponse` auto-completes the run when all required tasks are done, but does NOT advance the workflow's `nextDueDate` for the next cycle. A daily workflow created 10 days ago with no run completed reads as `daysLate: 10` instead of rolling forward. Fix needs: (a) on run completion, advance `nextDueDate` by recurrence period (`daily`/`weekly`/...); (b) decide policy when a cycle is fully missed (e.g. nextDueDate advances on the next "create run" attempt OR via a scheduled CF that walks every workflow nightly). Touches `rossSubmitResponse` + possibly `rossScheduledReminder` (already scheduled). Backlog candidate for a dedicated sprint — recurrence semantics need product input. | Medium (workflows accumulate large daysLate without progression) | PR #72 preview | No |
| **Unbounded-node retention policy ticket** — same root cause: in-app "Clear X" buttons are time-bombs without scheduled pruning. Now covers `/scanningData` (PR #70), `/wifiLogins`, `/activeUsers`, `/userPreferences`, `/rateLimitsWifi` (all per PR #73), and **`/billing/debitGuard/{uid}/{requestId}`** (PR #134 review T-2 — the slice-3 `requestId` is a fresh UUID per request, so guard nodes never dedupe and accumulate ~1/turn forever; a TTL/`expiresAt` sweep or 90-day prune closes it), and **`/ross/agentPending/{uid}/{turnId}`** (PR #136 review M-3 — abandoned/expired confirm actions are never consumed, so they orphan; a scheduled prune of `expiresAt < now − grace` closes it — bundle into the **slice 7** `ross/agent*` rules work). Options: (a) scheduled prune CF (`onSchedule('every 24 hours')`) using the chunked-delete pattern from `clearScanningData`, prune records older than 30/90 days; (b) TTL-on-write with `expiresAt`; (c) aggregate-then-discard to hourly/daily rollups. Needs product conversation on retention window per node (different windows likely OK — guest analytics may want 90d, rate-limits only need 1h). | Medium (recurrence guaranteed without retention policy) | PR #70 + PR #73 | No |
| **`updateWifiDisconnect` CF for disconnect-status tracking** — PR #73 made `logUserDisconnection` a no-op stub because the previous direct RTDB write to `/activeUsers/{sessionID}` is now blocked by admin-only rules. The disconnect signal is best-effort browser-unload (already unreliable on mobile) so the loss is minor, but admin analytics that filtered by `status === 'disconnected'` will see all records stuck at `active: true` indefinitely. Proper fix: small CF that takes a sessionID + anon-UID and updates the status server-side, called from the existing `beforeunload` handler. Anonymous auth UID must match the record's `anonUid` to authorize the update. | Low (degraded analytics; no user impact) | PR #73 | No |
| **Walled-garden audit at every deployed Meraki venue** — PR #73's `submitWifiLogin` CF lives at `*.cloudfunctions.net`. If a venue's Meraki captive-portal walled garden doesn't include that hostname, the CF call fails silently → fallback to localStorage offline-queue → guest still gets WiFi but no data captured. Audit each venue's walled-garden config (or document the canonical config IT teams should match). PR #73's fallback chain absorbs the failure so it's not user-visible, but it IS silent data loss for any venue with an incomplete whitelist. | Medium (silent data loss potential) | PR #73 | No |
| **Phone normalization to E.164 in `submitWifiLogin`** — `KNOWLEDGE BASE/PHONE_NUMBER_NORMALIZATION_AUDIT.md` documents the canonical normalization path. PR #73 stores raw `phoneNumber` as-received from intlTelInput's `getNumber()`. Adding libphonenumber server-side would normalize on write. Deferred from PR #73 to avoid the dep + audit-trail effort. | Low | PR #73 | No |
| **Open-redirect refusal log monitoring** — PR #73 added a host-pattern whitelist on `base_grant_url` before redirect. Refusals emit `console.error("WiFi LOGIN: refused to redirect…")` but stay on the client. If a new legitimate Meraki host family surfaces (whitepaper says hosts are dynamic), we won't know until a guest reports being blocked. Fix: forward the error to Cloud Logging via a tiny `logRefusedRedirect` CF, or include in PR 2's general telemetry layer. | Low (observability gap) | PR #73 | No |
| **`/scanningData` `.write: auth != null` audit** — separate from retention. PR #70 noted the rule is permissive (any authed user can write). The Meraki scanner POST flow writes here continuously; tightening without breaking that flow needs a parallel audit + CF pattern like `submitWifiLogin`. Defer until after Meraki scanner write path is fully mapped. | Low | PR #70 | No |
| **PR 2 — Hi-Fi v2 wifi-login rewrite** — closes deferred findings from PR #73 audit: #5 dual `activeUsers` schemas, #6 `ipinfo.io` no consent disclosure, #7 weak email/name regex, #8 localStorage PII unencrypted, #11 footer "v2.0" cosmetic. PLUS the automated review surfaced an offline-queue retry-path gap: `checkAndUploadOfflineData` only fires on captive-portal page load, but post-Meraki-redirect users won't return to that page in the same session — so queued records may be permanently lost for that session. PR 2 spec needs a different retry surface. Flag-gated via `customization/useV2WifiLogin`. | Medium | PR #73 | No |
| **Double-seed race on signup retry** — `rossSeedFirstWorkflow`'s idempotency pre-check is not transactional. A user double-clicking signup or hitting a network retry could pass the pre-check in both invocations and produce two seeded workflows. Mitigation options: (a) RTDB `transaction()` on the `firstWorkflowSeededAt` marker, OR (b) deterministic workflow ID per uid (e.g. `seed_{uid}` rather than push key) so duplicate write is overwrite. Exposure small; consequence mild (duplicate workflow, no corruption). | Low | PR #58 final review | No |
| **Workflow task edit not reflected in restarted run** — operator changed a task's inputType (temperature → photo) via the Phase 4e.1 task editor, hit save, then started a new run on the workflow. The run UI still showed the old inputType. Could be (a) the task editor not actually persisting `inputType` despite the allowedFields list including it; (b) `rossManageTask` server-side update not landing; (c) `getPlaybookWorkflows` caching or stale-read; (d) the operator hit the server's "Run is already completed" 409 case and was viewing the OLD completed run's task snapshot, not a fresh workflow read. Investigation needed before fix. | Medium | PR #65 preview | No |
| **Task editor UX needs single-Save + success feedback + auto-exit** — operator feedback on the Phase 4e.1 task editor (`RossPlaybookWorkflowTasksEditor.vue`): currently has a Save button per task row, and saving doesn't exit edit mode or confirm to the user. Desired: one Save button at the bottom that commits all dirty tasks, a success message, then auto-exit back to the workflow card view. Pre-existing scope from PR #32; not a Run-UI bug. | Low | PR #65 preview | No |
| **Subscription resolves "enterprise" tier for the operator's test account** — feature-access-control log on PR #65 preview shows `Resolved tier ID: enterprise (from: tierId)`. Tier system is supposed to be `free` / `all-in` only (PR #51). The operator's account has a legacy `tierId: 'enterprise'` value somewhere in `subscriptions/{uid}`. Unclear whether `enterprise` is treated as super-tier (all-in equivalent) downstream — likely silently falls through. Fix: audit operator's subscription record + decide whether to backfill to `all-in` or formalize `enterprise` as a third tier. | Medium | PR #65 preview | No |
| **Server auto-completes runs after a single response if workflow has small required-task count** — surfaced indirectly via PR #65 preview. Auto-completion check (`functions/ross.js:1311-1314`) treats all tasks with `required !== false` as required. The seeded Daily Opening Checklist has 4 subtasks but none carry an explicit `required` field — so all 4 are required, and auto-completion only fires when all 4 are responded. NOT actually a bug in the server logic per se, but: the operator hit "Run is already completed" 409, which implies SOMETHING auto-completed the run prematurely. Could be a separate run from an earlier test, OR a workflow with only 1 effective task. Worth a focused repro session before judging. | Medium | PR #65 preview | No |
| ✅ **CLOSED 2026-06-07 (#147)** — ~~`v-html="c.detail"` on the Ross home story card — latent stored-XSS surface~~ FIXED: `RossHomeDesktop.vue:203` now renders `{{ c.detail }}` (Vue auto-escapes); verified `c.detail` is plain text across `content.js`+`detectors.js` (no intentional markup); added `tests/unit/ross-home-desktop-no-vhtml.test.js` forward guard. _Original:_ `public/js/modules/ross/v2/components/RossHomeDesktop.vue:203` renders the card `detail` via `v-html` (presumably to allow `<strong>` emphasis). Today `detail` is sourced only from static copy (`v2/content.js`) and detector template strings built from server-derived **numbers/labels** (`v2/detectors.js` — `requiredTaskCount`, `nextDueLabel`, `aggSuffix`), so it is **not currently exploitable**. But it violates the "zero `v-html` on agent/server content" discipline (LESSON 2026-06-04 `vue/escaping`, #139) — the moment `detail` ever carries richer/free-text/agent-authored data it becomes a stored-XSS vector. Fix: render via `{{ }}` interpolation, or sanitize, or move the emphasis to structured fields. The `vue/no-v-html` eslint rule (guard-backfill seed item #2, `docs/analysis/2026-06-05-lesson-guard-coverage.md`) would flag this automatically once enabled. Confirmed live by the #143 automated review. | Low (latent — locally-sourced today) | 2026-06-05 (PR #143 review) | No |

### Operator design questions (PR #65 preview — for next-sprint design, not bugs)

| Question | Source |
|---|--------|
| What happens if a user mistakenly marks all tasks completed before actually checking? Should the run be retractable / responses revisable mid-run? Currently auto-save commits each response immediately with no undo affordance. | PR #65 final comment |
| What happens with multiple runs of a workflow over time? Should the Run page show today's run only, or all runs? Where does a fresh run come from when the previous one is completed? | PR #65 final comment |
| Where does an operator review the tasks that were completed on a finished run? The completion banner links to Activity tab, but Activity shows run summaries — task-level response review (per-task value + note + flagged status) is not currently surfaced anywhere. | PR #65 final comment |

---

## 🏁 Recently Completed (last 5)

| Feature | PR | Merged |
|---------|----|--------|
| **CF auth-posture guard tests (guard-backfill 3c seed #4)** — source-scan vitest suites asserting the admin-only Cloud Functions are genuinely gated: `requireAdmin` (`index.js`) + `verifyAdmin`/`verifySuperAdmin` (`entitlements/auth.js`) each have dual-factor structure (custom claim AND RTDB record; either alone → 403), and 8 CFs (`entitlementSetTier`/`GrantAddOn`/`CancelAddOn` + `getGoogleConfig` + 4 queue mutators) call their guard. 20 tests, no security holes found. Source-scan shape chosen because `vi.mock('firebase-admin')` can't intercept CJS `require()` in nested `functions/node_modules`. Secondary debris-denylist task dropped (entangled with intentional illustrative content). Review: 🟡 (count inflated by import line — fixed to `await verifyAuthToken(` anchor, floor `>=4`) + 2 🔵 folded. | #150 | 2026-06-07 |
| **askRoss slice-5 client polish** — the deferred Ask Ross ⌘K follow-ups: (a) `RossHomeMobile.vue` "Ask Ross" pill wired to the real modal (was decorative debris); (b) fetch cancellation — `streamRossChat`/`resumeRossChat` return `{promise, abort}`, modal aborts on close/new-chat + resets state so late SSE events can't fold into a reopened conversation; (c) ⌘K cold-start — `RossAskModal` hoisted outside the `.ross-home` v-if so the palette is live before the feed loads; (d) `:key` now `${resetCount}-${i}` + a "New chat" affordance. 24 tests, build green, no new `v-html`. Review: no must-fix, 2 🔵 (cosmetic + reflect-reminder). | #148 | 2026-06-07 |
| **askRoss eval-harness Group-A fairness fixes (3aa)** — made the live eval grade Ross honestly: realistic 2026 fixture dates (`BASE_2026` anchor — kills the epoch-1970 "20,609 days overdue" false-fail), `transcript.toolResults` capture seam (pairs each `tool_result` to its tool name via a `tool_use.id→name` map from `loop.messages`), and judge grounding (inject `system[1]` owner-context as a synthetic `__systemContext__` entry + drop the redundant `getWorkflowDigest` tool expectation on the 3 preloaded-digest cases). Group B (`q-staff`/`q-staff-runs`/`snooze`) left failing — a real product signal, not loosened. 24 tests (stricter capture guard). Operator re-runs `npm run eval` post-merge for ~18/21. Review: LGTM, 🟡+3🔵 folded — incl. catching that the reviewer's suggested `toolUseId` match referenced a field that doesn't exist on `toolsCalled`. | #149 | 2026-06-07 |
| **Ross home card v-html → stored-XSS fix** — `RossHomeDesktop.vue:203` rendered agent/detector-derived `c.detail` via `v-html`; swapped to `{{ c.detail }}` (Vue auto-escaping) after verifying `detail` is plain text across `content.js`+`detectors.js`. Latent stored-XSS closed (any HTML in interpolated names/labels would have executed). Added `tests/unit/ross-home-desktop-no-vhtml.test.js` forward guard. Security review: LGTM. Tiny single-purpose PR. | #147 | 2026-06-07 |
| **Phase 7 ② askRoss — slice 6 LIVE eval harness (the final build slice; askRoss build COMPLETE)** — `functions/agent/evals/`: a live harness (`npm run eval`) driving the real agent loop against the real Anthropic API with a seeded **two-tenant** in-memory RTDB, grading Ross's *judgment* — tool-selection, propose-not-act, refusal, pre-flight gates (free — never hit the API), grounding/tone (Haiku-as-judge, fail-closed parse), and a **cross-tenant isolation probe** (continuous regression for #144). 21 cases; 6 small files + 3 backward-compatible prod seams (temperature on streamTurn/runAgentLoop; export buildSystemForOwner). Machinery unit-tested in CI (18 tests); the run is live/manual; driver reuses prod `runGates`/`runAgentLoop`/`buildSystemForOwner` (no debit, no persistence). Subagent-driven 8-task build, per-task + final cross-cutting review (no blockers); 2 integration bugs caught+fixed (runAgentLoop returns-not-emits; live-client reset). **196/196 functions tests + build green.** ⚠ One operator step: a live `npm run eval` with the key. Follow-ups (harvester from real `agentChats`, nightly cron, judge tool-results) → backlog. | #142 | 2026-06-05 |
| **Phase 7 ② askRoss — cross-tenant read fix in `getRunHistory` (HIGH security)** — audit of all 8 agent adapters (operator asked how to stop bad actors extracting data they can't see). The agent runs via the Admin SDK (rules bypassed) → adapter code is the only tenant boundary. `getRunHistory` resolved a workflow owner from the GLOBAL `workflowsByLocation` index with no caller-access check → an authed user passing another tenant's (workflowId, locationId) leaked their run history (confirmed HIGH IDOR). Fix: caller must BE the owner or have the location in `userLocations/${caller}`. Independent security-reviewer confirmed it was the ONLY hole. +2 cross-tenant guards. **Deployed + verified live** (`rosschat-00005-not`). 177/177 functions green. Follow-up: shared agent location-access helper (3a). | #144 | 2026-06-05 |
| **Reflect protocol — lesson→guard coverage audit + Step 11 guard-first flip** — read-heavy audit classifying all 20 `LESSONS.md` entries (+ SELF_OPT validated rows) by whether a shipped/near-shipped defect is GUARDED / MECHANIZABLE-UNGUARDED / INHERENTLY-PROCESS, each with file:line evidence (`docs/analysis/2026-06-05-lesson-guard-coverage.md`). Ratio of the 17 defect-bearing entries: **6 / 6 / 5** → mechanizable-but-unguarded is non-trivial (tied with guarded), so flipped the CLAUDE.md Step 11 default: a validated/shipped-defect lesson ships a **mechanical guard** (prose demoted to the guard's comment), genuinely unmechanizable lessons stay prose. Mechanism proven in-repo by #140's `tools.test.js`. Meta-finding: **no CI workflow runs `npm test`/`npm run lint`** — guards exist but aren't enforced. Surgical one-bullet CLAUDE.md insert; no feature code. Review folded: doc line-number alignment, logged the live `RossHomeDesktop.vue:203` v-html surface (triage queue), added backlog item 3c. Follow-up guard-backfill is item 3c (seed list); a 2026-06-05 pre-flight found the CI gate is NOT turnkey (152k lint errors; legacy suites not green) → must be scoped. | #143 | 2026-06-05 |
| **Phase 7 ② askRoss — tool-schema dialect fix (Ask Ross was 400ing every turn)** — `toAnthropicTools` emitted the OpenAPI-3.0/draft-4 dialect (boolean `exclusiveMinimum` from `z.number().positive()` on `getRunHistory.limit`/`snoozeCard.hours`); the Anthropic Messages API requires **JSON Schema draft 2020-12**, so it rejected the tools array on EVERY turn → "Ross hit a problem mid-answer." Found via prod `rossChat` logs while smoke-testing the #139 preview (all 139 server tests mocked the LLM client, so the dialect was never validated against the real API). Fix: drop `{target:'openApi3'}` → default `jsonSchema7` (numeric `exclusiveMinimum`). **Verified live in prod** (clean turn, 1c debit). +2 regression guards scanning every tool's schema for draft-4 forms; 175/175 functions green. **rossChat redeployed** (`rosschat-00004-qun`). | #140 | 2026-06-04 |
| **Phase 7 ② askRoss — slice 5 Ask Ross ⌘K client (SSE streaming + inline confirm-cards)** — replaces the scripted `askRoss()` stub with a live ⌘K command-palette modal streaming a multi-turn Ross conversation from the `rossChat` SSE CF. 3 pure/thin modules (`ross-agent-sse.js` frame parser, `ross-agent-conversation.js` immutable reducer, `ross-agent-client.js` `fetch()`+`ReadableStream` transport with a **FLAT** body — `EventSource` can't POST/auth) + 3 components (modal + message + inline confirm-card, all text-interpolated/no `v-html`). Opens from ⌘K / rail / header / `#ask=` deep-link. 20 unit tests on the pure logic; build + 173/173 functions green; subagent-driven 8-task build with per-task + final cross-cutting review (2 findings folded, 1 declined). **Confirmed working live in prod** (after #140). Desktop only — mobile + cancellation backlogged (3a). | #139 | 2026-06-04 |
| **Phase 7 ② askRoss — slice 4 confirm-flow pause/resume + 4 confirm-tier tools** — the agent can now author the playbook with the owner in the loop. A `confirm`-tier tool PAUSES → persists a pending action at `ross/agentPending/{uid}/{turnId}` (10-min expiry, server-keyed, uid-scoped) + debits the tokens spent so far (§2.1 — abandoned confirms still bill) + emits a `confirm` card → the owner's resume (`{resumeTurnId, decision}`) runs the pre-flight gates again (kill-switch holds), checks expiry (fail-closed), **atomically one-time-consumes** the pending, executes (audited `confirmed:<uid>`) or declines, then continues the loop (Request-B debit, distinct requestId). HALF B: extracted `rossActivateWorkflow`/`rossCreateWorkflow`/`rossUpdateWorkflow` write-logic into owner-callable cores in `ross.js` (`*AsOwner`) shared by BOTH the CF handler and the agent adapters (byte-for-byte handler parity; coded errors → HTTP status or tool_result); promoted the 4 confirm tools pending→READY. Those 3 handlers had ZERO prior tests → the new `ross-workflow-ops.test.js` IS the regression net. 8 commits / 7 TDD phases; **two review rounds — local security-reviewer (H-1 gate-on-resume + M-1 fail-closed expiry + M-2 payload guard) + automated (M-1 pending-flag symmetry, M-2 email-in-audit, M-3 confirm-summary fallbacks), all folded.** 166/166 functions tests + build green. **⚠ Deploy: rossChat is LIVE — redeploy needed** (`ANTHROPIC_API_KEY` provisioned; `cd functions && npm install` first). | #136 | 2026-06-03 |
| **Phase 7 ② askRoss — slice 3 `rossChat` reactive SSE CF (the project's first AI-inference CF)** — `functions/agent/rossChat.js`: the v1 reactive engine over the raw Anthropic Messages API. Four pre-flight gates (kill switch → per-owner enable → `features.rossAgent` → ledger balance; super-admins skip entitlement+balance, not the kill switch) → cached two-breakpoint prompt + bounded history → `runAgentLoop` (streams text; auto tools via `executeTool`; non-auto refused server-side — confirm-flow is slice 4) → single idempotent ledger debit (`recordUsageAndDebit` gained an optional `requestId` guard) → persist turn + SSE. New `verifyRossAgentAccess` (recognises `rossAgent`; `verifyUserOrAdmin` untouched) + `ross.js` `__setDbForTests` seam. Built in 6 TDD phases; two review rounds (local security+code, then automated — **no blockers**) folded: server-generated `requestId` (closed a free-turn vector), threadId/clientToday validation, best-effort loop debits partial spend on LLM error, structured SSE events, warm-client reuse. **139/139 functions tests + build green.** Catalog updated (both copies). **⚠ Deploy: secret-first** (`functions:secrets:set ANTHROPIC_API_KEY`) — server-only, no client yet. | #134 | 2026-06-03 |
| **Phase 7 ④a — `maxWorkflows` cap, MERGED + DEPLOYED (first entitlement-limit enforcement, §8)** — `rossActivateWorkflow`/`rossCreateWorkflow` gate active workflow count (`status !== 'paused'`) vs materialized `subscriptions/{uid}/limits/maxWorkflows` → 403 `WORKFLOW_LIMIT_REACHED` + v2 "Upgrade plan" link; superAdmin bypass; absent/`-1` ⇒ unlimited. Pure `functions/ross-workflow-cap.js` (10 unit tests). **Shipped live:** CFs + hosting deployed; `subscriptionTiers/free/limits/maxWorkflows = 5` set via RTDB REST PATCH (firebase MCP errored + CLI stdin n/a on Windows); recompute 43/43; verified free users got `maxWorkflows:5`. Review 0 must-fix, 3 🟡 folded (upgradeUrl origin guard, TOCTOU comment, ROSS.md drift). **Completes ④a.** | #127 | 2026-06-02 |
| **Phase 7 ④a — `subscriptions/$uid` RULE LOCK (closes the High self-grant vuln)** — `.write` → admin-only + child `.validate:false` on `features`/`limits`; the resolver (Admin SDK) is the sole writer. 6 admin tier writers routed through `entitlementSetTier` (relaxed superAdmin→admin via new dual claim+RTDB `verifyAdmin`); signup Path-B fallback dropped (registerUser is sole provisioning path, errors surface loudly); `user-subscription.js` + `subscription-tier-fix.js` owner-writers made read-only; `locationIds` writers confirmed DEAD (no importers). **Corrected the merged plan's buggy §2 shape** (parent `!newData.hasChild` false-rejects multi-path admin writes → child `.validate:false`). security-reviewer APPROVED the vuln closure + caught a missed legacy `admin_tools/*.html` writer (folded). 2 🟡 + 3 🔵 review findings folded (verifyAdmin dual-check, Promise.all bulk loops, JSDoc). 49 tests + build green. **⚠ DEPLOY GATED — strict CFs→hosting→emulator→rules→backfill→signup-smoke.** | #125 | 2026-06-02 |
| **OWASP audit report recovery** — recovered `docs/security/owasp-audit-2026-05-30.html` (1198 lines, the backlog's cited security source-of-truth) which had never been merged and lived only on an orphaned branch. Caught during a repo house-clean (also pruned 100 merged remote branches → 2, removed 3 stale worktrees). Cherry-pick recovery → docs-only PR. | #124 | 2026-06-01 |
| **Phase 7 ④a — writer consolidation (PR3, additive half)** — server-side resolver now materializes `subscriptions/{uid}/features`+`limits` on the live write paths: `registerUser`/`createUserAccount` call `recomputeEntitlements` after the subscription write; `subscriptionStatusManager` recomputes on expiry (strip-to-Free immediately, not 24h later). All best-effort (account already written; readers fall back to tier constants per audit finding B; daily cron self-heals). 3 dead `subscription-service.js` client writers stripped of features/limits (⚠ DEAD-marked). `user-subscription.js` `upgradeToPlan` fake-charge free-upgrade → `/upgrade.html` redirect (operator decision; one-line swap to real checkout when ③ lands). NO rule change (deferred to PR4). Automated review LGTM; 🟡 folded (stronger DEAD warning), 🔵 captured the extra admin-writer audit targets for PR4. **CFs deployed to prod 2026-06-01.** | #120 | 2026-06-01 |
| **#117 review fold-ins — catalog the 5 entitlement CFs + GetEffective error-status** — resolved the open #117 review findings (caught at session start by re-reading #113–#117 review comments — the 🔴 had been merged unactioned). Added the **Entitlements (Phase 7 ④a)** section to both `CLOUD_FUNCTIONS_CATALOG.md` copies (🔴 catalog drift — the ② Agent build plans against this catalog); switched `entitlementGetEffective`'s `badRequest('Forbidden')` + regex-remap-to-403 to `authError()` (statusCode 403, exported from `entitlements/auth.js`); documented the intentional any-admin-read policy in JSDoc. No behaviour change; 49 entitlements+billing tests pass. | #118 | 2026-06-01 |
| **OWASP remediation — Highs + Mediums/Lows (10 PRs)** — completes the audit through all Highs plus most Mediums/Lows. **#93** H-4/H-7/H-10, **#94** H-5/H-6 (+ shared `functions/cors-allowlist.js`), **#95** H-3/H-4b, **#96** H-2 Bucket A/B (6 root nodes locked + `userLocations` child rules; design doc for the 3-bucket split), **#97** H-9 (npm audit, lockfile-only), **#98** M-6/M-10/L-1/L-2, **#99** M-4 XSS, **#100** M-1, **#101** M-2 headers, **#102** M-8 POPIA. Every PR auto-reviewed; 2 must-fixes caught + fixed pre-merge (#96 ownerId fallback, #102 phoneNumber indexes). Consumer-mapping (3 parallel agents) drove the H-2 bucket split — guests/queue deferred to Bucket C because RTDB can't rule-filter root location-queries. | #93–#102 | 2026-05-31 |
| **OWASP Top-10 audit remediation (3 PRs)** — 5/5 Criticals + 3 Highs closed from operator-supplied audit. **#89:** C-1 operator-precedence admin-bypass (`!token.admin === true` → `token.admin !== true`, 12 CFs — every admin guard was a no-op), C-2 hardcoded secrets → `defineSecret` + `INITIAL_SETUP_ENABLED` gate + dropped header logging, C-4 public-writable `receiptPatternLogs`/`debug` RTDB nodes. **#90:** C-5 OCR SSRF allowlist (`new URL().hostname` parse, both call paths), H-8 Storage `/receipts/` public write closed. **#91:** C-3 `getGoogleConfig` admin-gated + new shared `requireAdmin(req,res)` helper, H-1 four queue mutators gated. All reviewed; #91 caught a real auth≠authz must-fix (token-only insufficient for a billable-key endpoint). Non-regressive: grepped all 12 C-1 functions — zero non-admin frontend callers. Deploy needs secret provisioning + rotation. | #89, #90, #91 | 2026-05-30 |
| **ROSS Cloud Functions catalog + allowed-fields per mutator** — closed backlog item #12 (open since PR #30 review). `public/kb/features/ROSS.md#cloud-functions` is now the canonical source with all 22 functions documented (was 18 — added rossSeedFirstWorkflow, rossManageStaff, rossV2Snooze, rossGetHomeWorkflowDigest). New "Allowed-fields per mutator" subsection lists the 4 server `allowedFields` constants (rossUpdateWorkflow, rossUpdateTemplate, rossManageTask, rossManageStaff) with file:line refs — the highest-value payload, would have caught the PR #28 `t.id` bug at planning time. CLOUD_FUNCTIONS_CATALOG.md gets 8-category summary + Source File Map entries for the 3 missing ross-*.js files. Field-verify rule footnote cites the three negative-confirmation PRs (#28 / #72 / #86). Automated review caught a forward-reference to PR #86's workflow-status.js — fixed via "(ships with PR #86)" qualifier, then switched to past tense after #86 merged + #87 rebased. | #87 | 2026-05-20 |
| **Missed/Overdue counter consistency (PR #72 follow-up)** — fix the operator-reported inconsistency where home card warned about Missed workflows but Playbook + Activity tabs both said "Overdue: 0". Root cause: server's `locData.status` is 'active' from activation onwards and never updated to 'overdue' — `w.status === 'overdue'` filter always returned 0. Replaced with client-side derivation in shared `workflow-status.js` helper (MISSED_DAYS_THRESHOLD + computeDaysLate + isOverdue + isMissed); same helper consumed by both stores AND detectors.js (single source of truth across three surfaces). RossPlaybook + RossActivity stat tiles now use dynamic Missed/Overdue label mirroring the home card. 25 new vitest cases including a real-server-shape contract test that reproduces the operator's exact bug. | #86 | 2026-05-20 |
| **Phase 6 stretch — Weekly Compliance Sweep starter template** — first end-to-end curated ROSS template that exercises Phase 4e.2 per-subtask `inputType` + `inputConfig`. 5 subtasks × 4 input types: 2× temperature (fridge ≤ 4°C, freezer ≤ -18°C, requiredNote on out-of-range, auto-flag), yes_no (fire extinguisher in date), checkbox (first aid kit), text (incidents log, maxLength 500). `tier: 'free'`, weekly recurrence, daysBeforeAlert [2,1]. Two files: updated seed (14 templates now, log line uses `templates.length`) + new idempotent prod-additive script matching the PR #51/#58 one-off script pattern. No CF/client changes. | #83 | 2026-05-20 |
| **7-callable v2-signature sweep** — mechanical migration of the 7 remaining v1 `(data, context)` onCall handlers in `functions/index.js` to v2 `(request)` signature following PR #67's canonical pattern: `getQMSTierInfo`, `getQMSUsageStats`, `validateQMSFeatureAccess`, `validateQMSWhatsAppIntegration`, `performanceTest`, `runSystemOptimization`, `getSystemMetrics`. Each callable: signature swap + `context.auth` → `auth` + `functions.https.HttpsError` → `HttpsError` + outer-catch re-throw guard (included from the start per the 2026-05-15 scope-discipline-inverse lesson — fix-X makes previously-dead validation paths newly reachable). KB catalog synced (both copies). Build green, 0 residual v1 references. None on user-facing critical path per the PR #67 audit. | #82 | 2026-05-20 |
| **WiFi login Hi-Fi v2 (PR 2 of 2)** — `wifi-login.html` rewritten in Hi-Fi tokens with Sparks-only branding. Adds single `marketingConsent: boolean` field to `wifiLogins`. Meraki redirect flow preserved byte-for-byte (operator guardrail). Closes deferred audit findings #5-8 + #11 from PR #73 + offline-queue retry-path gap from the PR #73 review. | #81 | 2026-05-19 |
| **Bug-knock-off bundle** — three small backlog fixes shipped sequentially: PR #77 deleted dead `public/tools/archive/temp-setup.html` (called `tempClearData` CF removed in PR #70); PR #78 dropped `?v=${Date.now()}` Vite-breaking cache-busters at `feature-access-control.js:64` + `queue-management.js:1646`; PR #79 fixed mobile bottom-nav (decorative `<button>` with no href → real `<a>` with destinations) AND folded mobile card-footer hardcoded mock strings ("Target 28% · 3 days running" etc.) replaced with dynamic `cardMeta(c)` helper reading `c.sidecar` fields. Second occurrence of the prototype-debris-on-v2-surfaces pattern (PR #48 was the first). | #77, #78, #79 | 2026-05-19 |
| **Concierge home active-run surfacing (Phase 6 PR 4)** — new `rossGetHomeWorkflowDigest` CF (`onRequest` + `verifyUserOrAdmin`, read-only) + thin client detector `detectActiveWorkflows` slot 1 on `/ross.html`. Hybrid card mix: workflow card wins slot 1, food-cost/VIP/revenue detectors keep slots 2-3. 5 card variants (Overdue / In progress / Due today / Recently completed / All clear) + LEARNING_MODE fallback. 38 unit tests. Two preview-driven defects folded in (numeric-nextDueDate normalisation + canonical run schema with no `status` field). Operator design call mid-PR: "Missed" framing at `daysLate >= 4`. Home feed auto-reload on every mount. Same root-cause pattern as PR #28 — KB doc shape ≠ CF write-path shape, second occurrence of the validated 2026-05-01 SELF_OPT. | #72 | 2026-05-19 |
| **WiFi login security hardening (PR 1 of 2)** — closed 4 highest-severity audit findings on `wifi-login.html` flow. New `submitWifiLogin` v2 onCall CF: anonymous Firebase Auth + per-UID 5s debounce + shape validation + server-generated push-key sessionID + atomic multi-path write to `wifiLogins/{sessionID}` + `activeUsers/{client_mac\|sessionID}` + `rateLimitsWifi/{uid}`. Rules tightened: `.write` and `.read` on `wifiLogins`/`activeUsers`/`userPreferences` flipped from `auth != null` to admin-only (read scope expansion justified — guest PII was readable by any signed-in user). Added `rateLimitsWifi` node. Open-redirect guard: host-pattern whitelist (`n##.meraki.com` / `.network-auth.com`) on `base_grant_url` before redirect. Adjacent cleanup: `storeUserPreferences` + `logUserDisconnection` → no-op stubs (preferences were dead code; disconnect is now admin-only, deferred to backlog). Plan in `docs/plans/2026-05-19-wifi-login-security-plan.md`. Deployed CF + rules pre-merge per 2026-05-01 lesson; hosting closes the brief data-capture gap. Automated review caught one self-inflicted 🔴 (DATABASE_RULES_GUIDE doc claimed `.validate` fires on Admin SDK writes — contradicts 2026-05-12 LESSON I had loaded); fixed in fold-in commit `da527973` before merge. | #73 | 2026-05-19 |
| `clearScanningData` chunked-delete + WRITE_TOO_BIG fix (mid-session bug-triage pivot) — admin dashboard "Clear Scanning Data" button hit RTDB's ~16 MB single-write ceiling on grown `/scanningData` node. Migrated CF from `onRequest` (+ broken `httpsCallable` client shape that has been silently failing in try/catch) to v2 `onCall` + chunked delete via `orderByKey().limitToFirst(500)` + multi-path null `update()`. Hard cap of 200 batches (100K records) per invocation; CF returns `{ done }` so client loops across calls for larger nodes. Folded `tempClearData` deletion (hardcoded `MerakiSetup2024!` secret, same WRITE_TOO_BIG bug, same file/concern). Operator pre-unblocked via `firebase database:remove /scanningData` CLI (CLI bypasses per-write limit via own chunking). Deployed + smoke-tested before merge. | #70 | 2026-05-19 |
| `registerUser` callable Gen-2 signature fix (HIGH-sev bug-triage) — migrated v1 `(data, context) =>` to v2 `(request) =>` with `auth`/`data` destructured. Closed silent prod degradation that had been masking the Path A code path since the firebase-functions v7 upgrade. Pre-merge review fold-in: re-throw `HttpsError` from outer catch (preserves `invalid-argument` codes; previously rewrapped as `internal`), KB doc sync (`CLOUD_FUNCTIONS_CATALOG.md`), comment-block trim. Validated end-to-end on prod via fresh signup + invalid-tier negative test. | #67 | 2026-05-15 |
| ROSS Run execution UI (Phase 6 PR 3) — `?tab=run` page covering all 10 inputTypes (checkbox / text / number / temperature with auto-flag + client pre-flight `requiredNote` + server-422 safety net via `store.pendingNoteTaskId` + card watcher / yes_no / dropdown / timestamp / rating / photo / signature placeholders), auto-save on commit, sticky summary bar, inline completion banner. 40 unit tests across 4 new suites. Pre-merge fold-in resolved 6 of 7 automated-review items (1 🔴 must-fix + 1 🟡 + 4 🔵 NITs). | #65 | 2026-05-15 |
| Housekeeping — gitignore local agent/editor state + Session Opening Protocol clarification + dead-worktree cleanup | #62, #63 | 2026-05-13 |

---

## 📝 How to Use This File

**At session start:** Claude reads this, states the Sprint Goal, and confirms whether to stay on track or pivot.

**During session:** If a bug surfaces, it goes in Bug Triage Queue unless it blocks the sprint. If the user pivots, update the Sprint Goal line and note the change.

**At session end:** Claude updates this file — marks sprint tasks done `[x]`, moves completed features to Recently Completed, logs any new bugs discovered, clears the In Progress row if the branch was merged.

**To change sprint focus:** Tell Claude "new sprint goal: [X]" — it will update this file and reorient.

**Two sources of truth, kept in sync:** This markdown is canonical. `public/data/project-status.json` mirrors it for the in-app `Project Status` dashboard — both get updated together until the dashboard auto-derives from this file (item #8 in the backlog).
